---
phase: 03-gemini-live-nova-sonic-stub
plan: "03"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/eq_chatbot_core/realtime/providers/nova.py
  - tests/unit/realtime/test_realtime_nova.py
autonomous: true
requirements:
  - PROV-08
  - QUAL-01
must_haves:
  truths:
    - "isinstance(NovaSonicStub(), RealtimeAdapterContract) is True (structural Protocol conformance via @runtime_checkable duck-typing)"
    - "Every one of the 11 RealtimeAdapterContract methods raises NotImplementedError with a message containing 'v1.9.0'"
    - "nova.py is stdlib-only — no websockets, boto3, or AWS extras imports anywhere in the file"
    - "D-07: stub is under 30 LOC; error messages are concise and reference v1.9.0"
    - "D-08: get_realtime_provider('nova_sonic') resolves via factory — but that wiring is in Plan 04; this plan verifies structural conformance only"
    - "test_realtime_nova.py covers all three test classes: TestContractConformance, TestAllMethodsRaise, TestErrorMessages"
  artifacts:
    - path: src/eq_chatbot_core/realtime/providers/nova.py
      provides: "NovaSonicStub — structural RealtimeAdapterContract conformance, all methods raise NotImplementedError"
      contains: "NovaSonicStub"
    - path: tests/unit/realtime/test_realtime_nova.py
      provides: "Unit tests for PROV-08: isinstance check, all 10 async methods raise, iter_normalized_events raises, v1.9.0 message"
  key_links:
    - from: NovaSonicStub
      to: RealtimeAdapterContract
      via: "@runtime_checkable Protocol duck-typing — no inheritance needed"
      pattern: "isinstance.*NovaSonicStub.*RealtimeAdapterContract"
---

<objective>
Create src/eq_chatbot_core/realtime/providers/nova.py (NovaSonicStub, <30 LOC) and tests/unit/realtime/test_realtime_nova.py (unit test suite for PROV-08).

Purpose: Deliver the NovaSonicStub that satisfies RealtimeAdapterContract structurally (D-07/D-08/PROV-08) and its unit test suite covering isinstance conformance, all-methods-raise, and v1.9.0 message reference. NovaSonicStub is stdlib-only — no websockets or AWS dependency — making it suitable for any consumer test suite without extra installs.

Output: nova.py (<30 LOC, stdlib only), test_realtime_nova.py (3 test classes, covers PROV-08 + QUAL-01 Nova portion). No factory registration in this plan — that is Plan 04.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/ROADMAP.md
@/Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/03-gemini-live-nova-sonic-stub/03-CONTEXT.md
@/Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/03-gemini-live-nova-sonic-stub/03-RESEARCH.md
@/Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/03-gemini-live-nova-sonic-stub/03-PATTERNS.md

<interfaces>
<!-- RealtimeAdapterContract — 11 methods that NovaSonicStub must implement structurally. -->

From src/eq_chatbot_core/realtime/contracts.py:
  @runtime_checkable
  class RealtimeAdapterContract(Protocol):
      async def connect(self) -> None: ...
      async def close(self) -> None: ...
      async def initialize_session(self, *, instructions: str | None = None, voice: str | None = None, tools: list[Any] | None = None) -> None: ...
      async def update_session(self, payload: dict[str, Any]) -> None: ...
      async def append_client_audio(self, pcm16_audio: bytes) -> None: ...
      async def commit_client_turn(self) -> None: ...
      async def create_response(self) -> None: ...
      async def cancel_response(self, *, response_id: str | None = None) -> None: ...
      async def register_tools(self, tools: list[Any]) -> None: ...
      async def submit_tool_result(self, *, call_id: str, output: str) -> None: ...
      def iter_normalized_events(self) -> AsyncIterator[NormalizedRealtimeEvent]: ...

# These are the EXACT signatures NovaSonicStub must match for isinstance() to return True.
# Do NOT inherit from BaseRealtimeWebsocketClient or RealtimeProvider ABC — structural only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create NovaSonicStub (<30 LOC, stdlib-only) and test_realtime_nova.py</name>
  <files>src/eq_chatbot_core/realtime/providers/nova.py, tests/unit/realtime/test_realtime_nova.py</files>
  <read_first>
    - src/eq_chatbot_core/realtime/contracts.py (lines 70-115: RealtimeAdapterContract Protocol — copy the exact 11 method signatures into NovaSonicStub, adapted from `...` body to `raise NotImplementedError(...)`)
    - .planning/phases/03-gemini-live-nova-sonic-stub/03-PATTERNS.md §"nova.py (provider stub)" — full stub pattern (28 LOC)
    - .planning/phases/03-gemini-live-nova-sonic-stub/03-PATTERNS.md §"test_realtime_nova.py" — full test class structure
    - .planning/phases/03-gemini-live-nova-sonic-stub/03-RESEARCH.md §"Pattern 6: Nova Sonic Stub — Structural Conformance Without ABC" and §"Pitfall 6" (iter_normalized_events return type must match Protocol exactly) and §"Pitfall 7" (no websockets/boto3 imports)
    - /Users/picard/gitbase/GlassAgents/backend/realtime/providers/nova.py (confirm GlassAgents stub has incompatible signatures — do NOT copy it; write NovaSonicStub fresh per library Protocol)
  </read_first>
  <behavior>
    **nova.py:**
    - Module docstring: "AWS Nova Sonic realtime provider stub. Production implementation planned for v1.9.0. get_realtime_provider('nova_sonic') resolves correctly (D-08) but every method raises NotImplementedError pointing to the target version. No websockets or boto3 imports — this file is stdlib-only (Pitfall 7)."
    - Imports: `from __future__ import annotations`, `from collections.abc import AsyncIterator`, `from typing import Any` — NOTHING ELSE (no websockets, no boto3, no eq_chatbot_core.realtime.websocket_client)
    - `class NovaSonicStub:` — does NOT inherit from any base class (not BaseRealtimeWebsocketClient, not RealtimeProvider ABC, not RealtimeAdapterContract)
    - Class docstring: "AWS Nova Sonic placeholder. Production implementation in v1.9.0."
    - 11 method stubs — signatures EXACTLY match RealtimeAdapterContract Protocol (including keyword-only params): connect, close, initialize_session(*, instructions, voice, tools), update_session(payload), append_client_audio(pcm16_audio), commit_client_turn, create_response, cancel_response(*, response_id=None), register_tools(tools), submit_tool_result(*, call_id, output), iter_normalized_events — all raise `NotImplementedError("Nova Sonic not implemented; available in v1.9.0")`
    - `iter_normalized_events` return type annotation: `-> AsyncIterator[Any]` (matches Protocol structurally; Pitfall 6 — wrong annotation breaks type checkers)
    - `__all__ = ["NovaSonicStub"]`
    - Total: <30 LOC (target: 28 LOC per PATTERNS.md)

    **test_realtime_nova.py:**
    - No websockets mock needed — NovaSonicStub is stdlib-only; conftest.py mock_websockets_module autouse does not interfere
    - Imports: `pytest`, `from eq_chatbot_core.realtime.contracts import RealtimeAdapterContract`, `from eq_chatbot_core.realtime.providers.nova import NovaSonicStub`
    - `class TestContractConformance:` — `test_isinstance_realtime_adapter_contract`: asserts `isinstance(NovaSonicStub(), RealtimeAdapterContract)` is True
    - `class TestAllMethodsRaise:` — parametrized test covering all 10 async methods + `test_iter_normalized_events_raises` (sync method that raises directly); uses `@pytest.mark.asyncio` for async methods; uses `@pytest.mark.parametrize` over the 10 async method names+kwargs
    - `class TestErrorMessages:` — `test_message_references_v190`: `pytest.raises(NotImplementedError, match="v1.9.0")` on `await stub.connect()`
    - All test functions decorated with `@pytest.mark.unit`
  </behavior>
  <action>Write nova.py per PATTERNS.md §nova.py full stub pattern. Count lines after writing — must be <30. Confirm no `import websockets`, `import boto3`, or `from eq_chatbot_core.realtime.websocket_client` in the file. Then write test_realtime_nova.py per PATTERNS.md §test_realtime_nova.py pattern. Run pytest tests/unit/realtime/test_realtime_nova.py -v to verify all tests pass. Also run the isinstance check: `python -c "from eq_chatbot_core.realtime.providers.nova import NovaSonicStub; from eq_chatbot_core.realtime.contracts import RealtimeAdapterContract; assert isinstance(NovaSonicStub(), RealtimeAdapterContract); print('isinstance OK')"`. Run ruff check on nova.py.</action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && python -c "
from eq_chatbot_core.realtime.providers.nova import NovaSonicStub
from eq_chatbot_core.realtime.contracts import RealtimeAdapterContract

# PROV-08: structural conformance
stub = NovaSonicStub()
assert isinstance(stub, RealtimeAdapterContract), 'isinstance check failed — method signatures may not match Protocol'

# D-07: stdlib-only (no websockets import in nova.py)
import sys
assert 'websockets' not in str(type(stub).__module__), 'websockets should not be imported'

# D-07: LOC count under 30
import inspect
src = inspect.getsource(type(stub).__module__[:0] or NovaSonicStub)
print('isinstance and stdlib checks OK')
" && pytest tests/unit/realtime/test_realtime_nova.py -v && ruff check src/eq_chatbot_core/realtime/providers/nova.py && wc -l src/eq_chatbot_core/realtime/providers/nova.py</automated>
  </verify>
  <acceptance_criteria>
    - `isinstance(NovaSonicStub(), RealtimeAdapterContract)` is True
    - `pytest tests/unit/realtime/test_realtime_nova.py` exits 0 with all tests passing
    - TestContractConformance::test_isinstance_realtime_adapter_contract passes
    - TestAllMethodsRaise: all 10 async method tests pass, test_iter_normalized_events_raises passes
    - TestErrorMessages::test_message_references_v190 passes (match="v1.9.0")
    - `ruff check src/eq_chatbot_core/realtime/providers/nova.py` exits 0
    - `wc -l src/eq_chatbot_core/realtime/providers/nova.py` reports ≤30 lines
    - `grep -c "import websockets" src/eq_chatbot_core/realtime/providers/nova.py` = 0
    - `grep -c "import boto3" src/eq_chatbot_core/realtime/providers/nova.py` = 0
    - `grep -c "websocket_client" src/eq_chatbot_core/realtime/providers/nova.py` = 0
  </acceptance_criteria>
  <done>nova.py exists, is <30 LOC, stdlib-only, and NovaSonicStub() satisfies isinstance check; test_realtime_nova.py exists and all 3 test classes pass; ruff exits 0 on nova.py.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer → NovaSonicStub | No auth, no network — stub raises immediately; no credentials ever handled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-N01 | Information Disclosure | NovaSonicStub in factory import path | accept | Stub is stdlib-only; no credentials, no network I/O; raises NotImplementedError immediately — no data reaches any external system |
| T-03-N02 | Spoofing | Consumer assumes NovaSonicStub is functional | mitigate | Every method raises NotImplementedError with "available in v1.9.0"; no silent no-ops; consumer gets immediate feedback that the stub is not functional |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; nova.py is stdlib-only; package legitimacy audit is N/A |
</threat_model>

<verification>
After Task 1 completes:

1. `python -c "from eq_chatbot_core.realtime.providers.nova import NovaSonicStub"` — exits without ImportError
2. `isinstance(NovaSonicStub(), RealtimeAdapterContract)` — True
3. `pytest tests/unit/realtime/test_realtime_nova.py -v` — all tests pass
4. `ruff check src/eq_chatbot_core/realtime/providers/nova.py` — exits 0
5. `wc -l src/eq_chatbot_core/realtime/providers/nova.py` — ≤30 lines
6. `grep -c "import websockets" src/eq_chatbot_core/realtime/providers/nova.py` — 0 (stdlib-only confirmed)
7. `pytest tests/unit/ -q` — existing suite passes (no regressions)
</verification>

<success_criteria>
- nova.py exists, <30 LOC, stdlib-only (no websockets, boto3, or realtime.websocket_client imports)
- isinstance(NovaSonicStub(), RealtimeAdapterContract) is True
- Every method raises NotImplementedError with a message containing "v1.9.0"
- test_realtime_nova.py exists with 3 test classes (TestContractConformance, TestAllMethodsRaise, TestErrorMessages) — all passing
- ruff exits 0 on nova.py
</success_criteria>

<output>
Create .planning/phases/03-gemini-live-nova-sonic-stub/03-03-SUMMARY.md when done
</output>
