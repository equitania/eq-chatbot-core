---
phase: 03-gemini-live-nova-sonic-stub
plan: "05"
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - tests/unit/realtime/test_realtime_gemini.py
  - tests/integration/test_realtime_gemini_live.py
autonomous: true
requirements:
  - PROV-05
  - PROV-06
  - PROV-07
  - QUAL-01
  - QUAL-03
must_haves:
  truths:
    - "test_realtime_gemini.py covers PROV-05, PROV-06, PROV-07, QUAL-01: all 12 test classes passing"
    - "Developer endpoint mode: URL contains key=, no Authorization header — verified in TestEndpointModes"
    - "Vertex endpoint mode: URL is {region}-aiplatform.googleapis.com, Authorization header contains Bearer token — verified in TestEndpointModes"
    - "_redact_sensitive_url strips key= from Developer API URL — SC-2 verified in TestRedaction"
    - "_redact_sensitive_text strips raw bearer token from Vertex error strings — SC-2 verified in TestRedaction"
    - "_on_connected does NOT call initialize_session() — verified in TestConnectLifecycle"
    - "All Gemini wire types (setupComplete, serverContent, toolCall, toolCallCancellation, error, unknown) normalize to correct NormalizedRealtimeEventTypes constants — verified in TestIterNormalizedEvents"
    - "D-03: Gemini Vertex EU integration test (SC-3 / QUAL-03) uses the Vertex europe-west* path and skips cleanly when GEMINI_VERTEX_ACCESS_TOKEN absent (never logs raw access token); unit tests cover BOTH endpoint modes via TestEndpointModes (the accepted expanded test matrix)"
  artifacts:
    - path: tests/unit/realtime/test_realtime_gemini.py
      provides: "12 test classes covering PROV-05, PROV-06, PROV-07, QUAL-01 Gemini unit tests"
    - path: tests/integration/test_realtime_gemini_live.py
      provides: "QUAL-03 Vertex EU integration test — skipped when credentials absent"
  key_links:
    - from: "TestRedaction::test_redact_key_param_from_developer_url"
      to: "SC-2 phase success criterion"
      via: "_redact_sensitive_url output contains no api_key; 'key=[REDACTED]' present"
      pattern: "_redact_sensitive_url"
    - from: "TestConnectLifecycle::test_on_connected_does_not_call_initialize_session"
      to: "PITFALL-05 guard"
      via: "mock_init.assert_not_awaited()"
      pattern: "assert_not_awaited"
    - from: "test_gemini_live_vertex_eu_session_ready_and_pcm_chunk"
      to: "QUAL-03 SC-3"
      via: "@pytest.mark.skipif(not os.getenv('GEMINI_VERTEX_ACCESS_TOKEN'))"
      pattern: "GEMINI_VERTEX_ACCESS_TOKEN"
---

<objective>
Create tests/unit/realtime/test_realtime_gemini.py (12 test classes, PROV-05/06/07/QUAL-01 coverage) and tests/integration/test_realtime_gemini_live.py (QUAL-03 Vertex EU integration test, skipped when credentials absent).

Purpose: Complete the QUAL-01 requirement for Gemini (unit tests for both endpoint modes, all wire-type normalization, redaction, no-auto-init behavior, capabilities) and the QUAL-03 requirement (live Vertex EU integration test with SESSION_READY gate, PCM16 send, clean disconnect). These are the phase's primary validation artifacts.

Output: test_realtime_gemini.py (~200 LOC, 12 test classes, no real network calls); test_realtime_gemini_live.py (~60 LOC, 1 async test function, skip gate on env vars).
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/ROADMAP.md
@/Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/03-gemini-live-nova-sonic-stub/03-CONTEXT.md
@/Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/03-gemini-live-nova-sonic-stub/03-RESEARCH.md
@/Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/03-gemini-live-nova-sonic-stub/03-PATTERNS.md

<interfaces>
<!-- GeminiLiveClient API — extract from existing implementation before writing tests. -->

From src/eq_chatbot_core/realtime/providers/gemini_live.py (read the file to confirm):
  class GeminiLiveConfig:
      api_key: str = ""
      access_token: str = ""
      project: str = ""
      region: str = "europe-west4"
      mode: Literal["developer", "vertex"] = "developer"
      model: str  # verified alias from Plan 01
      instructions: str = ""
      base_url: str | None = None
      trace_events: bool = False

  class GeminiLiveClient(BaseRealtimeWebsocketClient, RealtimeProvider):
      _url: str           # set by super().__init__()
      _headers: dict      # set by super().__init__()
      _config: GeminiLiveConfig
      _secret: str        # api_key or access_token for redaction
      async def _on_connected(self) -> None       # logging no-op
      def _connection_error_endpoint(self) -> str # redacted URL
      @staticmethod def _redact_sensitive_url(url: str) -> str
      def _redact_sensitive_text(self, text: str) -> str
      def _to_normalized_runtime_events(self, event: dict) -> list[NormalizedRealtimeEventFull]
      def _build_setup_event(self, *, instructions, tools) -> dict

From tests/unit/realtime/conftest.py:
  # mock_websockets_module fixture — autouse=True, scope="session"
  # Automatically patches sys.modules["websockets"] for all tests in tests/unit/realtime/
  # No action needed in test_realtime_gemini.py — the mock is already active

From tests/integration/test_realtime_openai_live.py:
  # Pattern to mirror exactly for test_realtime_gemini_live.py:
  # - pytest.importorskip("websockets") before all imports
  # - pytestmark = pytest.mark.integration
  # - @pytest.mark.skipif(not os.getenv("OPENAI_API_KEY"), reason=...)
  # - @pytest.mark.asyncio + async def test_... pattern
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test_realtime_gemini.py — 12 unit test classes covering PROV-05/06/07/QUAL-01</name>
  <files>tests/unit/realtime/test_realtime_gemini.py</files>
  <read_first>
    - src/eq_chatbot_core/realtime/providers/gemini_live.py (full file — read to confirm exact field names, method signatures, and redaction behavior before writing tests against it)
    - tests/unit/realtime/test_realtime_openai.py (full file — use as structural template; mirror class layout, helper factories, mock patterns)
    - tests/unit/realtime/conftest.py (confirm mock_websockets_module is autouse=True, scope="session" — no manual setup needed)
    - .planning/phases/03-gemini-live-nova-sonic-stub/03-PATTERNS.md §"test_realtime_gemini.py" — full file header, helper factories, class list, key test implementations for TestConnectLifecycle, TestEndpointModes, TestRedaction
    - .planning/phases/03-gemini-live-nova-sonic-stub/03-RESEARCH.md §"Validation Architecture" §"Phase Requirements → Test Map" — what each test class must verify
  </read_first>
  <behavior>
    - File header comment per PATTERNS.md §File header + import pattern (Coverage: PROV-05, PROV-06, PROV-07, QUAL-01)
    - Imports: `import dataclasses`, `import pytest`, `from unittest.mock import AsyncMock, patch`, `from eq_chatbot_core.providers.base import ToolDefinition`, `from eq_chatbot_core.realtime.contracts import NormalizedRealtimeEventTypes, RealtimeAdapterContract`, `from eq_chatbot_core.realtime.providers.gemini_live import GEMINI_LIVE_REALTIME_CAPABILITIES, GeminiLiveClient, GeminiLiveConfig`
    - Module-level constants: `_FAKE_KEY = "test-api-key"`, `_FAKE_TOKEN = "ya29.fake-bearer-token"`
    - Helper factories: `_make_developer_client() -> GeminiLiveClient` (api_key=_FAKE_KEY, mode="developer"), `_make_vertex_client() -> GeminiLiveClient` (access_token=_FAKE_TOKEN, project="my-gcp-project", region="europe-west4", mode="vertex")
    - **12 test classes** (all methods `@pytest.mark.unit`):
      1. `TestGeminiLiveConfig` — frozen+slots (dataclasses.fields present), default model contains "gemini" not "2.0-flash", mode default "developer", region default "europe-west4"
      2. `TestCapabilities` — PROV-06: server_vad is False, manual_turn_commit_required is True, tool_result_submission_mode == "provider_call_id", streaming_audio_input/output True, tool_calling True
      3. `TestConstructorValidation` — D-06: empty api_key for developer raises ValueError, empty access_token for vertex raises ValueError, unknown mode raises ValueError, empty model raises ValueError
      4. `TestEndpointModes` — QUAL-01: developer URL contains key=_FAKE_KEY, developer has no Authorization header; vertex URL contains "aiplatform.googleapis.com", vertex has Authorization == "Bearer {_FAKE_TOKEN}", vertex has x-goog-user-project header
      5. `TestConnectionErrorEndpoint` — PROV-07: `_connection_error_endpoint()` return value does not contain _FAKE_KEY (developer); does not contain _FAKE_TOKEN (vertex); starts with "wss://"
      6. `TestRedaction` — PROV-07 / SC-2: `_redact_sensitive_url(dev._url)` does not contain _FAKE_KEY, contains "key=[REDACTED]"; `_redact_sensitive_text(text_with_token)` does not contain _FAKE_TOKEN (vertex)
      7. `TestSetupEvent` — `_build_setup_event` adds "models/" prefix if absent; does not add second "models/" if already present; systemInstruction included when instructions provided
      8. `TestToolSchemaConversion` — `_to_gemini_function_declaration` uses `tool.parameters` not `tool.input_schema`; `_to_gemini_schema` strips additionalProperties; empty object schema returns None
      9. `TestIterNormalizedEvents` — QUAL-01: recorded scrubbed frames → correct event types: setupComplete→SESSION_READY, serverContent with modelTurn→RESPONSE_AUDIO_DELTA+RESPONSE_DONE, toolCall→TOOL_CALL_COMPLETED, toolCallCancellation→TOOL_CALL_CANCELLED, error→ERROR, unknown key→UNHANDLED; call `_to_normalized_runtime_events()` directly (no network)
      10. `TestManualTurnCommit` — `commit_client_turn` sends correct JSON shape `{"realtimeInput": {"audioStreamEnd": True}}`; use `patch.object(client, 'send_json', new_callable=AsyncMock)`
      11. `TestToolResult` — `submit_tool_result` sends `toolResponse.functionResponses` with correct `id` and `response.output` shape; JSON output is decoded; malformed JSON falls back to `{"output": raw_string}`
      12. `TestConnectLifecycle` — `_on_connected()` does NOT call `initialize_session()` (use `patch.object` + `assert_not_awaited`); `isinstance(client, RealtimeAdapterContract)` is True
    - All test methods decorated `@pytest.mark.unit` (not @pytest.mark.asyncio unless the method is `async def`)
    - Async tests decorated `@pytest.mark.asyncio`
  </behavior>
  <action>Write test_realtime_gemini.py. Begin with the file header and imports per PATTERNS.md. Define helper factories _make_developer_client() and _make_vertex_client(). Write all 12 test classes. For TestIterNormalizedEvents: define scrubbed frame constants (e.g., SETUP_COMPLETE_FRAME = {"setupComplete": {}}) at module level and call client._to_normalized_runtime_events(frame) directly — no mocking of iter_events needed for this test. For TestManualTurnCommit and TestToolResult: use AsyncMock to patch send_json and capture what was sent. Run pytest tests/unit/realtime/test_realtime_gemini.py -v after writing; fix any failures.</action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && pytest tests/unit/realtime/test_realtime_gemini.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/realtime/test_realtime_gemini.py -v` exits 0 — all test classes and methods passing
    - TestRedaction::test_redact_key_param_from_developer_url passes (SC-2 unit test: key= stripped)
    - TestRedaction::test_redact_bearer_token_from_error_text passes (SC-2 unit test: bearer token stripped)
    - TestConnectionErrorEndpoint passes: _connection_error_endpoint() contains no _FAKE_KEY and no _FAKE_TOKEN
    - TestConnectLifecycle::test_on_connected_does_not_call_initialize_session passes (mock_init.assert_not_awaited())
    - TestEndpointModes: developer URL contains key=, vertex URL contains aiplatform.googleapis.com
    - TestIterNormalizedEvents: all 6 wire types normalized to correct constants
    - TestCapabilities: server_vad is False, manual_turn_commit_required is True
    - All 12 test classes represented in output
  </acceptance_criteria>
  <done>test_realtime_gemini.py exists with 12 test classes; pytest exits 0; SC-2 redaction tests pass; endpoint mode tests pass; _on_connected no-auto-init test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_realtime_gemini_live.py — QUAL-03 Vertex EU integration test</name>
  <files>tests/integration/test_realtime_gemini_live.py</files>
  <read_first>
    - tests/integration/test_realtime_openai_live.py (full file — mirror structure exactly for Vertex EU path)
    - .planning/phases/03-gemini-live-nova-sonic-stub/03-PATTERNS.md §"test_realtime_gemini_live.py" — full file pattern including docstring, skipif condition, async test flow with explicit initialize_session() call
    - .planning/phases/03-gemini-live-nova-sonic-stub/03-RESEARCH.md §"Integration test pattern" and §"Environment Availability" (env var names: GEMINI_VERTEX_ACCESS_TOKEN, VERTEX_PROJECT_ID, VERTEX_REGION)
    - src/eq_chatbot_core/realtime/providers/gemini_live.py (confirm async context manager support via BaseRealtimeWebsocketClient)
  </read_first>
  <behavior>
    - File docstring: "Live integration test for Gemini Live provider — Vertex AI EU endpoint (QUAL-03 / SC-3). Requires GEMINI_VERTEX_ACCESS_TOKEN and VERTEX_PROJECT_ID environment variables. VERTEX_REGION defaults to europe-west4. Test is automatically skipped when credentials are absent. Run with: pytest -m integration tests/integration/test_realtime_gemini_live.py -v"
    - `pytest.importorskip("websockets")` before all other imports (same guard as openai live test)
    - Imports: `import os`, `import pytest`, `from eq_chatbot_core.realtime.contracts import NormalizedRealtimeEventTypes`, `from eq_chatbot_core.realtime.providers.gemini_live import GeminiLiveClient, GeminiLiveConfig`
    - `pytestmark = pytest.mark.integration`
    - `@pytest.mark.skipif(not os.getenv("GEMINI_VERTEX_ACCESS_TOKEN") or not os.getenv("VERTEX_PROJECT_ID"), reason="GEMINI_VERTEX_ACCESS_TOKEN / VERTEX_PROJECT_ID not set — skipping Vertex EU integration test")`
    - `@pytest.mark.asyncio`
    - `async def test_gemini_live_vertex_eu_session_ready_and_pcm_chunk() -> None:` — with test docstring: "QUAL-03 / SC-3: Connect to Gemini Live Vertex EU, receive SESSION_READY, send PCM16 silence, commit turn, close cleanly. Uses europe-west4 (Netherlands) by default — DSGVO-compliant regional endpoint. If VERTEX_REGION is set, that region is used instead."
    - Test body (per PATTERNS.md §Integration test pattern):
      1. Build config: `GeminiLiveConfig(mode="vertex", access_token=os.environ["GEMINI_VERTEX_ACCESS_TOKEN"], project=os.environ["VERTEX_PROJECT_ID"], region=os.getenv("VERTEX_REGION", "europe-west4"))`
      2. `async with GeminiLiveClient(config) as client:`
      3. `await client.initialize_session()` — comment: "Gemini requires explicit setup (differs from OpenAI auto-init)"
      4. Iterate events until SESSION_READY: `async for event in client.iter_normalized_events(): assert event["type"] == NormalizedRealtimeEventTypes.SESSION_READY, ...; break`
      5. Send 100ms silence: `silence = b"\x00\x00" * 2400` (100ms at 24kHz mono PCM16 = 4800 bytes)
      6. `await client.append_client_audio(silence)`
      7. `await client.commit_client_turn()` — comment: "mandatory — Gemini has no server VAD"
      8. Async with __aexit__ triggers clean close()
    - Security: access_token is NOT logged anywhere in the test; no print() or logging calls that could expose the token; the __skipif__ condition means it only runs with real credentials, never in CI without them
  </behavior>
  <action>Write test_realtime_gemini_live.py per PATTERNS.md §test_realtime_gemini_live.py full file pattern. Place pytest.importorskip("websockets") before all other imports. Ensure the test function has the correct @pytest.mark.skipif and @pytest.mark.asyncio decorators. Verify the test skips cleanly when credentials are absent: `pytest tests/integration/test_realtime_gemini_live.py -v` (should show 1 skipped). Do NOT print or log the access_token or project_id anywhere in the test body.</action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && pytest tests/integration/test_realtime_gemini_live.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/integration/test_realtime_gemini_live.py -v` exits 0 — test is either skipped (no credentials) or passes (with credentials)
    - When GEMINI_VERTEX_ACCESS_TOKEN is absent: test status is "SKIPPED" with reason containing "GEMINI_VERTEX_ACCESS_TOKEN"
    - File contains `pytest.importorskip("websockets")` as the first substantive line after imports
    - File contains `pytestmark = pytest.mark.integration`
    - File contains `@pytest.mark.skipif(not os.getenv("GEMINI_VERTEX_ACCESS_TOKEN") or not os.getenv("VERTEX_PROJECT_ID"), ...)`
    - Test body calls `client.initialize_session()` explicitly (not relying on _on_connected auto-init)
    - `grep -c "access_token" tests/integration/test_realtime_gemini_live.py` — only in config construction, never in print/logging calls (security gate)
    - `grep -c "print(" tests/integration/test_realtime_gemini_live.py` = 0 (no credential exposure via print)
  </acceptance_criteria>
  <done>test_realtime_gemini_live.py exists; pytest shows 1 skipped (or passing) when run without credentials; no access_token printed or logged; explicit initialize_session() call present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → Vertex AI wss:// | Integration test only — bearer token supplied via env vars; test skipped in CI when absent |
| test output → stdout | Bearer token must never appear in test output (no print() calls containing credential values) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-T01 | Information Disclosure | GEMINI_VERTEX_ACCESS_TOKEN in test output | mitigate | No `print()` or `logging` calls in test body; test docstring describes flow without echoing credentials; acceptance criteria explicitly check grep for `print(` = 0 |
| T-03-T02 | Information Disclosure | SC-2 redaction: key= in log path | mitigate | TestRedaction::test_redact_key_param_from_developer_url asserts `_FAKE_KEY not in redacted` and `"key=[REDACTED]" in redacted` — this is the primary PROV-07 unit test cited in SC-2 |
| T-03-T03 | Information Disclosure | SC-2 redaction: bearer token in log path | mitigate | TestRedaction::test_redact_bearer_token_from_error_text asserts `_FAKE_TOKEN not in result` — covers Vertex bearer token protection (RESEARCH.md Pitfall 3) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; package legitimacy audit is N/A |
</threat_model>

<verification>
After both tasks complete:

1. `pytest tests/unit/realtime/test_realtime_gemini.py -v` exits 0 — all 12 classes pass
2. `pytest tests/integration/test_realtime_gemini_live.py -v` exits 0 — 1 skipped (credentials absent) or passing
3. TestRedaction::test_redact_key_param_from_developer_url passes (SC-2)
4. TestRedaction::test_redact_bearer_token_from_error_text passes (SC-2 Vertex)
5. TestConnectLifecycle::test_on_connected_does_not_call_initialize_session passes (no auto-init)
6. TestCapabilities: GEMINI_LIVE_REALTIME_CAPABILITIES.server_vad is False
7. `pytest tests/unit/ -q` — full unit suite passes (no regressions)
8. `grep -c "print(" tests/integration/test_realtime_gemini_live.py` = 0 (no credential leak)
</verification>

<success_criteria>
- test_realtime_gemini.py exists with 12 test classes — all passing
- SC-2 unit test: _redact_sensitive_url output contains no api_key and contains "key=[REDACTED]"
- SC-2 unit test: _redact_sensitive_text removes bearer token from error text
- test_on_connected_does_not_call_initialize_session passes (Pitfall-05 guard)
- test_realtime_gemini_live.py exists with Vertex EU integration test
- Integration test skips cleanly when GEMINI_VERTEX_ACCESS_TOKEN absent
- Integration test calls initialize_session() explicitly (not from _on_connected)
- No credentials printed or logged in integration test body
- Full unit suite passes with no regressions
</success_criteria>

<output>
Create .planning/phases/03-gemini-live-nova-sonic-stub/03-05-SUMMARY.md when done
</output>
