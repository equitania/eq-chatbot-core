---
phase: 03-gemini-live-nova-sonic-stub
plan: "04"
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - src/eq_chatbot_core/realtime/factory.py
  - tests/unit/realtime/test_factory.py
autonomous: true
requirements:
  - PROV-05
  - PROV-08
must_haves:
  truths:
    - "get_realtime_provider('gemini_live', api_key='x') resolves without ImportError (developer mode)"
    - "get_realtime_provider('gemini_live', access_token='y', mode='vertex') resolves without ImportError (vertex mode)"
    - "get_realtime_provider('nova_sonic') resolves and returns NovaSonicStub without any AWS extras installed (D-08 / SC-5)"
    - "Omitting api_key for developer mode raises ValueError before any network I/O (D-06 fail-fast)"
    - "Omitting access_token for vertex mode raises ValueError before any network I/O (D-06 fail-fast)"
    - "build_default_realtime_provider_registry() returns a registry containing 'gemini_live' and 'nova_sonic' alongside 'openai' and 'mock'"
  artifacts:
    - path: src/eq_chatbot_core/realtime/factory.py
      provides: "gemini_live and nova_sonic registered in build_default_realtime_provider_registry(); _build_gemini_live_provider; _build_nova_sonic_provider"
      contains: "gemini_live"
    - path: tests/unit/realtime/test_factory.py
      provides: "5 new test functions covering gemini_live and nova_sonic registry + fail-fast behavior"
  key_links:
    - from: "_build_gemini_live_provider"
      to: "GeminiLiveClient, GeminiLiveConfig"
      via: "deferred import with # noqa: PLC0415"
      pattern: "from eq_chatbot_core.realtime.providers.gemini_live import"
    - from: "_build_nova_sonic_provider"
      to: "NovaSonicStub"
      via: "deferred import — stdlib-only, no websockets"
      pattern: "from eq_chatbot_core.realtime.providers.nova import"
---

<objective>
Extend src/eq_chatbot_core/realtime/factory.py to register gemini_live and nova_sonic providers, and add 5 new test functions to tests/unit/realtime/test_factory.py covering registry presence and D-06 fail-fast behavior.

Purpose: Wires Plans 02 and 03 into the factory so get_realtime_provider("gemini_live", ...) and get_realtime_provider("nova_sonic") resolve correctly. Both registrations use the deferred-import pattern to keep factory.py importable without [realtime] extras or AWS packages.

Output: factory.py extended with 2 new provider registrations and 2 new _build_* functions (~30 LOC added); test_factory.py extended with 5 new @pytest.mark.unit test functions (~35 LOC added). No new files.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/ROADMAP.md
@/Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/03-gemini-live-nova-sonic-stub/03-CONTEXT.md
@/Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/03-gemini-live-nova-sonic-stub/03-PATTERNS.md

<interfaces>
<!-- Existing factory.py public API — add to this without changing existing registrations. -->

From src/eq_chatbot_core/realtime/factory.py (current state):
  def build_default_realtime_provider_registry() -> RealtimeProviderRegistry
      # Currently registers: "mock", "openai"
      # Phase 3 adds: "gemini_live", "nova_sonic"

  def _build_openai_provider(**kwargs: Any) -> Any
      # Pattern to mirror for _build_gemini_live_provider and _build_nova_sonic_provider

  class RealtimeProviderRegistry:
      def register(self, definition: RealtimeProviderDefinition) -> None
      def registered_names(self) -> list[str]
      def get(self, name: str) -> RealtimeProviderDefinition | None

  class RealtimeProviderDefinition:
      name: str
      factory_fn: Callable[..., Any]
      description: str = ""

  def _get_realtime_provider_impl(name: str, **kwargs: Any) -> Any
      # Called by realtime/__init__.py — used in test_factory.py as _get_realtime_provider_impl

<!-- Existing test_factory.py pattern — add after existing tests. -->

From tests/unit/realtime/test_factory.py (extend — do not modify existing tests):
  # Existing imports: build_default_realtime_provider_registry, _get_realtime_provider_impl
  # Existing @pytest.mark.unit test functions: test_registry_contains_mock,
  #   test_registry_contains_openai, test_get_realtime_provider_openai_missing_api_key_raises_value_error
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend factory.py — register gemini_live + nova_sonic, add _build_* functions</name>
  <files>src/eq_chatbot_core/realtime/factory.py</files>
  <read_first>
    - src/eq_chatbot_core/realtime/factory.py (full file — read before editing; understand exact insertion points for registrations and new _build_* functions)
    - .planning/phases/03-gemini-live-nova-sonic-stub/03-PATTERNS.md §"factory.py (modify — extend registry)" — exact code for two registry.register() calls and two _build_* functions
    - src/eq_chatbot_core/realtime/providers/gemini_live.py (confirm GeminiLiveClient, GeminiLiveConfig are exported)
    - src/eq_chatbot_core/realtime/providers/nova.py (confirm NovaSonicStub is exported)
  </read_first>
  <behavior>
    - In `build_default_realtime_provider_registry()`, add two `registry.register()` calls after the existing "openai" registration (before `return registry`):
      1. `RealtimeProviderDefinition(name="gemini_live", factory_fn=lambda **kwargs: _build_gemini_live_provider(**kwargs), description="Google Gemini Live API — BidiGenerateContent, manual turn commit, tool calling.")`
      2. `RealtimeProviderDefinition(name="nova_sonic", factory_fn=lambda **kwargs: _build_nova_sonic_provider(**kwargs), description="AWS Nova Sonic stub — production implementation in v1.9.0.")`
    - Add `def _build_gemini_live_provider(**kwargs: Any) -> Any:` after `_build_openai_provider`:
      - D-06 fail-fast before deferred import: check `mode = kwargs.get("mode", "developer")`, raise ValueError for missing api_key (developer) or access_token (vertex) with helpful message per PATTERNS.md
      - Deferred import: `from eq_chatbot_core.realtime.providers.gemini_live import GeminiLiveClient, GeminiLiveConfig  # noqa: PLC0415`
      - Build config: `config = GeminiLiveConfig(**kwargs); return GeminiLiveClient(config)`
    - Add `def _build_nova_sonic_provider(**kwargs: Any) -> Any:` after `_build_gemini_live_provider`:
      - No validation needed (stub accepts no kwargs)
      - Deferred import: `from eq_chatbot_core.realtime.providers.nova import NovaSonicStub  # noqa: PLC0415`
      - Return: `return NovaSonicStub()`
    - Do NOT modify `__all__` — `_build_*` functions are internal by convention
    - The existing `_DEFAULT_REGISTRY` singleton is reset when factory module is reimported in tests (by calling `build_default_realtime_provider_registry()` directly) — the singleton mechanism is unchanged
    - `ruff check` + `mypy` pass on the updated file
  </behavior>
  <action>Read factory.py in full first. Add the two registry.register() calls inside build_default_realtime_provider_registry() after the openai registration. Add _build_gemini_live_provider and _build_nova_sonic_provider functions after _build_openai_provider, following the exact pattern from PATTERNS.md. The _build_gemini_live_provider must perform D-06 fail-fast validation before the deferred import. The _build_nova_sonic_provider must not import websockets anywhere (stdlib-only nova.py). Run ruff check --fix and mypy on the updated file.</action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && python -c "
from eq_chatbot_core.realtime.factory import build_default_realtime_provider_registry, _get_realtime_provider_impl

# Registry contains all 4 providers
reg = build_default_realtime_provider_registry()
names = reg.registered_names()
assert 'gemini_live' in names, f'gemini_live not registered: {names}'
assert 'nova_sonic' in names, f'nova_sonic not registered: {names}'
assert 'openai' in names
assert 'mock' in names

# D-08: nova_sonic resolves without AWS extras
from eq_chatbot_core.realtime.providers.nova import NovaSonicStub
provider = _get_realtime_provider_impl('nova_sonic')
assert isinstance(provider, NovaSonicStub), f'nova_sonic did not return NovaSonicStub: {type(provider)}'

# D-06: gemini_live developer mode fail-fast
try:
    _get_realtime_provider_impl('gemini_live', mode='developer')
    raise AssertionError('Should have raised ValueError')
except ValueError as e:
    assert 'api_key' in str(e), f'Wrong error: {e}'

# D-06: gemini_live vertex mode fail-fast
try:
    _get_realtime_provider_impl('gemini_live', mode='vertex')
    raise AssertionError('Should have raised ValueError')
except ValueError as e:
    assert 'access_token' in str(e), f'Wrong error: {e}'

print('factory.py extension OK')
" && ruff check src/eq_chatbot_core/realtime/factory.py && mypy src/eq_chatbot_core/realtime/factory.py --ignore-missing-imports</automated>
  </verify>
  <acceptance_criteria>
    - `build_default_realtime_provider_registry().registered_names()` contains 'gemini_live' and 'nova_sonic'
    - `_get_realtime_provider_impl('nova_sonic')` returns a NovaSonicStub instance (no AWS extras needed)
    - `_get_realtime_provider_impl('gemini_live', mode='developer')` raises ValueError mentioning 'api_key'
    - `_get_realtime_provider_impl('gemini_live', mode='vertex')` raises ValueError mentioning 'access_token'
    - `ruff check` exits 0 on factory.py
    - `mypy` exits 0 on factory.py
    - Existing "openai" and "mock" registrations are unchanged
  </acceptance_criteria>
  <done>factory.py extended with gemini_live + nova_sonic registrations and _build_* functions; all four verification assertions pass; ruff and mypy pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend test_factory.py — 5 new unit tests for gemini_live + nova_sonic</name>
  <files>tests/unit/realtime/test_factory.py</files>
  <read_first>
    - tests/unit/realtime/test_factory.py (full file — read before editing; understand existing imports and test functions to avoid collisions)
    - .planning/phases/03-gemini-live-nova-sonic-stub/03-PATTERNS.md §"test_factory.py (modify — extend)" — exact 5 new test function patterns with @pytest.mark.unit decorators
  </read_first>
  <behavior>
    - Add 5 new `@pytest.mark.unit` test functions after the existing test functions (do not modify existing tests):
      1. `test_registry_contains_gemini_live`: `reg = build_default_realtime_provider_registry(); assert "gemini_live" in reg.registered_names()`
      2. `test_registry_contains_nova_sonic`: `reg = build_default_realtime_provider_registry(); assert "nova_sonic" in reg.registered_names()`
      3. `test_get_realtime_provider_nova_sonic_returns_stub`: `from eq_chatbot_core.realtime.providers.nova import NovaSonicStub; provider = _get_realtime_provider_impl("nova_sonic"); assert isinstance(provider, NovaSonicStub)` — with docstring "D-08: nova_sonic resolves without any AWS extras installed."
      4. `test_get_realtime_provider_gemini_live_developer_missing_api_key_raises`: `with pytest.raises(ValueError, match="api_key"): _get_realtime_provider_impl("gemini_live", mode="developer")`
      5. `test_get_realtime_provider_gemini_live_vertex_missing_access_token_raises`: `with pytest.raises(ValueError, match="access_token"): _get_realtime_provider_impl("gemini_live", mode="vertex")`
    - No modifications to existing tests or imports (except adding any new imports if needed by the new test functions — check if `pytest` is already imported)
    - All 5 functions are `@pytest.mark.unit`-decorated
  </behavior>
  <action>Read test_factory.py in full first. Append the 5 new test functions after the last existing test function, following exact PATTERNS.md §test_factory.py patterns. Run pytest tests/unit/realtime/test_factory.py -v to confirm all existing + new tests pass.</action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && pytest tests/unit/realtime/test_factory.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/realtime/test_factory.py -v` exits 0 — all existing tests still pass, all 5 new tests pass
    - test_registry_contains_gemini_live passes
    - test_registry_contains_nova_sonic passes
    - test_get_realtime_provider_nova_sonic_returns_stub passes (D-08 verified)
    - test_get_realtime_provider_gemini_live_developer_missing_api_key_raises passes
    - test_get_realtime_provider_gemini_live_vertex_missing_access_token_raises passes
    - No existing test functions modified or removed
  </acceptance_criteria>
  <done>test_factory.py extended with 5 new unit tests; pytest exits 0 on the full factory test file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| factory → deferred imports | Deferred imports must not trigger websockets or AWS imports at factory module load time |
| D-06 fail-fast gate | Credential validation runs before deferred import — ensures clear error even if [realtime] extra is absent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-F01 | Information Disclosure | D-06 error message leaking credentials | mitigate | Error messages reference kwarg names ("api_key", "access_token") not values; values never appear in error strings |
| T-03-F02 | Spoofing | nova_sonic factory returns functional provider | accept | NovaSonicStub raises NotImplementedError on every method call — no silent functionality implied; consumer is explicitly told to use v1.9.0 |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; stdlib-only; package legitimacy audit is N/A |
</threat_model>

<verification>
After both tasks complete:

1. `pytest tests/unit/realtime/test_factory.py -v` — all tests pass (existing + 5 new)
2. `build_default_realtime_provider_registry().registered_names()` contains ['gemini_live', 'mock', 'nova_sonic', 'openai'] (sorted)
3. `_get_realtime_provider_impl('nova_sonic')` returns NovaSonicStub instance — no import errors
4. `_get_realtime_provider_impl('gemini_live', mode='developer')` raises ValueError(match="api_key")
5. `_get_realtime_provider_impl('gemini_live', mode='vertex')` raises ValueError(match="access_token")
6. `ruff check src/eq_chatbot_core/realtime/factory.py` exits 0
7. `pytest tests/unit/ -q` — full unit suite passes (no regressions)
</verification>

<success_criteria>
- factory.py registers 'gemini_live' and 'nova_sonic' alongside existing 'openai' and 'mock'
- _build_gemini_live_provider validates credentials before deferred import (D-06)
- _build_nova_sonic_provider is stdlib-only — no websockets or boto3 in nova.py (confirmed by test)
- get_realtime_provider("nova_sonic") resolves without any AWS extras installed (D-08 / SC-5)
- test_factory.py has 5 new @pytest.mark.unit tests — all passing
- ruff and mypy pass on factory.py
</success_criteria>

<output>
Create .planning/phases/03-gemini-live-nova-sonic-stub/03-04-SUMMARY.md when done
</output>
