---
phase: 03-gemini-live-nova-sonic-stub
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - PROV-05
must_haves:
  truths:
    - "The live-valid Gemini Live model alias for both Developer API and Vertex AI is verified and recorded before any production code is written (D-05 / SC-1 / PITFALL-20)"
    - "GeminiLiveConfig.model default is set to a currently-accepted alias, not gemini-2.0-flash or gemini-live-2.5-flash-native-audio-09-2025 (both deprecated/shutdown)"
  artifacts:
    - path: .planning/phases/03-gemini-live-nova-sonic-stub/03-01-SUMMARY.md
      provides: "Verified model alias snapshot recorded as planning artifact"
  key_links:
    - from: "verified alias"
      to: "GeminiLiveConfig.model default in Plan 02"
      via: "SUMMARY.md → executor reads alias before committing gemini_live.py"
      pattern: "gemini-2.5-flash-preview-native-audio"
---

<objective>
[BLOCKING] Verify the currently-valid Gemini Live model alias against the live Gemini Developer API before any production code is written.

Purpose: D-05 / PITFALL-20 require that GeminiLiveConfig.model defaults to a verified, live-accepted alias — not an assumed or stale value. RESEARCH.md proposes `gemini-2.5-flash-preview-native-audio-12-2025` as ASSUMED. This plan verifies it (or finds the replacement) and records the result so Plan 02 can embed the correct default without guessing.

Output: A SUMMARY.md recording: (a) the verified current alias string, (b) the curl command used and its output (abbreviated), (c) whether the Vertex AI endpoint accepts the same alias. No production code is touched in this plan.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/ROADMAP.md
@/Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/03-gemini-live-nova-sonic-stub/03-CONTEXT.md
@/Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/03-gemini-live-nova-sonic-stub/03-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Live-verify Gemini Live model alias for Developer API</name>
  <read_first>
    - .planning/phases/03-gemini-live-nova-sonic-stub/03-RESEARCH.md §"D-05: Verified Model Alias Snapshot" and §"Open Questions" — item 1 (exact curl command to run and what to look for)
    - .planning/phases/03-gemini-live-nova-sonic-stub/03-RESEARCH.md §"State of the Art" — aliases that are definitively DEAD (gemini-2.0-flash shutdown June 1 2026; gemini-live-2.5-flash-preview-native-audio-09-2025 removed March 2026)
  </read_first>
  <what-built>
    RESEARCH.md documents the assumed default `gemini-2.5-flash-preview-native-audio-12-2025` but marks it [ASSUMED]. The Gemini models endpoint lists all available models including Live API models. This task queries that endpoint to confirm which Live models are currently accepting requests, then records the definitive alias in SUMMARY.md.

    The live query command (requires GEMINI_API_KEY in environment):
      curl "https://generativelanguage.googleapis.com/v1beta/models?key=$GEMINI_API_KEY" | python3 -m json.tool | grep -i "name" | grep -i "live\|audio\|native"

    If GEMINI_API_KEY is not available in the environment, check the Gemini API models reference at https://ai.google.dev/gemini-api/docs/models and identify the currently-listed Live API model name. Record the source URL and date alongside the alias.

    The expected output contains the currently-valid model name(s) for the Gemini Live (BidiGenerateContent) endpoint. Identify: (1) the floating alias if one exists (e.g. `gemini-2.5-flash-preview-native-audio-12-2025`), (2) any newer alias that supersedes it (e.g. a 2026 variant), (3) whether the same alias works on Vertex AI.

    Record the verified alias in .planning/phases/03-gemini-live-nova-sonic-stub/03-01-SUMMARY.md under a "## Verified Model Alias" section. This alias becomes the GeminiLiveConfig.model default in Plan 02.

    Do NOT use gemini-2.0-flash or gemini-2.0-flash-001 (shutdown June 1 2026).
    Do NOT use gemini-live-2.5-flash-preview-native-audio-09-2025 (removed March 2026).
  </what-built>
  <how-to-verify>
    1. Run `echo $GEMINI_API_KEY` — if non-empty, the curl command above will return the live models list
    2. Identify at least one model name containing "live" or "native-audio" that is NOT in the dead-alias list above
    3. Confirm the alias does NOT include "2.0-flash" or "09-2025"
    4. Record the confirmed alias in 03-01-SUMMARY.md

    Acceptable outcome if GEMINI_API_KEY is absent:
    - Check https://ai.google.dev/gemini-api/docs/models (Live API section) for the current model table
    - Record the alias found there with "source: ai.google.dev docs, date: 2026-05-25"
    - Note: "Vertex AI alias — assumed same; confirm against Vertex model list if credentials available"
  </how-to-verify>
  <resume-signal>
    Type "verified: {alias}" where {alias} is the confirmed model name (e.g. "verified: gemini-2.5-flash-preview-native-audio-12-2025"), OR describe what was found if the alias differs from the RESEARCH.md assumption.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → Gemini models API | Read-only GET request; GEMINI_API_KEY travels as URL query param — keep out of logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-P1-01 | Information Disclosure | GEMINI_API_KEY in curl command | accept | This is a local verification task run by the developer; key not logged to any persistent store; curl stdout is ephemeral |
| T-03-SC | Tampering | Stale model alias committed to source | mitigate | This entire plan is the mitigation — no code is written until the alias is verified live |
</threat_model>

<verification>
After Task 1 completes:

1. .planning/phases/03-gemini-live-nova-sonic-stub/03-01-SUMMARY.md exists with a "## Verified Model Alias" section
2. The alias recorded does not contain "2.0-flash" or "09-2025"
3. The alias recorded contains "gemini" and ("live" or "audio" or "native")
4. Plan 02 executor reads this SUMMARY before committing GeminiLiveConfig.model default
</verification>

<success_criteria>
- 03-01-SUMMARY.md exists with a verified model alias that is accepted by the Gemini Live BidiGenerateContent endpoint
- The alias is NOT gemini-2.0-flash, gemini-2.0-flash-001, or gemini-live-2.5-flash-preview-native-audio-09-2025
- Plan 02 can safely embed this alias as GeminiLiveConfig.model default with a code comment citing the verification date
</success_criteria>

<output>
Create .planning/phases/03-gemini-live-nova-sonic-stub/03-01-SUMMARY.md when done.
Include a "## Verified Model Alias" section with: alias string, verification method (curl or docs), date verified, and Vertex AI note.
</output>
