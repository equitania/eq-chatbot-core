---
phase: 01-contracts-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eq_chatbot_core/realtime/contracts.py
  - src/eq_chatbot_core/realtime/abc.py
  - src/eq_chatbot_core/providers/base.py
  - pyproject.toml
autonomous: true
requirements:
  - CON-01
  - CON-02
  - CON-03
  - CON-04
  - CON-05
  - CON-06
  - CON-07
  - CON-12

must_haves:
  truths:
    - "NormalizedRealtimeEventTypes exposes exactly 12 string constants with the verified values"
    - "NormalizedRealtimeEvent is a TypedDict with total=False and keys: type, payload, source, raw"
    - "RealtimeProviderCapabilities is a frozen dataclass with slots=True; session_sample_rate defaults to 24000"
    - "RealtimeAdapterContract is a @runtime_checkable Protocol with exactly 11 async methods"
    - "INPUT_AUDIO_SAMPLE_RATE == 24000 is exported from realtime/contracts.py"
    - "RealtimeProvider ABC has exactly 4 abstract methods: connect, close, initialize_session, iter_normalized_events"
    - "RealtimeEvent union covers exactly 7 frozen dataclass event types"
    - "ToolDefinition is a frozen dataclass with slots=True in providers/base.py"
    - "chat_completion signature in BaseLLMProvider accepts list[ToolDefinition] | list[dict] | None for tools"
    - "pyproject.toml [project.optional-dependencies] contains realtime = [websockets>=13.0,<17.0]"
  artifacts:
    - path: "src/eq_chatbot_core/realtime/contracts.py"
      provides: "12 event type constants, TypedDict, capabilities dataclass, Protocol, INPUT_AUDIO_SAMPLE_RATE"
      exports:
        - NormalizedRealtimeEventTypes
        - NormalizedRealtimeEvent
        - RealtimeProviderCapabilities
        - RealtimeAdapterContract
        - INPUT_AUDIO_SAMPLE_RATE
    - path: "src/eq_chatbot_core/realtime/abc.py"
      provides: "RealtimeProvider ABC + 7 event dataclasses"
      exports:
        - RealtimeProvider
        - RealtimeEvent
    - path: "src/eq_chatbot_core/providers/base.py"
      provides: "ToolDefinition frozen dataclass, updated chat_completion signature"
      contains: "class ToolDefinition"
    - path: "pyproject.toml"
      provides: "[realtime] extra dependency declaration"
      contains: "websockets>=13.0,<17.0"
  key_links:
    - from: "realtime/contracts.py"
      to: "tests/unit/realtime/test_contracts.py"
      via: "import NormalizedRealtimeEventTypes"
      pattern: "NormalizedRealtimeEventTypes\\.SESSION_READY"
    - from: "providers/base.py"
      to: "providers/__init__.py"
      via: "re-export ToolDefinition"
      pattern: "ToolDefinition"
---

<objective>
Create the complete realtime type system: NormalizedRealtimeEventTypes (12 constants), NormalizedRealtimeEvent TypedDict, RealtimeProviderCapabilities frozen dataclass, RealtimeAdapterContract @runtime_checkable Protocol, INPUT_AUDIO_SAMPLE_RATE constant, RealtimeProvider ABC with 7 event dataclasses, ToolDefinition in providers/base.py, and the [realtime] extra in pyproject.toml.

Purpose: Foundation layer — all subsequent plans (02-05) import from these files. The 12 string constants are the GlassAgents migration safety gate. Zero network calls; stdlib-only output.

Output: 4 files created/modified; ruff + mypy clean.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-contracts-foundation/01-RESEARCH.md
@.planning/phases/01-contracts-foundation/01-PATTERNS.md

<interfaces>
<!-- Existing base.py pattern to mirror — read before modifying -->

From src/eq_chatbot_core/providers/base.py (frozen dataclass pattern):
  @dataclass(frozen=True, slots=True) with typed fields — use for ToolDefinition, RealtimeProviderCapabilities
  Exception hierarchy: ProviderError(Exception) base with __init__(message, provider, status_code, retry_after)
  ABC pattern: BaseLLMProvider(ABC) with @property @abstractmethod and @abstractmethod methods

From src/eq_chatbot_core/providers/__init__.py (re-export pattern, lines 152-188):
  Exports at end of file after function definitions, noqa: E402 comments
  __all__ list at bottom

pyproject.toml [project.optional-dependencies] existing extras: local, security, pdf, azure, vertex, server
  Insert realtime = ["websockets>=13.0,<17.0"] after vertex, before server.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create realtime/contracts.py — 12 constants, TypedDict, capabilities, Protocol</name>
  <files>src/eq_chatbot_core/realtime/contracts.py</files>
  <read_first>
    - .planning/phases/01-contracts-foundation/01-RESEARCH.md §Pattern 1 (12 verified strings), §Pattern 5 (RealtimeClosedError with code field), §RealtimeProviderCapabilities with session_sample_rate, §NormalizedRealtimeEvent TypedDict
    - .planning/phases/01-contracts-foundation/01-PATTERNS.md §contracts.py section (imports pattern, dataclass pattern, TypedDict pattern, Protocol pattern)
    - src/eq_chatbot_core/providers/base.py (frozen dataclass pattern — lines 1-10, 213-251)
  </read_first>
  <action>
    Create src/eq_chatbot_core/realtime/contracts.py (new file — the realtime package directory must be created first with mkdir -p).

    Imports: from dataclasses import dataclass; from typing import Any, TypedDict, Protocol, runtime_checkable; from collections.abc import AsyncIterator.

    Module docstring: "Realtime provider type contracts. String constants are frozen — any change requires a coordinated GlassAgents migration PR."

    Constant: INPUT_AUDIO_SAMPLE_RATE: int = 24_000 (with comment: # PCM16 24 kHz — matches OpenAI and Gemini defaults; prep for ElevenLabs 16kHz via session_sample_rate override)

    Class NormalizedRealtimeEventTypes — plain class (no ABC, no dataclass), 12 class-level string constants in this exact order and with these exact byte-for-byte values (per RESEARCH.md Pattern 1, verified from GlassAgents source):
      SESSION_READY = "session.ready"
      RESPONSE_AUDIO_DELTA = "response.audio.delta"
      RESPONSE_AUDIO_DONE = "response.audio.done"
      RESPONSE_DONE = "response.done"
      RESPONSE_CREATED = "response.created"
      INPUT_SPEECH_STARTED = "input.speech.started"
      INPUT_SPEECH_STOPPED = "input.speech.stopped"
      INPUT_AUDIO_COMMITTED = "input.audio.committed"
      TOOL_CALL_COMPLETED = "tool.call.completed"
      TOOL_CALL_CANCELLED = "tool.call.cancelled"
      ERROR = "error"
      UNHANDLED = "provider.event.unhandled"

    Class NormalizedRealtimeEvent(TypedDict, total=False) — keys: type: str, payload: dict[str, Any], source: str, raw: Any. Docstring: "Envelope for normalized events from any realtime provider."

    Class RealtimeProviderCapabilities — @dataclass(frozen=True, slots=True) with fields:
      streaming_audio_input: bool
      streaming_audio_output: bool
      server_vad: bool
      manual_turn_commit_required: bool
      tool_calling: bool
      tool_result_submission_mode: str  # "conversation_item" | "provider_call_id"
      voice_selection: bool
      interruption_cancel: bool
      startup_validation: bool = True
      session_sample_rate: int = 24_000  # ElevenLabs 16kHz prep — do not remove (PROV-FUT-03)

    Class RealtimeAdapterContract — @runtime_checkable Protocol (from typing import Protocol, runtime_checkable).
    Exactly 11 async methods with ... bodies (Protocol stubs):
      async def connect(self) -> None: ...
      async def close(self) -> None: ...
      async def initialize_session(self, *, instructions: str | None = None, voice: str | None = None, tools: list[Any] | None = None) -> None: ...
      async def update_session(self, payload: dict[str, Any]) -> None: ...
      async def append_client_audio(self, pcm16_audio: bytes) -> None: ...
      async def commit_client_turn(self) -> None: ...
      async def create_response(self) -> None: ...
      async def cancel_response(self, *, response_id: str | None = None) -> None: ...
      async def register_tools(self, tools: list[Any]) -> None: ...
      async def submit_tool_result(self, *, call_id: str, output: str) -> None: ...
      def iter_normalized_events(self) -> AsyncIterator[NormalizedRealtimeEvent]: ...

    Note: iter_normalized_events is a regular def returning AsyncIterator, not async def — this matches GlassAgents pattern and allows the mock to return a pre-built async generator.

    __all__ = ["INPUT_AUDIO_SAMPLE_RATE", "NormalizedRealtimeEventTypes", "NormalizedRealtimeEvent", "RealtimeProviderCapabilities", "RealtimeAdapterContract"]

    ruff format compliance: line-length 120, no trailing commas in single-element tuples.
    mypy strict: all parameters typed; no implicit Any.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && python -c "from eq_chatbot_core.realtime.contracts import NormalizedRealtimeEventTypes, NormalizedRealtimeEvent, RealtimeProviderCapabilities, RealtimeAdapterContract, INPUT_AUDIO_SAMPLE_RATE; assert INPUT_AUDIO_SAMPLE_RATE == 24000; print('contracts.py OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `from eq_chatbot_core.realtime.contracts import NormalizedRealtimeEventTypes` succeeds without error
    - `NormalizedRealtimeEventTypes.SESSION_READY == "session.ready"` is True
    - `NormalizedRealtimeEventTypes.UNHANDLED == "provider.event.unhandled"` is True (exactly 12 constants)
    - `INPUT_AUDIO_SAMPLE_RATE == 24000` is True
    - `RealtimeProviderCapabilities.__dataclass_params__.frozen` is True
    - `issubclass(RealtimeAdapterContract, object)` and runtime_checkable decorator present (no TypeError on isinstance check)
    - `ruff check src/eq_chatbot_core/realtime/contracts.py` exits 0
    - `mypy src/eq_chatbot_core/realtime/contracts.py --strict` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create realtime/abc.py — RealtimeProvider ABC + 7 event dataclasses</name>
  <files>src/eq_chatbot_core/realtime/abc.py</files>
  <read_first>
    - .planning/phases/01-contracts-foundation/01-RESEARCH.md §CON-06 row in requirements table (4 abstract methods, 7 event dataclasses)
    - .planning/phases/01-contracts-foundation/01-PATTERNS.md §abc.py section (ABC pattern from base.py, frozen dataclass for event types)
    - src/eq_chatbot_core/providers/base.py lines 68-184 (BaseLLMProvider ABC structure to mirror)
  </read_first>
  <action>
    Create src/eq_chatbot_core/realtime/abc.py (new file in the realtime package).

    Imports: from abc import ABC, abstractmethod; from collections.abc import AsyncIterator; from dataclasses import dataclass; from typing import Any, Union.

    Module docstring: "Minimal ABC for realtime providers plus typed event dataclasses. Production providers implement RealtimeAdapterContract (Protocol); stubs and mocks inherit RealtimeProvider (ABC)."

    7 frozen event dataclasses — all with @dataclass(frozen=True, slots=True):
      AudioDeltaEvent: audio: bytes, item_id: str, response_id: str
      AudioDoneEvent: item_id: str, response_id: str
      ResponseDoneEvent: response_id: str, status: str, output_tokens: int = 0
      ResponseCreatedEvent: response_id: str
      SpeechStartedEvent: item_id: str = ""
      SpeechStoppedEvent: item_id: str = ""
      ErrorEvent: message: str, error_type: str = "error", retriable: bool = False

    RealtimeEvent type alias: Union of all 7 event types above. Define as:
      RealtimeEvent = Union[AudioDeltaEvent, AudioDoneEvent, ResponseDoneEvent, ResponseCreatedEvent, SpeechStartedEvent, SpeechStoppedEvent, ErrorEvent]

    Class RealtimeProvider(ABC) — minimal ABC with docstring "Base class for realtime stubs and mocks. Production providers implement RealtimeAdapterContract via structural typing."
    Exactly 4 abstract methods:
      @abstractmethod async def connect(self) -> None: ...
      @abstractmethod async def close(self) -> None: ...
      @abstractmethod async def initialize_session(self, *, instructions: str | None = None, voice: str | None = None, tools: list[Any] | None = None) -> None: ...
      @abstractmethod def iter_normalized_events(self) -> AsyncIterator[Any]: ...

    __all__ = ["RealtimeProvider", "RealtimeEvent", "AudioDeltaEvent", "AudioDoneEvent", "ResponseDoneEvent", "ResponseCreatedEvent", "SpeechStartedEvent", "SpeechStoppedEvent", "ErrorEvent"]

    ruff + mypy strict compliance. No imports from realtime/contracts.py — abc.py is independently importable.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && python -c "from eq_chatbot_core.realtime.abc import RealtimeProvider, RealtimeEvent, AudioDeltaEvent, ErrorEvent; import inspect; abstract_methods = {m for m in dir(RealtimeProvider) if getattr(getattr(RealtimeProvider, m, None), '__isabstractmethod__', False)}; assert len(abstract_methods) == 4, f'Expected 4 abstract methods, got {abstract_methods}'; print('abc.py OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `from eq_chatbot_core.realtime.abc import RealtimeProvider, RealtimeEvent` succeeds
    - `RealtimeProvider.__abstractmethods__` has exactly 4 entries: connect, close, initialize_session, iter_normalized_events
    - All 7 event dataclasses are importable and frozen (frozen=True)
    - `AudioDeltaEvent(audio=b"", item_id="x", response_id="y")` instantiates without error
    - `ruff check src/eq_chatbot_core/realtime/abc.py` exits 0
    - `mypy src/eq_chatbot_core/realtime/abc.py --strict` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add ToolDefinition to providers/base.py + update pyproject.toml</name>
  <files>src/eq_chatbot_core/providers/base.py, pyproject.toml</files>
  <read_first>
    - .planning/phases/01-contracts-foundation/01-RESEARCH.md §ToolDefinition Dataclass Addition (exact field list + backward-compatible union signature)
    - .planning/phases/01-contracts-foundation/01-PATTERNS.md §providers/base.py MODIFIED section (insertion point: before BaseLLMProvider, after imports ~line 10; backward-compatible union in chat_completion line 115)
    - src/eq_chatbot_core/providers/base.py (full file — read before Edit to get exact line content)
    - pyproject.toml lines 44-81 (optional-dependencies section — insertion point after vertex, before server)
  </read_first>
  <action>
    MODIFICATION 1 — src/eq_chatbot_core/providers/base.py:

    The existing imports at line 8 already include `from dataclasses import dataclass, field` and `from typing import Any`. No new imports needed.

    Insert ToolDefinition as a new frozen dataclass AFTER the existing `@dataclass class StreamChunk:` block (after line ~66) and BEFORE `class BaseLLMProvider(ABC):` (before line ~68). The insertion must not break existing LLMResponse and StreamChunk.

    ToolDefinition definition (per RESEARCH.md §ToolDefinition Dataclass Addition):
      @dataclass(frozen=True, slots=True)
      class ToolDefinition:
          """Typed tool/function definition shared by chat and realtime providers.

          Parameters must use flat JSON Schema (no $ref, $defs, allOf, anyOf, oneOf).
          Both OpenAI and Gemini strip or reject JSON Schema references (PITFALL-12).
          """
          name: str
          description: str
          parameters: dict[str, Any]  # Flat JSON Schema dict
          strict: bool = False

    UPDATE chat_completion signature in BaseLLMProvider (line ~115):
      Change: tools: list[dict[str, Any]] | None = None,
      To: tools: list["ToolDefinition"] | list[dict[str, Any]] | None = None,

    Also update stream_completion signature at line ~145 identically.

    MODIFICATION 2 — pyproject.toml:

    In [project.optional-dependencies], after the vertex block and before the server block, insert:
      realtime = [
          "websockets>=13.0,<17.0",
      ]

    No other changes to pyproject.toml.

    After both edits: run ruff check + mypy on base.py to verify no regressions.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && python -c "from eq_chatbot_core.providers.base import ToolDefinition, BaseLLMProvider; td = ToolDefinition(name='test', description='desc', parameters={'type': 'object'}); assert td.name == 'test'; assert td.strict == False; print('ToolDefinition OK')" && python -c "import tomllib; data = tomllib.loads(open('pyproject.toml').read()); extras = data['project']['optional-dependencies']; assert 'realtime' in extras; assert any('websockets' in dep for dep in extras['realtime']); print('pyproject.toml [realtime] OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `from eq_chatbot_core.providers.base import ToolDefinition` succeeds
    - `ToolDefinition(name="fn", description="d", parameters={"type": "object"})` creates instance with strict=False
    - `ToolDefinition.__dataclass_params__.frozen` is True
    - `pyproject.toml` contains `realtime` key in `[project.optional-dependencies]` with `websockets>=13.0,<17.0`
    - Existing tests still pass: `pytest tests/unit/test_factory.py -q` exits 0 (no regressions to chat provider factory)
    - `ruff check src/eq_chatbot_core/providers/base.py` exits 0
    - `mypy src/eq_chatbot_core/providers/base.py` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundary | This plan creates type definitions, frozen dataclasses, a Protocol, an ABC, and pyproject.toml metadata. No network calls, no file I/O, no user input. All data flows are import-time declarations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | NormalizedRealtimeEventTypes string constants | mitigate | CON-13 (test_contracts.py byte-for-byte assertions) prevents silent drift; established in Plan 02 Wave 1 parallel |
| T-01-02 | Information Disclosure | ToolDefinition.parameters field (flat JSON Schema) | accept | Parameters are tool schemas, not secrets; no API keys or credentials; PITFALL-12 note in docstring guides consumers |
| T-01-SC | Tampering | websockets package install (pyproject.toml addition) | mitigate | Package legitimacy audit in RESEARCH.md confirms websockets [ASSUMED-OK]: 50M+/wk PyPI downloads, 12yr history, transitive dep of google-genai; blocking human-verify checkpoint is in Plan 03 (install-time) |
</threat_model>

<verification>
1. `python -c "from eq_chatbot_core.realtime.contracts import NormalizedRealtimeEventTypes, INPUT_AUDIO_SAMPLE_RATE, RealtimeAdapterContract, RealtimeProviderCapabilities"` — exits 0
2. `python -c "from eq_chatbot_core.realtime.abc import RealtimeProvider, AudioDeltaEvent"` — exits 0
3. `python -c "from eq_chatbot_core.providers.base import ToolDefinition"` — exits 0
4. `ruff check src/eq_chatbot_core/realtime/contracts.py src/eq_chatbot_core/realtime/abc.py src/eq_chatbot_core/providers/base.py` — exits 0
5. `mypy src/eq_chatbot_core/realtime/contracts.py src/eq_chatbot_core/realtime/abc.py src/eq_chatbot_core/providers/base.py` — exits 0
6. `pytest tests/unit/ -q -m unit` — no regressions in existing tests
</verification>

<success_criteria>
- realtime/ package directory exists with contracts.py and abc.py
- All 12 NormalizedRealtimeEventTypes constants are present with verified string values
- RealtimeAdapterContract is @runtime_checkable with exactly 11 async method stubs
- RealtimeProviderCapabilities is frozen with session_sample_rate: int = 24_000
- ToolDefinition is a frozen dataclass (slots=True) in providers/base.py
- chat_completion and stream_completion accept list[ToolDefinition] | list[dict] | None for tools parameter (non-breaking)
- pyproject.toml [realtime] extra declares websockets>=13.0,<17.0
- ruff + mypy clean on all modified files
- Existing unit test suite passes without regressions
</success_criteria>

<output>
Create `.planning/phases/01-contracts-foundation/01-01-SUMMARY.md` when done.
</output>
