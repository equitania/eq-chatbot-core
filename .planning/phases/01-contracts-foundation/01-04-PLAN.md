---
phase: 01-contracts-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - src/eq_chatbot_core/realtime/factory.py
  - src/eq_chatbot_core/realtime/mock.py
  - src/eq_chatbot_core/realtime/providers/__init__.py
  - tests/unit/realtime/test_mock.py
  - tests/unit/realtime/test_factory.py
autonomous: true
requirements:
  - CON-09
  - CON-11

must_haves:
  truths:
    - "get_realtime_provider('mock') returns a MockRealtimeProvider instance"
    - "get_realtime_provider('unknown_name') raises ValueError with sorted available names"
    - "MockRealtimeProvider is stdlib-only — imports only asyncio, collections.abc, typing"
    - "isinstance(MockRealtimeProvider(), RealtimeAdapterContract) is True without [realtime] installed"
    - "MockRealtimeProvider.enqueue_event() pre-loads events for iter_normalized_events consumption"
    - "MockRealtimeProvider.append_client_audio() raises ValueError for odd-length bytes (PCM16 invariant)"
    - "RealtimeProviderRegistry.register() and registered_names() work"
    - "build_default_realtime_provider_registry() registers 'mock' as a valid name"
    - "realtime/providers/__init__.py exists as empty package marker"
  artifacts:
    - path: "src/eq_chatbot_core/realtime/factory.py"
      provides: "RealtimeProviderDefinition, RealtimeProviderRegistry, get_realtime_provider, build_default_realtime_provider_registry"
      exports:
        - RealtimeProviderRegistry
        - RealtimeProviderDefinition
        - get_realtime_provider
        - build_default_realtime_provider_registry
    - path: "src/eq_chatbot_core/realtime/mock.py"
      provides: "MockRealtimeProvider — stdlib-only, queue-backed, satisfies RealtimeAdapterContract"
      exports:
        - MockRealtimeProvider
    - path: "src/eq_chatbot_core/realtime/providers/__init__.py"
      provides: "empty sub-package marker for Phase 2+ provider files"
    - path: "tests/unit/realtime/test_mock.py"
      provides: "isinstance check + queue-backed event flow tests"
      contains: "test_isinstance_check"
    - path: "tests/unit/realtime/test_factory.py"
      provides: "factory resolution tests"
      contains: "test_get_realtime_provider_mock"
  key_links:
    - from: "realtime/factory.py"
      to: "realtime/mock.py"
      via: "deferred import inside _get_realtime_provider_impl"
      pattern: "from eq_chatbot_core.realtime.mock import MockRealtimeProvider"
    - from: "tests/unit/realtime/test_mock.py"
      to: "realtime/contracts.py"
      via: "isinstance(MockRealtimeProvider(), RealtimeAdapterContract)"
      pattern: "isinstance.*RealtimeAdapterContract"
---

<objective>
Create the factory (RealtimeProviderRegistry + get_realtime_provider), the stdlib-only MockRealtimeProvider, and the empty providers/ sub-package. Also creates test_mock.py (success criterion #2: isinstance check without [realtime]) and test_factory.py.

This plan has zero file overlap with Plan 03 — both can conceptually run after Wave 2, but they depend on Plan 03 for the error hierarchy (factory and mock need the realtime package to exist).

Purpose: After this plan completes, `get_realtime_provider("mock")` works end-to-end and `isinstance(MockRealtimeProvider(), RealtimeAdapterContract)` returns True without websockets installed.

Output: 5 files; `pytest tests/unit/realtime/test_mock.py tests/unit/realtime/test_factory.py -v -m unit` green.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-contracts-foundation/01-RESEARCH.md
@.planning/phases/01-contracts-foundation/01-PATTERNS.md
@.planning/phases/01-contracts-foundation/01-01-SUMMARY.md
@.planning/phases/01-contracts-foundation/01-03-SUMMARY.md

<interfaces>
<!-- Factory pattern from providers/__init__.py (lines 90-149) -->
def get_provider(provider_name: str, ...) -> "BaseLLMProvider":
    from eq_chatbot_core.providers.openai_provider import OpenAIProvider  # deferred imports
    providers = {"openai": OpenAIProvider, ...}
    provider_name_lower = provider_name.lower()
    provider_class = providers.get(provider_name_lower)
    if provider_class is None:
        available = list(providers.keys())
        raise ValueError(f"Unknown provider: {provider_name}. Available: {', '.join(sorted(set(available)))}")
    return provider_class(api_key=api_key or "", ...)
# Adapt: get_realtime_provider(name, **kwargs) — no api_key positional arg; use **kwargs

<!-- MockRealtimeProvider (RESEARCH.md §Pattern 6 — no codebase analog) -->
import asyncio
from collections.abc import AsyncIterator
from typing import Any
class MockRealtimeProvider:
    def __init__(self) -> None:
        self._event_queue: asyncio.Queue[dict[str, Any]] = asyncio.Queue()
        self._connected: bool = False
    def enqueue_event(self, event: dict[str, Any]) -> None: self._event_queue.put_nowait(event)
    # 11 async methods matching RealtimeAdapterContract structurally (duck-typing)
    # append_client_audio: validate even-length
    # iter_normalized_events: returns async generator from _event_queue
    # __aenter__/__aexit__ for context manager

<!-- RealtimeAdapterContract (11 methods, from contracts.py Task 1) -->
connect, close, initialize_session, update_session, append_client_audio, commit_client_turn,
create_response, cancel_response, register_tools, submit_tool_result, iter_normalized_events
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create realtime/factory.py + realtime/providers/__init__.py</name>
  <files>src/eq_chatbot_core/realtime/factory.py, src/eq_chatbot_core/realtime/providers/__init__.py</files>
  <read_first>
    - .planning/phases/01-contracts-foundation/01-PATTERNS.md §factory.py section (dict-based factory pattern, ValueError with sorted list)
    - src/eq_chatbot_core/providers/__init__.py lines 90-149 (get_provider factory — exact pattern to mirror)
  </read_first>
  <action>
    Create src/eq_chatbot_core/realtime/providers/__init__.py as empty package marker with docstring:
    '"""Realtime provider implementations. Phase 2 (OpenAI) and Phase 3 (Gemini, Nova) add files here."""'

    Create src/eq_chatbot_core/realtime/factory.py.

    Imports: from dataclasses import dataclass; from typing import Any, Callable.

    Module docstring: "Realtime provider registry and factory. Use get_realtime_provider() — do not import this module directly."

    @dataclass class RealtimeProviderDefinition:
      name: str  # canonical name, lowercase
      factory_fn: Callable[..., Any]  # callable(**kwargs) -> provider instance
      description: str = ""

    class RealtimeProviderRegistry:
      __init__: sets self._providers: dict[str, RealtimeProviderDefinition] = {}
      register(self, definition: RealtimeProviderDefinition) -> None: self._providers[definition.name] = definition
      registered_names(self) -> list[str]: return sorted(self._providers.keys())
      get(self, name: str) -> RealtimeProviderDefinition | None: return self._providers.get(name)

    def build_default_realtime_provider_registry() -> RealtimeProviderRegistry:
      Creates a new registry and registers "mock" (and in Phase 3 will also register "nova_sonic").
      For "mock": factory_fn = lambda **kwargs: MockRealtimeProvider() (import inside function to keep stdlib-first).
      Import MockRealtimeProvider inside this function body (deferred): from eq_chatbot_core.realtime.mock import MockRealtimeProvider
      Returns the registry.

    _DEFAULT_REGISTRY: RealtimeProviderRegistry | None = None  # module-level singleton; lazy init

    def _get_realtime_provider_impl(name: str, **kwargs: Any) -> Any:
      (This is called from realtime/__init__.py after the websockets import guard.)
      global _DEFAULT_REGISTRY
      if _DEFAULT_REGISTRY is None:
          _DEFAULT_REGISTRY = build_default_realtime_provider_registry()
      name_lower = name.lower()
      definition = _DEFAULT_REGISTRY.get(name_lower)
      if definition is None:
          available = _DEFAULT_REGISTRY.registered_names()
          raise ValueError(f"Unknown realtime provider: {name}. Available: {', '.join(available)}")
      return definition.factory_fn(**kwargs)

    Note: In Phase 2, OpenAIRealtimeClient is registered here. In Phase 3, GeminiLiveClient and NovaSonicStub are registered. Each registration is a deferred import inside build_default_realtime_provider_registry() — no module-level imports of websockets-dependent providers.

    __all__ = ["RealtimeProviderRegistry", "RealtimeProviderDefinition", "get_realtime_provider", "build_default_realtime_provider_registry"]

    Wait — get_realtime_provider lives in realtime/__init__.py (public API), not here. Export _get_realtime_provider_impl from factory.py (the internal implementation called by __init__.py). The public get_realtime_provider in __init__.py does the websockets import guard first, then delegates here.

    ruff + mypy strict compliance. No websockets import in factory.py — factory is called after the guard fires in __init__.py.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && python -c "from eq_chatbot_core.realtime.factory import build_default_realtime_provider_registry, RealtimeProviderRegistry; reg = build_default_realtime_provider_registry(); names = reg.registered_names(); assert 'mock' in names, f'mock not in {names}'; print('factory.py OK:', names)"</automated>
  </verify>
  <acceptance_criteria>
    - `from eq_chatbot_core.realtime.factory import RealtimeProviderRegistry, RealtimeProviderDefinition, build_default_realtime_provider_registry` succeeds
    - `build_default_realtime_provider_registry().registered_names()` contains "mock"
    - `_get_realtime_provider_impl("unknown")` raises ValueError with "Available:" in the message
    - realtime/providers/__init__.py exists (package marker)
    - `ruff check src/eq_chatbot_core/realtime/factory.py src/eq_chatbot_core/realtime/providers/__init__.py` exits 0
    - `mypy src/eq_chatbot_core/realtime/factory.py` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create realtime/mock.py — stdlib-only MockRealtimeProvider</name>
  <files>src/eq_chatbot_core/realtime/mock.py</files>
  <read_first>
    - .planning/phases/01-contracts-foundation/01-RESEARCH.md §Pattern 6 (MockRealtimeProvider full code excerpt — this is the only pattern source; no codebase analog exists)
    - .planning/phases/01-contracts-foundation/01-PATTERNS.md §mock.py section (constraints: stdlib-only, 11 methods, asyncio.Queue, __aenter__/__aexit__)
    - .planning/phases/01-contracts-foundation/01-01-SUMMARY.md (confirm 11 methods of RealtimeAdapterContract from Task 1)
  </read_first>
  <action>
    Create src/eq_chatbot_core/realtime/mock.py.

    CRITICAL CONSTRAINT: This file must NOT import websockets or any non-stdlib module. Allowed imports: asyncio, collections.abc (AsyncIterator), typing (Any). This is the stdlib-only guarantee for success criterion #2.

    Class MockRealtimeProvider:
      Docstring: "Queue-backed in-process realtime provider. Stdlib-only (no websockets). Ships in the installed package for consumer test suites. Satisfies RealtimeAdapterContract via structural typing (duck-typing)."

      __init__(self) -> None:
        self._event_queue: asyncio.Queue[dict[str, Any]] = asyncio.Queue()
        self._connected: bool = False

      def enqueue_event(self, event: dict[str, Any]) -> None:
        """Pre-load an event for iter_normalized_events to yield."""
        self._event_queue.put_nowait(event)

      All 11 async methods matching RealtimeAdapterContract structurally (exact names required for isinstance check):
        async def connect(self) -> None: self._connected = True
        async def close(self) -> None: self._connected = False
        async def initialize_session(self, *, instructions: str | None = None, voice: str | None = None, tools: list[Any] | None = None) -> None: pass
        async def update_session(self, payload: dict[str, Any]) -> None: pass
        async def append_client_audio(self, pcm16_audio: bytes) -> None:
          if len(pcm16_audio) % 2 != 0:
              raise ValueError(f"PCM16 audio must be even-length bytes, got {len(pcm16_audio)}")
        async def commit_client_turn(self) -> None: pass
        async def create_response(self) -> None: pass
        async def cancel_response(self, *, response_id: str | None = None) -> None: pass
        async def register_tools(self, tools: list[Any]) -> None: pass
        async def submit_tool_result(self, *, call_id: str, output: str) -> None: pass

      def iter_normalized_events(self) -> AsyncIterator[dict[str, Any]]:
        """Returns async generator yielding all queued events then stopping."""
        return self._iter_impl()

      async def _iter_impl(self) -> AsyncIterator[dict[str, Any]]:
        while True:
            event = await self._event_queue.get()
            yield event
            if self._event_queue.empty():
                break

      async def __aenter__(self) -> "MockRealtimeProvider":
        await self.connect()
        return self

      async def __aexit__(self, *_: Any) -> None:
        await self.close()

    Note: iter_normalized_events is a regular def (not async def) returning AsyncIterator — matches RealtimeAdapterContract Protocol definition. _iter_impl is async def (generator) and is called by iter_normalized_events.

    __all__ = ["MockRealtimeProvider"]

    ruff + mypy strict compliance.
    After creating, verify no websockets import exists: `grep -n websockets src/eq_chatbot_core/realtime/mock.py` must return nothing.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && python -c "
from eq_chatbot_core.realtime.mock import MockRealtimeProvider
from eq_chatbot_core.realtime.contracts import RealtimeAdapterContract
m = MockRealtimeProvider()
assert isinstance(m, RealtimeAdapterContract), 'isinstance check failed'
print('MockRealtimeProvider isinstance OK')
" && grep -c "websockets" src/eq_chatbot_core/realtime/mock.py && echo "ERROR: websockets found in mock.py" || echo "No websockets import in mock.py (expected)"</automated>
  </verify>
  <acceptance_criteria>
    - `from eq_chatbot_core.realtime.mock import MockRealtimeProvider` succeeds without websockets installed
    - `isinstance(MockRealtimeProvider(), RealtimeAdapterContract)` returns True (Protocol structural check)
    - `grep -c websockets src/eq_chatbot_core/realtime/mock.py` returns 0 (no websockets import)
    - `MockRealtimeProvider().append_client_audio(b"\x00\x01")` succeeds (even length)
    - `MockRealtimeProvider().append_client_audio(b"\x00")` raises ValueError (odd length)
    - `ruff check src/eq_chatbot_core/realtime/mock.py` exits 0
    - `mypy src/eq_chatbot_core/realtime/mock.py` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create test_mock.py + test_factory.py</name>
  <files>tests/unit/realtime/test_mock.py, tests/unit/realtime/test_factory.py</files>
  <read_first>
    - .planning/phases/01-contracts-foundation/01-PATTERNS.md §test_mock.py section (isinstance Protocol check, async test pattern)
    - .planning/phases/01-contracts-foundation/01-RESEARCH.md §Pattern 6 (isinstance check code excerpt) + §CON-09 row in requirements table (factory test coverage)
  </read_first>
  <action>
    Create tests/unit/realtime/test_mock.py:

    Imports: import pytest, import asyncio, from eq_chatbot_core.realtime.mock import MockRealtimeProvider, from eq_chatbot_core.realtime.contracts import RealtimeAdapterContract

    @pytest.mark.unit def test_isinstance_check:
      assert isinstance(MockRealtimeProvider(), RealtimeAdapterContract)
      # Negative: a class missing iter_normalized_events should NOT satisfy it
      class _Incomplete:
          async def connect(self): pass
      assert not isinstance(_Incomplete(), RealtimeAdapterContract)

    @pytest.mark.unit async def test_connect_sets_connected:
      m = MockRealtimeProvider(); await m.connect(); assert m._connected is True

    @pytest.mark.unit async def test_close_sets_disconnected:
      m = MockRealtimeProvider(); await m.connect(); await m.close(); assert m._connected is False

    @pytest.mark.unit async def test_context_manager:
      async with MockRealtimeProvider() as m:
          assert m._connected is True
      assert m._connected is False

    @pytest.mark.unit async def test_enqueue_and_iter:
      m = MockRealtimeProvider()
      m.enqueue_event({"type": "session.ready", "payload": {}})
      m.enqueue_event({"type": "response.done", "payload": {}})
      events = []
      async for ev in m.iter_normalized_events():
          events.append(ev)
      assert len(events) == 2
      assert events[0]["type"] == "session.ready"
      assert events[1]["type"] == "response.done"

    @pytest.mark.unit async def test_append_client_audio_even_ok:
      m = MockRealtimeProvider()
      await m.append_client_audio(b"\x00\x01")  # 2 bytes — even, should not raise

    @pytest.mark.unit async def test_append_client_audio_odd_raises:
      m = MockRealtimeProvider()
      with pytest.raises(ValueError, match="even-length"):
          await m.append_client_audio(b"\x00")  # 1 byte — odd, must raise

    Create tests/unit/realtime/test_factory.py:

    Imports: import pytest, from eq_chatbot_core.realtime.factory import build_default_realtime_provider_registry, _get_realtime_provider_impl, from eq_chatbot_core.realtime.mock import MockRealtimeProvider

    @pytest.mark.unit def test_registry_contains_mock:
      reg = build_default_realtime_provider_registry()
      assert "mock" in reg.registered_names()

    @pytest.mark.unit def test_get_realtime_provider_mock:
      provider = _get_realtime_provider_impl("mock")
      assert isinstance(provider, MockRealtimeProvider)

    @pytest.mark.unit def test_get_realtime_provider_case_insensitive:
      provider = _get_realtime_provider_impl("Mock")
      assert isinstance(provider, MockRealtimeProvider)

    @pytest.mark.unit def test_get_realtime_provider_unknown_raises:
      with pytest.raises(ValueError, match="Available:"):
          _get_realtime_provider_impl("nonexistent_provider_xyz")

    @pytest.mark.unit def test_registry_registered_names_sorted:
      reg = build_default_realtime_provider_registry()
      names = reg.registered_names()
      assert names == sorted(names)  # must be alphabetically sorted
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && pytest tests/unit/realtime/test_mock.py tests/unit/realtime/test_factory.py -v -m unit 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/realtime/test_mock.py tests/unit/realtime/test_factory.py -v -m unit` exits 0
    - test_isinstance_check: isinstance(MockRealtimeProvider(), RealtimeAdapterContract) is True
    - test_enqueue_and_iter: events yield in correct order
    - test_append_client_audio_odd_raises: ValueError raised for odd-length bytes
    - test_get_realtime_provider_mock: factory returns MockRealtimeProvider instance
    - test_get_realtime_provider_unknown_raises: ValueError with "Available:" in message
    - `ruff check tests/unit/realtime/test_mock.py tests/unit/realtime/test_factory.py` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundary | factory.py and mock.py are in-process components with no network I/O. MockRealtimeProvider uses asyncio.Queue (in-memory only). Factory uses deferred imports (no network at import time). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | MockRealtimeProvider.append_client_audio PCM16 invariant | mitigate | Validates even-length bytes; raises ValueError immediately — consumer bugs surface at mock stage, not in production |
| T-04-02 | Denial of Service | _DEFAULT_REGISTRY singleton in factory.py | accept | Module-level singleton; no lock needed (single-threaded factory calls; asyncio event loop is single-threaded per Python GIL) |
| T-04-03 | Spoofing | Provider name collision ("MOCK" vs "mock") | mitigate | name.lower() normalization in _get_realtime_provider_impl prevents case-sensitivity confusion |
| T-04-SC | Tampering | No new package installs | accept | This plan creates Python-only source files with no new dependencies. No pip/uv install actions. |
</threat_model>

<verification>
1. `pytest tests/unit/realtime/test_mock.py tests/unit/realtime/test_factory.py -v -m unit` — all green
2. `python -c "from eq_chatbot_core.realtime.mock import MockRealtimeProvider; from eq_chatbot_core.realtime.contracts import RealtimeAdapterContract; assert isinstance(MockRealtimeProvider(), RealtimeAdapterContract); print('SUCCESS CRITERION 2 OK')"` — exits 0
3. `grep -c websockets src/eq_chatbot_core/realtime/mock.py` — returns 0
4. `ruff check src/eq_chatbot_core/realtime/factory.py src/eq_chatbot_core/realtime/mock.py` — exits 0
5. `mypy src/eq_chatbot_core/realtime/factory.py src/eq_chatbot_core/realtime/mock.py` — exits 0
</verification>

<success_criteria>
- get_realtime_provider("mock") resolves to MockRealtimeProvider (after Plan 05 wires the __init__.py)
- isinstance(MockRealtimeProvider(), RealtimeAdapterContract) is True — success criterion #2
- MockRealtimeProvider has no websockets import — stays stdlib-only
- PCM16 even-length invariant enforced in append_client_audio
- Factory ValueError on unknown name includes sorted available list
- Test suite for mock and factory passes
</success_criteria>

<output>
Create `.planning/phases/01-contracts-foundation/01-04-SUMMARY.md` when done.
</output>
