---
phase: 01-contracts-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/realtime/__init__.py
  - tests/unit/realtime/conftest.py
  - tests/unit/realtime/test_contracts.py
autonomous: true
requirements:
  - CON-13
  - QUAL-02

must_haves:
  truths:
    - "tests/unit/realtime/ is a pytest-discoverable package"
    - "conftest.py installs a session-scoped sys.modules websockets mock using AsyncMock (not MagicMock)"
    - "conftest.py provides a function-scoped mock_ws_instance fixture"
    - "test_contracts.py has inline byte-for-byte string assertions for all 12 NormalizedRealtimeEventTypes constants"
    - "test_contracts.py verifies INPUT_AUDIO_SAMPLE_RATE == 24000"
    - "test_contracts.py verifies RealtimeProviderCapabilities defaults (session_sample_rate=24000)"
    - "test_contracts.py verifies RealtimeAdapterContract is runtime_checkable"
    - "All tests run without real websockets installed (sys.modules mock covers the import)"
  artifacts:
    - path: "tests/unit/realtime/__init__.py"
      provides: "package marker"
    - path: "tests/unit/realtime/conftest.py"
      provides: "mock_websockets_module (session) + mock_ws_instance (function) fixtures"
      contains: "AsyncMock"
    - path: "tests/unit/realtime/test_contracts.py"
      provides: "CON-13 byte-for-byte assertions + CON-01..CON-06 structural tests"
      contains: "test_event_type_string_values"
  key_links:
    - from: "tests/unit/realtime/conftest.py"
      to: "sys.modules[websockets]"
      via: "mock_websockets_module session fixture"
      pattern: "AsyncMock.*connect"
    - from: "tests/unit/realtime/test_contracts.py"
      to: "eq_chatbot_core.realtime.contracts"
      via: "direct import (no mock needed)"
      pattern: "NormalizedRealtimeEventTypes\\.SESSION_READY.*session\\.ready"
---

<objective>
Create the realtime test package, the AsyncMock-based websockets conftest fixture, and test_contracts.py with inline byte-for-byte assertions for all 12 NormalizedRealtimeEventTypes constants.

Per RESEARCH.md recommendation: test_contracts.py is the first commit of Phase 1 (CON-13 before CON-01 implementation ships — the test is the spec). This plan runs in Wave 1 parallel with Plan 01 because the test files have zero file overlap with contracts.py / abc.py.

Purpose: Establish the GlassAgents migration safety gate. Any string constant drift is caught immediately in CI.

Output: 3 new files; `pytest tests/unit/realtime/test_contracts.py -v -m unit` green after Plan 01 completes.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-contracts-foundation/01-RESEARCH.md
@.planning/phases/01-contracts-foundation/01-PATTERNS.md

<interfaces>
<!-- Existing test patterns to replicate exactly -->

From tests/unit/test_openai.py (sys.modules mock injection — lines 7-14):
  import sys
  from unittest.mock import MagicMock
  sys.modules["openai"] = MagicMock()
  from eq_chatbot_core.providers.openai_provider import OpenAIProvider
  # Pattern: module-level injection before target import

From tests/conftest.py (mock fixture pattern — lines 641-661):
  @pytest.fixture
  def mock_openai_client():
      client = MagicMock()
      ...
      return client
  # DEVIATION for realtime: use AsyncMock for connect(), scope="session" for sys.modules injection

pyproject.toml line 138: asyncio_mode = "auto" — no @pytest.mark.asyncio needed on async test functions
pyproject.toml line 116-120: "tests/**/*.py" = ["E402", "B017"] — E402 already ignored for test files
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/unit/realtime/__init__.py and conftest.py</name>
  <files>tests/unit/realtime/__init__.py, tests/unit/realtime/conftest.py</files>
  <read_first>
    - .planning/phases/01-contracts-foundation/01-RESEARCH.md §Pattern 7 (AsyncMock for websockets conftest.py — full code excerpt)
    - .planning/phases/01-contracts-foundation/01-PATTERNS.md §tests/unit/realtime/conftest.py section (critical deviation: AsyncMock not MagicMock; scope rules)
    - tests/unit/__init__.py (check if it exists and is empty — replicate pattern)
  </read_first>
  <action>
    Step 1: Create tests/unit/realtime/__init__.py as an empty file (just a module docstring: '"""Realtime unit tests."""').

    Step 2: Create tests/unit/realtime/conftest.py.

    Imports section (before any from-imports — these must be at the top for E402 exemption):
      import sys
      from unittest.mock import AsyncMock, MagicMock
      import pytest

    Fixture mock_websockets_module — scope="session", autouse=True:
      - Creates mock_ws_module = MagicMock()
      - Creates mock_ws_instance = AsyncMock() with: closed=False, recv=AsyncMock(return_value='{"type":"test"}'), send=AsyncMock(), close=AsyncMock()
      - Sets mock_ws_module.connect = AsyncMock(return_value=mock_ws_instance)
      - Sets mock_ws_module.connect.return_value.__aenter__ = AsyncMock(return_value=mock_ws_instance)
      - Sets mock_ws_module.connect.return_value.__aexit__ = AsyncMock(return_value=False)
      - Imports REAL exception classes (so except clauses work) from websockets.exceptions: ConnectionClosed, ConnectionClosedOK, ConnectionClosedError, InvalidStatus, WebSocketException
        NOTE: websockets must be installed for this fixture to import real exceptions. If not installed, fall back to using MagicMock() for exceptions. Guard with try/except ImportError.
      - Sets mock_ws_module.exceptions = MagicMock() then assigns each real exception class attribute
      - Injects: sys.modules["websockets"] = mock_ws_module; sys.modules["websockets.exceptions"] = mock_ws_module.exceptions
      - yield mock_ws_module (do NOT clean up sys.modules on teardown — session scope; other tests may depend on mock)

    Fixture mock_ws_instance — scope="function" (default), depends on mock_websockets_module:
      Creates and returns a fresh AsyncMock() with closed=False, recv, send, close set to AsyncMock().
      Function scope prevents state leakage between tests (PITFALL-16 per RESEARCH.md).

    CRITICAL: websockets.connect() is used as `async with websockets.connect(...) as ws:` — MagicMock breaks this with TypeError. The AsyncMock + __aenter__/__aexit__ setup above is the correct pattern (PITFALL-14).
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && python -c "import ast, sys; ast.parse(open('tests/unit/realtime/conftest.py').read()); print('conftest.py parses OK')" && grep -c "AsyncMock" tests/unit/realtime/conftest.py</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/realtime/__init__.py exists (empty or module docstring only)
    - tests/unit/realtime/conftest.py parses without SyntaxError
    - conftest.py contains at least 2 occurrences of "AsyncMock" (connect + __aenter__ at minimum)
    - conftest.py does NOT contain bare "MagicMock()" for the connect call (only for the module wrapper)
    - `pytest tests/unit/realtime/ --collect-only -q` exits 0 (package is pytest-discoverable even before test files exist)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create tests/unit/realtime/test_contracts.py — CON-13 byte-for-byte assertions</name>
  <files>tests/unit/realtime/test_contracts.py</files>
  <read_first>
    - .planning/phases/01-contracts-foundation/01-RESEARCH.md §Code Examples "The 12 NormalizedRealtimeEventTypes — Exact Strings for test_contracts.py" (the canonical test code)
    - .planning/phases/01-contracts-foundation/01-PATTERNS.md §tests/unit/realtime/test_contracts.py section (no mock injection needed, inline assert pattern, @pytest.mark.unit)
    - tests/unit/test_openai.py lines 1-21 (pytest.mark.unit usage pattern)
  </read_first>
  <action>
    Create tests/unit/realtime/test_contracts.py.

    Module comment at top:
    """
    Byte-for-byte string assertions for NormalizedRealtimeEventTypes constants.
    These values are CANONICAL — any change requires a coordinated GlassAgents migration PR.
    See PITFALL-27 / PITFALL-29 in .planning/research/PITFALLS.md.
    CON-13 requirement.
    """

    Imports (no sys.modules mock needed — contracts.py is stdlib-only):
      import pytest
      from eq_chatbot_core.realtime.contracts import (
          NormalizedRealtimeEventTypes,
          NormalizedRealtimeEvent,
          RealtimeProviderCapabilities,
          RealtimeAdapterContract,
          INPUT_AUDIO_SAMPLE_RATE,
      )

    Test functions — each decorated with @pytest.mark.unit:

    test_event_type_string_values — inline asserts (not a helper) for all 12 constants:
      SESSION_READY == "session.ready"
      RESPONSE_AUDIO_DELTA == "response.audio.delta"
      RESPONSE_AUDIO_DONE == "response.audio.done"
      RESPONSE_DONE == "response.done"
      RESPONSE_CREATED == "response.created"
      INPUT_SPEECH_STARTED == "input.speech.started"
      INPUT_SPEECH_STOPPED == "input.speech.stopped"
      INPUT_AUDIO_COMMITTED == "input.audio.committed"
      TOOL_CALL_COMPLETED == "tool.call.completed"
      TOOL_CALL_CANCELLED == "tool.call.cancelled"
      ERROR == "error"
      UNHANDLED == "provider.event.unhandled"
    All 12 must be inline assert statements — not a loop, not a helper function. Pytest assertion rewriting works best on inline asserts; a helper hides which constant drifted.

    test_event_type_count — verifies exactly 12 public string attributes on NormalizedRealtimeEventTypes:
      attrs = [k for k, v in vars(NormalizedRealtimeEventTypes).items() if not k.startswith("_") and isinstance(v, str)]
      assert len(attrs) == 12

    test_input_audio_sample_rate:
      assert INPUT_AUDIO_SAMPLE_RATE == 24_000
      assert INPUT_AUDIO_SAMPLE_RATE == 24000  # same value, decimal form

    test_normalized_event_typeddict:
      from typing import get_type_hints
      hints = get_type_hints(NormalizedRealtimeEvent)
      assert "type" in hints
      assert "payload" in hints
      assert "source" in hints
      assert "raw" in hints

    test_capabilities_defaults:
      # Cannot instantiate without required fields; check field defaults only
      import dataclasses
      fields = {f.name: f.default for f in dataclasses.fields(RealtimeProviderCapabilities) if f.default is not dataclasses.MISSING}
      assert fields["session_sample_rate"] == 24_000
      assert fields["startup_validation"] is True

    test_adapter_contract_runtime_checkable:
      # A class with all 11 method names satisfies the Protocol
      class _FullImpl:
          async def connect(self): ...
          async def close(self): ...
          async def initialize_session(self, *, instructions=None, voice=None, tools=None): ...
          async def update_session(self, payload): ...
          async def append_client_audio(self, pcm16_audio): ...
          async def commit_client_turn(self): ...
          async def create_response(self): ...
          async def cancel_response(self, *, response_id=None): ...
          async def register_tools(self, tools): ...
          async def submit_tool_result(self, *, call_id, output): ...
          def iter_normalized_events(self): ...
      assert isinstance(_FullImpl(), RealtimeAdapterContract)
      # A class missing one method must NOT satisfy it
      class _MissingOne:
          async def connect(self): ...
          # ... intentionally missing close and other methods
      assert not isinstance(_MissingOne(), RealtimeAdapterContract)

    test_capabilities_frozen:
      import dataclasses
      assert RealtimeProviderCapabilities.__dataclass_params__.frozen is True

    ruff + asyncio_mode = "auto" means async test functions need no @pytest.mark.asyncio.
    All tests in this file are synchronous (no await needed for type inspection).
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && pytest tests/unit/realtime/test_contracts.py -v -m unit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/realtime/test_contracts.py -v -m unit` exits 0 (all tests pass)
    - test_event_type_string_values passes with all 12 inline assertions
    - test_event_type_count verifies exactly 12 string constants
    - test_adapter_contract_runtime_checkable verifies isinstance works and negative case works
    - No test uses a loop or helper for the 12 string assertions (all inline per anti-pattern rule)
    - `ruff check tests/unit/realtime/test_contracts.py` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundary | Test infrastructure and test files only. No network I/O, no file writes outside the test package. The sys.modules mock is a controlled in-process substitution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | sys.modules["websockets"] mock injection | accept | Test-only mock; session-scoped fixture; does not affect production code paths. Real exception classes are imported from websockets.exceptions for correctness of except clauses. |
| T-02-02 | Information Disclosure | Test fixture replay data | accept | conftest.py uses synthetic '{"type":"test"}' — no real API credentials. Pre-commit hook for API key patterns added in Plan 05. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed by this plan. websockets is already declared in pyproject.toml [realtime] by Plan 01. This plan only creates test files. |
</threat_model>

<verification>
1. `pytest tests/unit/realtime/test_contracts.py -v -m unit` — all tests green
2. `pytest tests/unit/realtime/ --collect-only -q` — package discoverable, no collection errors
3. `ruff check tests/unit/realtime/` — exits 0
4. Verify conftest.py uses AsyncMock: `grep -c "AsyncMock" tests/unit/realtime/conftest.py` returns >= 2
</verification>

<success_criteria>
- tests/unit/realtime/ is a pytest-discoverable package
- conftest.py session fixture installs sys.modules["websockets"] mock with AsyncMock for connect
- conftest.py function fixture provides fresh mock_ws_instance per test
- test_contracts.py has 12 inline byte-for-byte string assertions that pass after Plan 01 completes
- test_event_type_count confirms exactly 12 constants (future additions trigger CI failure)
- isinstance check for RealtimeAdapterContract verified positive + negative cases
- ruff clean on all 3 files
</success_criteria>

<output>
Create `.planning/phases/01-contracts-foundation/01-02-SUMMARY.md` when done.
</output>
