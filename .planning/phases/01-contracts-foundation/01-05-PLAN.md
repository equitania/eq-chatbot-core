---
phase: 01-contracts-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
  - "01-04"
files_modified:
  - src/eq_chatbot_core/realtime/__init__.py
  - src/eq_chatbot_core/providers/__init__.py
  - tests/unit/realtime/test_import_guard.py
  - tests/unit/realtime/test_pyproject.py
autonomous: true
requirements:
  - CON-10
  - CON-12
  - QUAL-02

must_haves:
  truths:
    - "from eq_chatbot_core.realtime import MockRealtimeProvider, RealtimeAdapterContract, INPUT_AUDIO_SAMPLE_RATE succeeds without [realtime] installed"
    - "from eq_chatbot_core.realtime import get_realtime_provider raises friendly ImportError when websockets is absent"
    - "from eq_chatbot_core.realtime import get_realtime_provider, RealtimeAdapterContract, INPUT_AUDIO_SAMPLE_RATE all resolve when [realtime] is installed"
    - "REALTIME_PROVIDERS list is exported from realtime/__init__.py and contains exactly mock, openai, gemini_live, nova_sonic"
    - "providers/__init__.py re-exports ToolDefinition and it appears in __all__"
    - "pyproject.toml [realtime] extra with websockets>=13.0,<17.0 is verifiable by test_pyproject.py"
    - "pytest tests/unit/realtime/ -v passes entirely"
  artifacts:
    - path: "src/eq_chatbot_core/realtime/__init__.py"
      provides: "public API: get_realtime_provider (guarded), REALTIME_PROVIDERS, all contracts/abc/mock re-exports"
      exports:
        - get_realtime_provider
        - REALTIME_PROVIDERS
        - RealtimeAdapterContract
        - RealtimeProviderCapabilities
        - NormalizedRealtimeEventTypes
        - NormalizedRealtimeEvent
        - INPUT_AUDIO_SAMPLE_RATE
        - RealtimeProvider
        - RealtimeEvent
        - MockRealtimeProvider
    - path: "src/eq_chatbot_core/providers/__init__.py"
      provides: "ToolDefinition re-exported from base.py, added to __all__"
      contains: "ToolDefinition"
    - path: "tests/unit/realtime/test_import_guard.py"
      provides: "CON-10 test: friendly ImportError path + positive resolution path"
      contains: "test_import_guard_friendly_error"
    - path: "tests/unit/realtime/test_pyproject.py"
      provides: "CON-12 static test: websockets>=13.0,<17.0 in pyproject.toml [realtime]"
      contains: "test_realtime_extra_declared"
  key_links:
    - from: "realtime/__init__.py"
      to: "realtime/factory.py"
      via: "deferred import inside get_realtime_provider() function body"
      pattern: "_get_realtime_provider_impl"
    - from: "providers/__init__.py"
      to: "providers/base.py"
      via: "re-export ToolDefinition"
      pattern: "ToolDefinition"
---

<objective>
Wire the public API: realtime/__init__.py with import guard, re-exports, and REALTIME_PROVIDERS constant. Add ToolDefinition re-export to providers/__init__.py. Create test_import_guard.py and test_pyproject.py.

This is the final assembly plan — after it completes all 5 phase success criteria are verifiable.

Purpose: Consumers get a single-import entry point. The import guard fires a friendly message (not bare ModuleNotFoundError) when websockets is absent. MockRealtimeProvider and contracts remain importable regardless.

Output: 4 files modified/created; full `pytest tests/unit/realtime/ -v -m unit` green.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-contracts-foundation/01-RESEARCH.md
@.planning/phases/01-contracts-foundation/01-PATTERNS.md
@.planning/phases/01-contracts-foundation/01-04-SUMMARY.md

<interfaces>
<!-- providers/__init__.py exports pattern (lines 152-188) -->
from eq_chatbot_core.providers.base import (  # noqa: E402
    AuthenticationError, BaseLLMProvider, ContextLengthError, LLMResponse, ModelInfo, OverloadedError,
    ProviderError, RateLimitError, StreamChunk,
)
__all__ = ["get_provider", "BaseLLMProvider", ..., "CLOUD_PROVIDERS", "LOCAL_PROVIDERS"]
# ADD: from eq_chatbot_core.providers.base import ToolDefinition  # noqa: E402
# ADD: "ToolDefinition" to __all__

<!-- realtime/__init__.py import guard pattern (RESEARCH.md §Pattern 2) -->
# Always importable (stdlib-only):
from eq_chatbot_core.realtime.contracts import NormalizedRealtimeEventTypes, ...
from eq_chatbot_core.realtime.mock import MockRealtimeProvider
# Guarded at call time (not module import time):
def get_realtime_provider(name: str, **kwargs: Any) -> Any:
    try:
        import websockets  # noqa: F401
    except ImportError as exc:
        raise ImportError(
            "eq-chatbot-core[realtime] is required for realtime voice support. "
            "Install with: pip install eq-chatbot-core[realtime]"
        ) from exc
    from eq_chatbot_core.realtime.factory import _get_realtime_provider_impl
    return _get_realtime_provider_impl(name, **kwargs)

<!-- REALTIME_PROVIDERS constant (RESEARCH.md §Pattern 2) -->
REALTIME_PROVIDERS: list[str] = ["openai", "gemini_live", "nova_sonic", "mock"]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create realtime/__init__.py — import guard + public re-exports</name>
  <files>src/eq_chatbot_core/realtime/__init__.py</files>
  <read_first>
    - .planning/phases/01-contracts-foundation/01-RESEARCH.md §Pattern 2 (import guard full code excerpt) + §Anti-Patterns to Avoid (no websockets at module level)
    - .planning/phases/01-contracts-foundation/01-PATTERNS.md §realtime/__init__.py section (Analog 1: constants list + TYPE_CHECKING guard; Analog 2: deferred import guard)
    - src/eq_chatbot_core/providers/__init__.py lines 35-41, 152-188 (constants + exports-after-function pattern to mirror)
  </read_first>
  <action>
    Create src/eq_chatbot_core/realtime/__init__.py.

    Module docstring:
    """
    Realtime voice provider integration for eq-chatbot-core.

    Usage (requires [realtime] extra: pip install eq-chatbot-core[realtime]):
        from eq_chatbot_core.realtime import get_realtime_provider
        provider = get_realtime_provider("openai", api_key="...")

    MockRealtimeProvider and type contracts are always importable (no extra needed):
        from eq_chatbot_core.realtime import MockRealtimeProvider, RealtimeAdapterContract
    """

    REALTIME_PROVIDERS: list[str] = ["openai", "gemini_live", "nova_sonic", "mock"]

    from typing import Any

    def get_realtime_provider(name: str, **kwargs: Any) -> Any:
        """Factory for realtime voice providers.

        Args:
            name: Provider name — one of "openai", "gemini_live", "nova_sonic", "mock"
            **kwargs: Provider-specific arguments (api_key, model, voice, ...)

        Returns:
            Provider instance satisfying RealtimeAdapterContract.

        Raises:
            ImportError: When eq-chatbot-core[realtime] is not installed.
            ValueError: When name is not a registered provider.
        """
        try:
            import websockets  # noqa: F401
        except ImportError as exc:
            raise ImportError(
                "eq-chatbot-core[realtime] is required for realtime voice support. "
                "Install with: pip install eq-chatbot-core[realtime]"
            ) from exc
        from eq_chatbot_core.realtime.factory import _get_realtime_provider_impl
        return _get_realtime_provider_impl(name, **kwargs)

    # Always importable (stdlib-only) — no websockets required for these imports
    from eq_chatbot_core.realtime.contracts import (  # noqa: E402
        INPUT_AUDIO_SAMPLE_RATE,
        NormalizedRealtimeEvent,
        NormalizedRealtimeEventTypes,
        RealtimeAdapterContract,
        RealtimeProviderCapabilities,
    )
    from eq_chatbot_core.realtime.abc import (  # noqa: E402
        AudioDeltaEvent,
        AudioDoneEvent,
        ErrorEvent,
        RealtimeEvent,
        RealtimeProvider,
        ResponseCreatedEvent,
        ResponseDoneEvent,
        SpeechStartedEvent,
        SpeechStoppedEvent,
    )
    from eq_chatbot_core.realtime.mock import MockRealtimeProvider  # noqa: E402

    __all__ = [
        "get_realtime_provider",
        "REALTIME_PROVIDERS",
        # Contracts
        "INPUT_AUDIO_SAMPLE_RATE",
        "NormalizedRealtimeEvent",
        "NormalizedRealtimeEventTypes",
        "RealtimeAdapterContract",
        "RealtimeProviderCapabilities",
        # ABC + events
        "RealtimeProvider",
        "RealtimeEvent",
        "AudioDeltaEvent",
        "AudioDoneEvent",
        "ResponseDoneEvent",
        "ResponseCreatedEvent",
        "SpeechStartedEvent",
        "SpeechStoppedEvent",
        "ErrorEvent",
        # Mock
        "MockRealtimeProvider",
    ]

    CRITICAL ANTI-PATTERN to avoid: Do NOT put `import websockets` or `from eq_chatbot_core.realtime.factory import ...` at module level. The websockets check must stay inside the get_realtime_provider() function body. If websockets is imported at module level, all consumers without [realtime] will get ModuleNotFoundError on package import.

    ruff + mypy strict compliance. TYPE_CHECKING guard not needed here (no forward references).
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && python -c "
# Test 1: always-importable names work without websockets
from eq_chatbot_core.realtime import MockRealtimeProvider, RealtimeAdapterContract, INPUT_AUDIO_SAMPLE_RATE, REALTIME_PROVIDERS
assert INPUT_AUDIO_SAMPLE_RATE == 24000
assert 'mock' in REALTIME_PROVIDERS
print('Always-importable names OK')

# Test 2: isinstance check (success criterion 2)
from eq_chatbot_core.realtime import MockRealtimeProvider, RealtimeAdapterContract
assert isinstance(MockRealtimeProvider(), RealtimeAdapterContract)
print('SUCCESS CRITERION 2: isinstance check OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `from eq_chatbot_core.realtime import MockRealtimeProvider, RealtimeAdapterContract, INPUT_AUDIO_SAMPLE_RATE` succeeds
    - `isinstance(MockRealtimeProvider(), RealtimeAdapterContract)` is True (success criterion #2)
    - REALTIME_PROVIDERS contains all 4 provider names: openai, gemini_live, nova_sonic, mock
    - `from eq_chatbot_core.realtime import get_realtime_provider` does NOT raise — only calling get_realtime_provider() triggers ImportError when websockets absent
    - `ruff check src/eq_chatbot_core/realtime/__init__.py` exits 0
    - `mypy src/eq_chatbot_core/realtime/__init__.py` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update providers/__init__.py — add ToolDefinition re-export</name>
  <files>src/eq_chatbot_core/providers/__init__.py</files>
  <read_first>
    - src/eq_chatbot_core/providers/__init__.py (full file — needed for Edit to match exactly)
    - src/eq_chatbot_core/providers/base.py (confirm ToolDefinition is there after Plan 01 Task 3)
  </read_first>
  <action>
    MODIFICATION to src/eq_chatbot_core/providers/__init__.py.

    In the exports block (lines ~153-164), add ToolDefinition to the existing base.py import:
    Change:
      from eq_chatbot_core.providers.base import (  # noqa: E402
          AuthenticationError,
          BaseLLMProvider,
          ContextLengthError,
          LLMResponse,
          ModelInfo,
          OverloadedError,
          ProviderError,
          RateLimitError,
          StreamChunk,
      )
    To:
      from eq_chatbot_core.providers.base import (  # noqa: E402
          AuthenticationError,
          BaseLLMProvider,
          ContextLengthError,
          LLMResponse,
          ModelInfo,
          OverloadedError,
          ProviderError,
          RateLimitError,
          StreamChunk,
          ToolDefinition,
      )

    In __all__, add "ToolDefinition" to the list (alphabetical position or at end — consistent with existing style).

    No other changes. Do not modify get_provider(), CLOUD_PROVIDERS, LOCAL_PROVIDERS, or any other existing export.

    Verify: `from eq_chatbot_core.providers import ToolDefinition` succeeds and `"ToolDefinition" in eq_chatbot_core.providers.__all__` is True.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && python -c "from eq_chatbot_core.providers import ToolDefinition; import eq_chatbot_core.providers as p; assert 'ToolDefinition' in p.__all__; print('ToolDefinition re-export OK')" && pytest tests/unit/test_factory.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `from eq_chatbot_core.providers import ToolDefinition` succeeds
    - `"ToolDefinition" in eq_chatbot_core.providers.__all__` is True
    - `pytest tests/unit/test_factory.py -q` exits 0 (no regressions to existing factory tests)
    - `ruff check src/eq_chatbot_core/providers/__init__.py` exits 0
    - `mypy src/eq_chatbot_core/providers/__init__.py` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create test_import_guard.py + test_pyproject.py — phase exit gate tests</name>
  <files>tests/unit/realtime/test_import_guard.py, tests/unit/realtime/test_pyproject.py</files>
  <read_first>
    - .planning/phases/01-contracts-foundation/01-VALIDATION.md (validation contract — the 5 invariants; friendly ImportError test is invariant 3)
    - .planning/phases/01-contracts-foundation/01-RESEARCH.md §CON-10 row (import guard behavior: ImportError with install instructions, not bare ModuleNotFoundError)
    - pyproject.toml lines 44-81 (structure to parse in test_pyproject.py)
  </read_first>
  <action>
    Create tests/unit/realtime/test_import_guard.py:

    Module docstring: "CON-10: Verifies friendly ImportError when [realtime] extra is absent. Tests the get_realtime_provider() import guard behavior."

    Imports: import sys, from unittest.mock import patch, import pytest

    @pytest.mark.unit def test_import_guard_friendly_error:
      """Success criterion #3: get_realtime_provider raises friendly ImportError when websockets absent."""
      # Temporarily hide websockets from sys.modules to simulate [realtime] not installed
      original_websockets = sys.modules.pop("websockets", None)
      original_ws_exc = sys.modules.pop("websockets.exceptions", None)
      try:
          with patch.dict(sys.modules, {"websockets": None}):
              from eq_chatbot_core.realtime import get_realtime_provider
              with pytest.raises(ImportError) as exc_info:
                  get_realtime_provider("mock")
              err_msg = str(exc_info.value)
              assert "eq-chatbot-core[realtime]" in err_msg, f"Missing install hint in: {err_msg}"
              assert "pip install" in err_msg, f"Missing pip install instruction in: {err_msg}"
      finally:
          # Restore websockets in sys.modules
          if original_websockets is not None:
              sys.modules["websockets"] = original_websockets
          if original_ws_exc is not None:
              sys.modules["websockets.exceptions"] = original_ws_exc

    @pytest.mark.unit def test_always_importable_without_websockets:
      """Success criterion #2: MockRealtimeProvider and contracts importable without [realtime]."""
      from eq_chatbot_core.realtime import MockRealtimeProvider, RealtimeAdapterContract, INPUT_AUDIO_SAMPLE_RATE
      # These must not raise even when websockets mock is active
      assert INPUT_AUDIO_SAMPLE_RATE == 24000
      assert isinstance(MockRealtimeProvider(), RealtimeAdapterContract)

    @pytest.mark.unit def test_realtime_providers_constant:
      from eq_chatbot_core.realtime import REALTIME_PROVIDERS
      assert "mock" in REALTIME_PROVIDERS
      assert "openai" in REALTIME_PROVIDERS
      assert "gemini_live" in REALTIME_PROVIDERS
      assert "nova_sonic" in REALTIME_PROVIDERS
      assert len(REALTIME_PROVIDERS) == 4

    Create tests/unit/realtime/test_pyproject.py:

    Module docstring: "CON-12: Static check that pyproject.toml declares the [realtime] extra with correct websockets version bounds."

    Imports: import tomllib, import pytest, from pathlib import Path

    @pytest.mark.unit def test_realtime_extra_declared:
      pyproject_path = Path(__file__).parent.parent.parent.parent / "pyproject.toml"
      assert pyproject_path.exists(), f"pyproject.toml not found at {pyproject_path}"
      with open(pyproject_path, "rb") as f:
          data = tomllib.load(f)
      optional_deps = data.get("project", {}).get("optional-dependencies", {})
      assert "realtime" in optional_deps, "Missing [realtime] extra in pyproject.toml"
      realtime_deps = optional_deps["realtime"]
      websockets_deps = [d for d in realtime_deps if d.startswith("websockets")]
      assert len(websockets_deps) == 1, f"Expected 1 websockets dep, got: {websockets_deps}"
      dep_str = websockets_deps[0]
      assert ">=13.0" in dep_str, f"Missing >=13.0 lower bound: {dep_str}"
      assert "<17.0" in dep_str, f"Missing <17.0 upper bound: {dep_str}"

    @pytest.mark.unit def test_dev_extra_has_pytest_asyncio:
      """Verify pytest-asyncio is in [dev] extra — required for all async realtime unit tests."""
      pyproject_path = Path(__file__).parent.parent.parent.parent / "pyproject.toml"
      with open(pyproject_path, "rb") as f:
          data = tomllib.load(f)
      dev_deps = data.get("project", {}).get("optional-dependencies", {}).get("dev", [])
      asyncio_deps = [d for d in dev_deps if "pytest-asyncio" in d]
      assert len(asyncio_deps) >= 1, "pytest-asyncio not found in [dev] extra"

    Note: tomllib is stdlib in Python 3.11+. For Python 3.10 compatibility: import sys; if sys.version_info < (3, 11): import tomli as tomllib. But since tomli is not in pyproject.toml dependencies, use `import tomllib` directly and add a version check comment noting this test requires Python 3.11+. Alternative: use `importlib.util.find_spec("tomllib")` guard. Preferred: just use `import tomllib` — pyproject.toml says requires-python = ">=3.10" but the CI matrix covers 3.10–3.13; on Python 3.10, tomllib is absent. Guard the test: `@pytest.mark.skipif(sys.version_info < (3, 11), reason="tomllib requires Python 3.11+")`. Python 3.10 users can install `tomli` as a fallback — add try/except import.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && pytest tests/unit/realtime/test_import_guard.py tests/unit/realtime/test_pyproject.py -v -m unit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/realtime/test_import_guard.py tests/unit/realtime/test_pyproject.py -v -m unit` exits 0
    - test_import_guard_friendly_error: ImportError message contains "eq-chatbot-core[realtime]" and "pip install"
    - test_always_importable_without_websockets: MockRealtimeProvider and contracts import cleanly
    - test_realtime_providers_constant: REALTIME_PROVIDERS has exactly 4 entries
    - test_realtime_extra_declared: websockets>=13.0,<17.0 found in pyproject.toml [realtime]
    - `ruff check tests/unit/realtime/test_import_guard.py tests/unit/realtime/test_pyproject.py` exits 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All 5 phase success criteria should now be verifiable. Run these commands to confirm the phase is complete:

    1. `pytest tests/unit/realtime/test_contracts.py -v -k test_event_type_string_values` — criterion 1 (12 byte-for-byte strings)
    2. `python -c "from eq_chatbot_core.realtime import MockRealtimeProvider, RealtimeAdapterContract; assert isinstance(MockRealtimeProvider(), RealtimeAdapterContract); print('CRITERION 2 OK')"` — criterion 2 (isinstance without [realtime])
    3. `pytest tests/unit/realtime/test_import_guard.py::test_import_guard_friendly_error -v` — criterion 3 (friendly ImportError)
    4. `python -c "from eq_chatbot_core.realtime import get_realtime_provider, RealtimeAdapterContract, INPUT_AUDIO_SAMPLE_RATE; print('CRITERION 4 OK')"` — criterion 4 (all names resolve with [realtime] installed)
    5. `pytest tests/unit/realtime/test_websocket_client.py::test_connect_with_backoff_3_failures_then_success -v` — criterion 5 (backoff test)

    Also run the full suite: `pytest tests/unit/realtime/ -v -m unit`
    And verify no regressions: `pytest tests/unit/ -q`
  </what-built>
  <how-to-verify>
    Run all 5 commands above and the full suite. All must exit 0.
    Also verify: `ruff check src/eq_chatbot_core/realtime/ src/eq_chatbot_core/providers/base.py src/eq_chatbot_core/providers/__init__.py` exits 0.
    And: `mypy src/eq_chatbot_core/realtime/ src/eq_chatbot_core/providers/base.py` exits 0.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 criteria pass, or describe which criteria failed and the error output.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Import-time vs call-time | The import guard ensures websockets is checked at call time (not import time). This is the critical security-relevant behavior: a consumer importing realtime/__init__.py without [realtime] must NOT get a crash; they must get a usable package with MockRealtimeProvider and types. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | get_realtime_provider ImportError message | accept | Message contains only "eq-chatbot-core[realtime]" and "pip install" — no secrets, no internal paths |
| T-05-02 | Tampering | REALTIME_PROVIDERS list (Phase 2/3 will add to factory, list must match) | mitigate | REALTIME_PROVIDERS is declared in __init__.py as the authoritative list; test_realtime_providers_constant verifies count==4; Phase 2 plan must update both the factory and this list |
| T-05-03 | Denial of Service | sys.modules mutation in test_import_guard.py | accept | Test-only; uses patch.dict + try/finally to restore state; session-scoped conftest already injects websockets mock, so this test carefully pops and restores |
| T-05-SC | Tampering | No new package installs | accept | This plan only creates/modifies Python source files. No pip/uv install actions needed. |
</threat_model>

<verification>
Run ALL 5 phase success criteria commands (listed in the checkpoint task above), then:
1. `pytest tests/unit/realtime/ -v -m unit` — entire realtime test suite green
2. `pytest tests/unit/ -q` — no regressions in existing tests
3. `ruff check src/eq_chatbot_core/realtime/ src/eq_chatbot_core/providers/base.py src/eq_chatbot_core/providers/__init__.py` — exits 0
4. `mypy src/eq_chatbot_core/realtime/ src/eq_chatbot_core/providers/base.py src/eq_chatbot_core/providers/__init__.py` — exits 0
</verification>

<success_criteria>
All 5 phase success criteria verified:
1. test_contracts.py byte-for-byte assertions pass for all 12 NormalizedRealtimeEventTypes constants
2. isinstance(MockRealtimeProvider(), RealtimeAdapterContract) is True without [realtime] extra
3. get_realtime_provider() raises ImportError with "eq-chatbot-core[realtime]" and "pip install" in message when websockets absent
4. from eq_chatbot_core.realtime import get_realtime_provider, RealtimeAdapterContract, INPUT_AUDIO_SAMPLE_RATE all resolve with [realtime] installed
5. test_connect_with_backoff_3_failures_then_success passes: delays [1.0, 2.0, 4.0], 4 attempts, no network

Additional:
- ToolDefinition importable from providers package (CON-07)
- REALTIME_PROVIDERS constant lists exactly 4 providers
- Full realtime unit test suite green
- No regressions in existing test suite
- ruff + mypy clean across all modified files
</success_criteria>

<output>
Create `.planning/phases/01-contracts-foundation/01-05-SUMMARY.md` when done.
</output>
