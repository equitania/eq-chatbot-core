---
phase: 00-codebase-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 00-01
files_modified:
  - src/eq_chatbot_core/cli.py
  - src/eq_chatbot_core/server/app.py
autonomous: true
requirements:
  - CLN-02
  - CLN-03
  - CLN-04

must_haves:
  truths:
    - "cli.py imports CLOUD_PROVIDERS and LOCAL_PROVIDERS from providers/__init__.py — no inline list definitions remain"
    - "server/app.py imports CLOUD_PROVIDERS and LOCAL_PROVIDERS from providers/__init__.py — no inline list definitions remain"
    - "grep -rn CLOUD_PROVIDERS|LOCAL_PROVIDERS src/ returns exactly one definition site (providers/__init__.py)"
  artifacts:
    - path: "src/eq_chatbot_core/cli.py"
      provides: "Consumer of canonical constants — imports, does not redefine"
      contains: "from eq_chatbot_core.providers import"
    - path: "src/eq_chatbot_core/server/app.py"
      provides: "Consumer of canonical constants — imports, does not redefine"
      contains: "from eq_chatbot_core.providers import"
  key_links:
    - from: "src/eq_chatbot_core/providers/__init__.py"
      to: "src/eq_chatbot_core/cli.py"
      via: "import CLOUD_PROVIDERS, LOCAL_PROVIDERS"
      pattern: "from eq_chatbot_core\\.providers import.*CLOUD_PROVIDERS"
    - from: "src/eq_chatbot_core/providers/__init__.py"
      to: "src/eq_chatbot_core/server/app.py"
      via: "import CLOUD_PROVIDERS, LOCAL_PROVIDERS"
      pattern: "from eq_chatbot_core\\.providers import.*CLOUD_PROVIDERS"
---

<objective>
Replace hardcoded CLOUD_PROVIDERS / LOCAL_PROVIDERS inline list definitions in cli.py and server/app.py with imports from providers/__init__.py. After this plan, grep finds zero duplicate list definitions anywhere in src/.

Purpose: Eliminate the anti-pattern established in Phase 0 goal. Both consumers become thin importers of the canonical constants.
Output: cli.py and server/app.py are modified; providers/__init__.py is untouched (already done in plan 00-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-codebase-cleanup/00-01-SUMMARY.md

<interfaces>
<!-- After plan 00-01, providers/__init__.py exports: -->
<!--   CLOUD_PROVIDERS: list[str] = ["openai","anthropic","langdock","openrouter","mammouth","azure","vertex"] -->
<!--   LOCAL_PROVIDERS: list[str] = ["local","lm_studio","lmstudio","ollama"] -->

<!-- cli.py current state (read before editing): -->
<!-- Line 32: CLOUD_PROVIDERS = ["openai", "anthropic", "langdock", "openrouter", "mammouth", "azure", "vertex"] -->
<!-- Line 33: LOCAL_PROVIDERS = ["local", "lm_studio", "ollama"] -->
<!-- Line 34: ALL_PROVIDERS = CLOUD_PROVIDERS + LOCAL_PROVIDERS -->
<!-- cli.py already has: from eq_chatbot_core.version import __version__ (line 17) -->
<!-- cli.py uses LOCAL_PROVIDERS at lines 91, 196, 351 (in-check: provider.lower() in LOCAL_PROVIDERS) -->
<!-- cli.py uses ALL_PROVIDERS as click.Choice(ALL_PROVIDERS, ...) in test-provider, list-models, chat commands -->

<!-- server/app.py current state (read before editing): -->
<!-- Lines 48-56: _CLOUD_PROVIDERS: list[str] = [...] (private underscore name) -->
<!-- Line 57: _LOCAL_PROVIDERS: list[str] = ["local", "lm_studio", "lmstudio", "ollama"] -->
<!-- Line 81: ProviderInfo(cloud=list(_CLOUD_PROVIDERS), local=list(_LOCAL_PROVIDERS)) -->
<!-- server/app.py already imports from eq_chatbot_core.providers (lines 26-33) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded lists in cli.py with imports from providers</name>
  <files>src/eq_chatbot_core/cli.py</files>
  <read_first>
    - src/eq_chatbot_core/cli.py (full file — read before any edit)
    - src/eq_chatbot_core/providers/__init__.py (source of truth for canonical constants)
  </read_first>
  <action>
Make two changes to cli.py:

1. ADD import line. Insert directly after `from eq_chatbot_core.version import __version__` (currently the last import, line 17):

    from eq_chatbot_core.providers import CLOUD_PROVIDERS, LOCAL_PROVIDERS

2. REMOVE the three inline definitions. Delete these exact lines (currently lines 31-34, including the preceding blank line and comment):

    # Available providers for CLI choices
    CLOUD_PROVIDERS = ["openai", "anthropic", "langdock", "openrouter", "mammouth", "azure", "vertex"]
    LOCAL_PROVIDERS = ["local", "lm_studio", "ollama"]
    ALL_PROVIDERS = CLOUD_PROVIDERS + LOCAL_PROVIDERS

3. ADD ALL_PROVIDERS derivation. After the import line added in step 1, insert:

    ALL_PROVIDERS = CLOUD_PROVIDERS + LOCAL_PROVIDERS

Placement: All three lines (import + ALL_PROVIDERS) should sit in the import section at the top of the file, before the @click.group() decorator. The existing usages of LOCAL_PROVIDERS (lines 91, 196, 351) and ALL_PROVIDERS (click.Choice arguments) require NO changes — the names remain the same, only their definition site moves.

Do NOT change any function bodies, click option definitions, or command logic.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && python -c "from eq_chatbot_core.cli import CLOUD_PROVIDERS, LOCAL_PROVIDERS, ALL_PROVIDERS; assert 'openai' in CLOUD_PROVIDERS; assert 'local' in LOCAL_PROVIDERS; assert len(ALL_PROVIDERS) == len(CLOUD_PROVIDERS) + len(LOCAL_PROVIDERS); print('cli OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^CLOUD_PROVIDERS\|^LOCAL_PROVIDERS" src/eq_chatbot_core/cli.py` returns 0 lines (no inline definitions)
    - `grep -n "from eq_chatbot_core.providers import.*CLOUD_PROVIDERS" src/eq_chatbot_core/cli.py` returns 1 line
    - `python -c "from eq_chatbot_core.cli import CLOUD_PROVIDERS, LOCAL_PROVIDERS, ALL_PROVIDERS"` exits 0
    - The import of CLOUD_PROVIDERS, LOCAL_PROVIDERS appears in the import block, not in a function body
  </acceptance_criteria>
  <done>cli.py imports CLOUD_PROVIDERS and LOCAL_PROVIDERS from providers/__init__.py; no inline list literals for these constants remain in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded private lists in server/app.py with imports from providers</name>
  <files>src/eq_chatbot_core/server/app.py</files>
  <read_first>
    - src/eq_chatbot_core/server/app.py (full file — read before any edit)
    - src/eq_chatbot_core/providers/__init__.py (source of truth for canonical constants)
  </read_first>
  <action>
Make two changes to server/app.py:

1. EXTEND the existing providers import. The file already imports from eq_chatbot_core.providers (lines 26-33). Add CLOUD_PROVIDERS and LOCAL_PROVIDERS to that existing import block:

    from eq_chatbot_core.providers import (
        AuthenticationError,
        ContextLengthError,
        CLOUD_PROVIDERS,
        LOCAL_PROVIDERS,
        OverloadedError,
        ProviderError,
        RateLimitError,
        get_provider,
    )

2. REMOVE the private hardcoded list block. Delete lines 44-57 (the comment block plus _CLOUD_PROVIDERS and _LOCAL_PROVIDERS definitions):

    # Hardcoded provider catalog. Mirrors the dispatch table in
    # eq_chatbot_core.providers.__init__.get_provider() — keep in sync when adding
    # providers there.
    _CLOUD_PROVIDERS: list[str] = [
        "openai",
        ...
        "vertex",
    ]
    _LOCAL_PROVIDERS: list[str] = ["local", "lm_studio", "lmstudio", "ollama"]

3. UPDATE the one usage site. Line 81 currently reads:
        return ProviderInfo(cloud=list(_CLOUD_PROVIDERS), local=list(_LOCAL_PROVIDERS))

   Change to (drop the private underscore prefix):
        return ProviderInfo(cloud=list(CLOUD_PROVIDERS), local=list(LOCAL_PROVIDERS))

No other code in server/app.py references _CLOUD_PROVIDERS or _LOCAL_PROVIDERS. Do not modify endpoints, middleware, or any other logic.

Behavioral note: The canonical LOCAL_PROVIDERS now includes "lmstudio" (previously missing from cli.py's list). The server/app.py private list already included "lmstudio", so no behavior change occurs for the server. cli.py gains "lmstudio" silently — it was already handled by get_provider()'s local_aliases and is correct.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && python -c "from eq_chatbot_core.server.app import create_app; print('server/app.py import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_CLOUD_PROVIDERS\|_LOCAL_PROVIDERS" src/eq_chatbot_core/server/app.py` returns 0 lines
    - `grep -n "from eq_chatbot_core.providers import" src/eq_chatbot_core/server/app.py | grep "CLOUD_PROVIDERS"` returns 1 line
    - `python -c "from eq_chatbot_core.server.app import create_app"` exits 0
    - `grep -rn "CLOUD_PROVIDERS\|LOCAL_PROVIDERS" src/ | grep -v "providers/__init__"` returns lines only showing import statements, zero list-literal definitions (lines containing " = [")
  </acceptance_criteria>
  <done>server/app.py imports CLOUD_PROVIDERS and LOCAL_PROVIDERS from providers/__init__.py; private _CLOUD_PROVIDERS and _LOCAL_PROVIDERS inline lists are removed; usage in /providers endpoint updated to use public names.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan removes private inline lists and wires consumers to the canonical import. The /providers endpoint still returns the same provider names — no new inputs, no new data flows.

## STRIDE Threat Register

No new threats — refactor moves existing constants behind a single import; no new attack surface, no new data flows, no new inputs. ASVS L1 unchanged.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-02 | Tampering | server/app.py /providers endpoint | accept | Provider name list sourced from library constant; no secret data; endpoint is auth-guarded by BearerTokenMiddleware |
</threat_model>

<verification>
After plan execution, run the full grep gate:

```bash
cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core
grep -rn "CLOUD_PROVIDERS\|LOCAL_PROVIDERS" src/
```

Expected: All output lines are either:
- The single definition in `src/eq_chatbot_core/providers/__init__.py`
- Import lines (`from eq_chatbot_core.providers import ... CLOUD_PROVIDERS`) in cli.py and server/app.py
- Usage lines (`in CLOUD_PROVIDERS`, `in LOCAL_PROVIDERS`) in cli.py function bodies

Zero lines should contain ` = [` outside of providers/__init__.py.
</verification>

<success_criteria>
1. `grep -rn "CLOUD_PROVIDERS\|LOCAL_PROVIDERS" src/` shows exactly one definition site (providers/__init__.py) and import/usage lines in cli.py and server/app.py — no inline list literals in consumers.
2. `python -c "from eq_chatbot_core.providers import CLOUD_PROVIDERS, LOCAL_PROVIDERS"` exits 0.
3. `python -c "from eq_chatbot_core.cli import ALL_PROVIDERS"` exits 0.
4. `python -c "from eq_chatbot_core.server.app import create_app"` exits 0.
</success_criteria>

<output>
Create `.planning/phases/00-codebase-cleanup/00-02-SUMMARY.md` when done.
</output>
