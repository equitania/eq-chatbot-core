---
phase: 00-codebase-cleanup
plan: 03
type: execute
wave: 3
depends_on:
  - 00-01
  - 00-02
files_modified: []
autonomous: true
requirements:
  - CLN-04

must_haves:
  truths:
    - "All four phase success criteria are verified by automated checks"
    - "Unit tests pass without modification — zero behavior change from the refactor"
    - "grep gate confirms single definition site for both constants"
  artifacts:
    - path: "src/eq_chatbot_core/providers/__init__.py"
      provides: "Single definition site confirmed by grep"
      contains: "CLOUD_PROVIDERS"
  key_links:
    - from: "grep output"
      to: "providers/__init__.py definition"
      via: "zero inline list literals in consumers"
      pattern: "CLOUD_PROVIDERS.*=.*\\["
---

<objective>
Verify all four Phase 0 success criteria are satisfied using automated checks. No files are modified — this is a read-only verification plan.

Purpose: Provide an explicit, automated gate confirming the refactor is complete and no tests were broken. The executor runs each check command and reports results.
Output: All four check commands exit 0 / return expected output. SUMMARY records pass/fail per criterion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/00-codebase-cleanup/00-01-SUMMARY.md
@.planning/phases/00-codebase-cleanup/00-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run grep gate — single definition site, zero inline duplicates</name>
  <files></files>
  <read_first>
    - src/eq_chatbot_core/providers/__init__.py (confirm definition lines)
  </read_first>
  <action>
Run the following grep commands and capture their output:

Command A — Definition count:
    grep -rn "CLOUD_PROVIDERS\|LOCAL_PROVIDERS" src/eq_chatbot_core/providers/__init__.py

Expected: exactly 2 definition lines (one for each constant), plus however many __all__ entries.

Command B — Consumer scan for inline list literals:
    grep -rn "CLOUD_PROVIDERS\|LOCAL_PROVIDERS" src/ | grep " = \["

Expected: exactly 1 match — the definition line in providers/__init__.py. Zero matches from cli.py or server/app.py.

Command C — Import presence in consumers:
    grep -rn "from eq_chatbot_core.providers import" src/eq_chatbot_core/cli.py src/eq_chatbot_core/server/app.py | grep "CLOUD_PROVIDERS"

Expected: at least 1 line per file (both consumers import the constants).

If any command returns unexpected results, document the deviation in the SUMMARY without modifying files. A deviation here means plans 00-01 or 00-02 did not complete correctly — do not attempt to fix inline; report back.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && grep -rn "CLOUD_PROVIDERS\|LOCAL_PROVIDERS" src/ | grep " = \[" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "CLOUD_PROVIDERS\|LOCAL_PROVIDERS" src/ | grep " = \[" | wc -l` outputs `1` (only providers/__init__.py definition)
    - `grep -rn "CLOUD_PROVIDERS\|LOCAL_PROVIDERS" src/ | grep "cli.py" | grep " = \["` returns empty (no inline definition in cli.py)
    - `grep -rn "_CLOUD_PROVIDERS\|_LOCAL_PROVIDERS" src/eq_chatbot_core/server/app.py` returns empty (private vars gone)
  </acceptance_criteria>
  <done>Grep gate confirms: single definition site in providers/__init__.py; zero inline list literals in cli.py or server/app.py.</done>
</task>

<task type="auto">
  <name>Task 2: Run unit test suite — confirm zero behavior change</name>
  <files></files>
  <read_first>
    - src/eq_chatbot_core/providers/__init__.py (source of truth; no tests import from it directly beyond what the factory already tests)
  </read_first>
  <action>
Activate the project virtual environment and run the unit test suite:

    cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core
    source .venv/bin/activate
    pytest tests/unit/ -v

Do NOT modify any test files. Do NOT add, skip, or xfail any tests. If any test fails, document the failure verbatim in the SUMMARY and mark this task FAILED. A test failure here indicates an unintended behavior change from the refactor.

Expected outcome: all tests pass, exit code 0.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && source .venv/bin/activate && pytest tests/unit/ -v --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/unit/ -v` exits with code 0
    - No test failures, no test errors
    - Test count is unchanged from pre-refactor baseline (no tests were deleted or added by this phase)
  </acceptance_criteria>
  <done>All unit tests pass with exit code 0. Zero behavior change confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No changes to any files in this plan. Verification-only.

## STRIDE Threat Register

No new threats — read-only verification plan; no new attack surface, no new data flows, no new inputs. ASVS L1 unchanged.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-03 | — | (verification only) | accept | No production code modified in this plan |
</threat_model>

<verification>
This plan IS the verification. All four Phase 0 success criteria map to the two tasks:

| Success Criterion | Verified by |
|-------------------|-------------|
| 1. Single definition site in providers/__init__.py | Task 1 grep gate |
| 2. server/app.py imports constants (no hardcoded list) | Task 1 Command B + C |
| 3. cli.py imports constants (no hardcoded list) | Task 1 Command B + C |
| 4. All tests pass without modification | Task 2 pytest run |
</verification>

<success_criteria>
All four Phase 0 success criteria confirmed:
1. `grep -rn "CLOUD_PROVIDERS\|LOCAL_PROVIDERS" src/ | grep " = \["` returns exactly 1 line (providers/__init__.py)
2. server/app.py: zero `_CLOUD_PROVIDERS`/`_LOCAL_PROVIDERS` private definitions; import present
3. cli.py: zero `CLOUD_PROVIDERS = [` / `LOCAL_PROVIDERS = [` inline definitions; import present
4. `pytest tests/unit/ -v` exits 0 with all tests passing
</success_criteria>

<output>
Create `.planning/phases/00-codebase-cleanup/00-03-SUMMARY.md` when done.
</output>
