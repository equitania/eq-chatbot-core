---
phase: 00-codebase-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eq_chatbot_core/providers/__init__.py
autonomous: true
requirements:
  - CLN-01

must_haves:
  truths:
    - "CLOUD_PROVIDERS and LOCAL_PROVIDERS are defined exactly once in providers/__init__.py"
    - "Both constants are present in __all__ and importable from eq_chatbot_core.providers"
  artifacts:
    - path: "src/eq_chatbot_core/providers/__init__.py"
      provides: "Canonical CLOUD_PROVIDERS and LOCAL_PROVIDERS constants"
      contains: "CLOUD_PROVIDERS"
  key_links:
    - from: "src/eq_chatbot_core/providers/__init__.py"
      to: "src/eq_chatbot_core/cli.py"
      via: "from eq_chatbot_core.providers import CLOUD_PROVIDERS, LOCAL_PROVIDERS"
      pattern: "CLOUD_PROVIDERS.*=.*\\["
    - from: "src/eq_chatbot_core/providers/__init__.py"
      to: "src/eq_chatbot_core/server/app.py"
      via: "from eq_chatbot_core.providers import CLOUD_PROVIDERS, LOCAL_PROVIDERS"
      pattern: "LOCAL_PROVIDERS.*=.*\\["
---

<objective>
Define CLOUD_PROVIDERS and LOCAL_PROVIDERS as authoritative module-level constants in providers/__init__.py and export them via __all__. This is the single-source-of-truth that waves 2 consumers will import.

Purpose: Establish the canonical constants before removing the hardcoded duplicates in cli.py and server/app.py (wave 2). Without these exports, wave 2 tasks have nothing to import.
Output: providers/__init__.py gains two exported list constants; no other file is modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current exports in src/eq_chatbot_core/providers/__init__.py (read before editing) -->
<!-- __all__ currently contains: get_provider, BaseLLMProvider, LLMResponse, StreamChunk,
     ModelInfo, ProviderError, RateLimitError, AuthenticationError, ContextLengthError,
     OverloadedError, AzureProvider, VertexProvider, LocalLLMProvider, MammouthProvider,
     OpenRouterProvider -->

<!-- Canonical provider values (sourced from get_provider() dispatch table, lines 97-106): -->
<!-- Cloud: "openai", "anthropic", "langdock", "openrouter", "mammouth", "azure", "vertex" -->
<!-- Local aliases (lines 109-113): "lm_studio", "lmstudio", "ollama" plus canonical "local" -->
<!-- Note: server/app.py _LOCAL_PROVIDERS includes "lmstudio"; cli.py LOCAL_PROVIDERS omits it.
     The canonical source of truth is get_provider()'s local_aliases dict, which contains
     "lm_studio", "lmstudio", and "ollama". The new LOCAL_PROVIDERS must include all three
     aliases plus "local". -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CLOUD_PROVIDERS and LOCAL_PROVIDERS constants to providers/__init__.py</name>
  <files>src/eq_chatbot_core/providers/__init__.py</files>
  <read_first>
    - src/eq_chatbot_core/providers/__init__.py (full file — read before any edit)
  </read_first>
  <action>
Insert two module-level list constants immediately after the module docstring, before the `from typing import ...` import line. Position: between the closing triple-quote of the docstring and the `from typing` line.

Constant definitions to insert (exact names, exact values):

    CLOUD_PROVIDERS: list[str] = ["openai", "anthropic", "langdock", "openrouter", "mammouth", "azure", "vertex"]
    LOCAL_PROVIDERS: list[str] = ["local", "lm_studio", "lmstudio", "ollama"]

Values are derived from the `providers` dict (lines 97-106) and `local_aliases` dict (lines 109-113) inside `get_provider()`. The canonical LOCAL_PROVIDERS includes "lmstudio" (present in local_aliases and server/app.py but missing from cli.py's current inline list — cli.py's omission is the bug being fixed).

After inserting the constants, add both names to the `__all__` list at the bottom of the file:
    "CLOUD_PROVIDERS",
    "LOCAL_PROVIDERS",

Do NOT modify get_provider(), the imports, or any other part of the file. Do NOT remove the existing dispatch table inside get_provider() — that table drives runtime dispatch and is separate from the exported constants.
  </action>
  <verify>
    <automated>python -c "from eq_chatbot_core.providers import CLOUD_PROVIDERS, LOCAL_PROVIDERS; assert 'openai' in CLOUD_PROVIDERS; assert 'lmstudio' in LOCAL_PROVIDERS; print('OK')" && grep -c "^CLOUD_PROVIDERS" /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/src/eq_chatbot_core/providers/__init__.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^CLOUD_PROVIDERS" src/eq_chatbot_core/providers/__init__.py` returns exactly 1 line
    - `grep -n "^LOCAL_PROVIDERS" src/eq_chatbot_core/providers/__init__.py` returns exactly 1 line
    - `python -c "from eq_chatbot_core.providers import CLOUD_PROVIDERS, LOCAL_PROVIDERS"` exits 0
    - `grep "CLOUD_PROVIDERS\|LOCAL_PROVIDERS" src/eq_chatbot_core/providers/__init__.py | grep "__all__"` confirms both names appear in __all__
  </acceptance_criteria>
  <done>providers/__init__.py exports CLOUD_PROVIDERS = ["openai","anthropic","langdock","openrouter","mammouth","azure","vertex"] and LOCAL_PROVIDERS = ["local","lm_studio","lmstudio","ollama"]; both in __all__; no other file changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan adds two string-list constants to an existing module.

## STRIDE Threat Register

No new threats — refactor moves existing constants behind a single import; no new attack surface, no new data flows, no new inputs. ASVS L1 unchanged.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Tampering | providers/__init__.py | accept | String list of provider names contains no secrets; tampering has no privilege-escalation surface |
</threat_model>

<verification>
After plan execution:

```bash
cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core
grep -n "^CLOUD_PROVIDERS\|^LOCAL_PROVIDERS" src/eq_chatbot_core/providers/__init__.py
python -c "from eq_chatbot_core.providers import CLOUD_PROVIDERS, LOCAL_PROVIDERS; print(CLOUD_PROVIDERS); print(LOCAL_PROVIDERS)"
```

Expected: exactly two definition lines, correct list values printed.
</verification>

<success_criteria>
providers/__init__.py contains exactly one definition of CLOUD_PROVIDERS and one definition of LOCAL_PROVIDERS, both exported via __all__, importable without error, containing the canonical provider names derived from get_provider()'s dispatch table.
</success_criteria>

<output>
Create `.planning/phases/00-codebase-cleanup/00-01-SUMMARY.md` when done.
</output>
