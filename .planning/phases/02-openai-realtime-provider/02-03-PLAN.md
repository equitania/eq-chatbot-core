---
phase: 02-openai-realtime-provider
plan: "03"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/eq_chatbot_core/realtime/factory.py
  - src/eq_chatbot_core/realtime/__init__.py
  - tests/integration/test_realtime_openai_live.py
autonomous: true
requirements:
  - PROV-01
  - PROV-02
  - QUAL-03
must_haves:
  truths:
    - "get_realtime_provider('openai', api_key='x') returns an OpenAIRealtimeClient instance"
    - "OpenAIRealtimeClient, OpenAIRealtimeConfig, OPENAI_REALTIME_CAPABILITIES are importable from eq_chatbot_core.realtime"
    - "Factory import stays deferred so factory.py is importable without [realtime] extra"
    - "Integration test exists, is skipped when OPENAI_API_KEY is absent, and covers SESSION_READY + PCM16 chunk + clean close"
  artifacts:
    - path: src/eq_chatbot_core/realtime/factory.py
      provides: "'openai' registered in build_default_realtime_provider_registry() with deferred import"
    - path: src/eq_chatbot_core/realtime/__init__.py
      provides: "OpenAIRealtimeClient, OpenAIRealtimeConfig, OPENAI_REALTIME_CAPABILITIES re-exported in try/except ImportError guard"
    - path: tests/integration/test_realtime_openai_live.py
      provides: "Live integration test gated on OPENAI_API_KEY env var; pytestmark = pytest.mark.integration"
  key_links:
    - from: get_realtime_provider("openai")
      to: _build_openai_provider
      via: "factory_fn lambda in RealtimeProviderDefinition"
      pattern: "_build_openai_provider"
    - from: realtime/__init__.py
      to: realtime/providers/openai.py
      via: "try/except ImportError block; only executed when [realtime] installed"
      pattern: "OpenAIRealtimeClient"
---

<objective>
Wire the OpenAI provider into the factory and public API, then create the live integration test.

Purpose: After Plan 02-01 creates openai.py, this plan (a) extends factory.py to register "openai" with a deferred import builder so get_realtime_provider("openai", api_key=...) works; (b) extends realtime/__init__.py to re-export the three public symbols inside a try/except ImportError guard; (c) creates the integration test that, when OPENAI_API_KEY is set, connects to the real OpenAI Realtime API and verifies SESSION_READY → PCM16 chunk → clean disconnect (SC-3 / QUAL-03 OpenAI portion).

Output: Two modified files (factory.py, __init__.py) and one new test file (tests/integration/test_realtime_openai_live.py). No changes to openai.py.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/ROADMAP.md
@/Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/02-openai-realtime-provider/02-RESEARCH.md
@/Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/02-openai-realtime-provider/02-PATTERNS.md

<interfaces>
<!-- Current Phase 1 factory.py structure — append to build_default_realtime_provider_registry() -->

From src/eq_chatbot_core/realtime/factory.py:
  def build_default_realtime_provider_registry() -> RealtimeProviderRegistry:
      """..."""
      from eq_chatbot_core.realtime.mock import MockRealtimeProvider  # deferred — stdlib-only
      registry = RealtimeProviderRegistry()
      registry.register(RealtimeProviderDefinition(
          name="mock",
          factory_fn=lambda **kwargs: MockRealtimeProvider(),
          description="Queue-backed in-process mock provider for consumer test suites.",
      ))
      return registry
  # NOTE: add "openai" block AFTER mock block, then add _build_openai_provider as a module-level helper

<!-- Current Phase 1 realtime/__init__.py structure — add guarded re-exports -->

From src/eq_chatbot_core/realtime/__init__.py:
  REALTIME_PROVIDERS: list[str] = ["openai", "gemini_live", "nova_sonic", "mock"]
  # Already includes "openai" — no change needed to the list
  def get_realtime_provider(name: str, **kwargs: Any) -> Any:
      ...  # import guard fires inside here
  from eq_chatbot_core.realtime.abc import ...    # always-importable
  from eq_chatbot_core.realtime.contracts import ... # always-importable
  from eq_chatbot_core.realtime.mock import MockRealtimeProvider  # always-importable
  __all__ = ["get_realtime_provider", "REALTIME_PROVIDERS", ...]
  # ADD: try/except ImportError block re-exporting OpenAIRealtimeClient, OpenAIRealtimeConfig,
  #      OPENAI_REALTIME_CAPABILITIES — only importable when [realtime] installed

<!-- From 02-01-PLAN.md produced file -->

From src/eq_chatbot_core/realtime/providers/openai.py:
  class OpenAIRealtimeClient
  class OpenAIRealtimeConfig
  OPENAI_REALTIME_CAPABILITIES: RealtimeProviderCapabilities
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register "openai" in factory.py with deferred import builder</name>
  <files>src/eq_chatbot_core/realtime/factory.py</files>
  <read_first>
    - src/eq_chatbot_core/realtime/factory.py (full current file — understand exact build_default_realtime_provider_registry structure and mock registration block)
    - /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/02-openai-realtime-provider/02-PATTERNS.md §factory.py (registry extension pattern + _build_openai_provider deferred import helper)
    - src/eq_chatbot_core/realtime/providers/openai.py (confirm class/config names are exactly OpenAIRealtimeClient and OpenAIRealtimeConfig)
  </read_first>
  <behavior>
    - Add a "openai" RealtimeProviderDefinition block inside build_default_realtime_provider_registry() AFTER the mock block: name="openai", factory_fn=lambda **kwargs: _build_openai_provider(**kwargs), description="OpenAI Realtime API — GPT speech-to-speech, server VAD, tool calling."
    - Add _build_openai_provider as a module-level private function (below build_default_realtime_provider_registry): deferred-imports OpenAIRealtimeClient and OpenAIRealtimeConfig from eq_chatbot_core.realtime.providers.openai (with # noqa: PLC0415); pops api_key from kwargs; constructs OpenAIRealtimeConfig(api_key=api_key, **kwargs); returns OpenAIRealtimeClient(config)
    - The _DEFAULT_REGISTRY singleton is already thread-safe — do NOT reset it; the new registration happens inside build_default_realtime_provider_registry() which is only called on first use
    - ruff check + mypy pass on factory.py after the change
  </behavior>
  <action>Edit src/eq_chatbot_core/realtime/factory.py. In build_default_realtime_provider_registry(), after the mock registry.register() call, add the openai RealtimeProviderDefinition block per PATTERNS.md §factory.py. Add _build_openai_provider(** kwargs) helper function below the function, using deferred import with noqa: PLC0415 comment. Add _build_openai_provider to __all__. Run ruff check --fix and mypy to verify clean.</action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && python -c "
from eq_chatbot_core.realtime.factory import build_default_realtime_provider_registry
registry = build_default_realtime_provider_registry()
assert 'openai' in registry.registered_names(), f'openai missing; got: {registry.registered_names()}'
# Confirm factory resolves to correct type
from eq_chatbot_core.realtime.providers.openai import OpenAIRealtimeClient
client = registry.get('openai').factory_fn(api_key='test-key')
assert isinstance(client, OpenAIRealtimeClient), f'Wrong type: {type(client)}'
print('Task 1 OK')
"</automated>
  </verify>
  <done>"openai" appears in registry.registered_names(); registry.get("openai").factory_fn(api_key="test-key") returns an OpenAIRealtimeClient instance; ruff and mypy pass on factory.py.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-export OpenAI symbols from realtime/__init__.py + verify get_realtime_provider("openai") end-to-end</name>
  <files>src/eq_chatbot_core/realtime/__init__.py</files>
  <read_first>
    - src/eq_chatbot_core/realtime/__init__.py (full current file — note existing __all__, REALTIME_PROVIDERS, and the try/except ImportError pattern absence)
    - /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/02-openai-realtime-provider/02-PATTERNS.md §realtime/__init__.py (extension pattern with try/except ImportError guard)
    - /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/01-contracts-foundation/01-05-SUMMARY.md (key decision: import guard at call time, not module import time — do not import websockets at module level)
  </read_first>
  <behavior>
    - Add a try/except ImportError block AFTER the existing always-importable imports (after MockRealtimeProvider import, before __all__): imports OpenAIRealtimeClient, OpenAIRealtimeConfig, OPENAI_REALTIME_CAPABILITIES from eq_chatbot_core.realtime.providers.openai (with # noqa: E402); on ImportError sets all three to None (so they are defined in module namespace even without [realtime])
    - Add "OpenAIRealtimeClient", "OpenAIRealtimeConfig", "OPENAI_REALTIME_CAPABILITIES" to __all__
    - REALTIME_PROVIDERS already contains "openai" — do NOT modify the list
    - After the change: `from eq_chatbot_core.realtime import OpenAIRealtimeClient` works when [realtime] is installed; when [realtime] is NOT installed, OpenAIRealtimeClient is None (same pattern as existing optional exports)
    - ruff check + mypy pass on __init__.py after the change
  </behavior>
  <action>Edit src/eq_chatbot_core/realtime/__init__.py. After the MockRealtimeProvider import line and before __all__, add a try/except ImportError block that imports OpenAIRealtimeClient, OpenAIRealtimeConfig, OPENAI_REALTIME_CAPABILITIES from eq_chatbot_core.realtime.providers.openai with noqa: E402 comment (because these are after module-level code). Set all three to None in the except block. Add the three names to __all__. Run ruff check --fix and mypy. Then verify end-to-end: get_realtime_provider("openai", api_key="x") returns an OpenAIRealtimeClient without error.</action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && python -c "
from eq_chatbot_core.realtime import get_realtime_provider, OpenAIRealtimeClient, OpenAIRealtimeConfig, OPENAI_REALTIME_CAPABILITIES
# Direct import works
assert OpenAIRealtimeClient is not None, 'OpenAIRealtimeClient is None — [realtime] extra not installed?'
# Factory resolution works end-to-end
client = get_realtime_provider('openai', api_key='test-key')
assert isinstance(client, OpenAIRealtimeClient), f'Wrong type: {type(client)}'
# Config direct import
cfg = OpenAIRealtimeConfig(api_key='x')
assert cfg.model == 'gpt-realtime'
print('Task 2 OK')
" && ruff check src/eq_chatbot_core/realtime/__init__.py && mypy src/eq_chatbot_core/realtime/__init__.py</automated>
  </verify>
  <done>from eq_chatbot_core.realtime import OpenAIRealtimeClient resolves to the class; get_realtime_provider("openai", api_key="x") returns an OpenAIRealtimeClient; ruff and mypy pass on __init__.py; pytest tests/unit/ -q passes (no regressions from import changes).</done>
</task>

<task type="auto">
  <name>Task 3: Create integration test tests/integration/test_realtime_openai_live.py</name>
  <files>tests/integration/test_realtime_openai_live.py</files>
  <read_first>
    - tests/integration/ directory listing (confirm existing test_openai_live.py structure and skip pattern used for other providers)
    - tests/integration/test_openai_live.py (check existing live test patterns — skip decorator, pytestmark, fixture style)
    - /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/02-openai-realtime-provider/02-PATTERNS.md §tests/integration/test_realtime_openai_live.py (exact skip pattern, async context manager usage)
    - /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/02-openai-realtime-provider/02-RESEARCH.md §Integration Test Pattern (SC-3 exact flow: SESSION_READY → PCM16 → close)
  </read_first>
  <action>Create tests/integration/test_realtime_openai_live.py. Set pytestmark = pytest.mark.integration. Write one async test function test_openai_realtime_session_ready_and_pcm_chunk decorated with @pytest.mark.skipif(not os.getenv("OPENAI_API_KEY"), reason="OPENAI_API_KEY not set — skipping live integration test") and @pytest.mark.asyncio. The test: creates OpenAIRealtimeConfig(api_key=os.environ["OPENAI_API_KEY"], include_turn_detection=False); uses async with OpenAIRealtimeClient(config) as client; iterates iter_normalized_events() and breaks on the first event after asserting its type == NormalizedRealtimeEventTypes.SESSION_READY; then calls await client.append_client_audio(b"\x00\x00" * 2400) (100ms silence at 24kHz mono PCM16); the async with block exit triggers close(). Never print or log the api_key in any assertion message. Run pytest tests/integration/test_realtime_openai_live.py --collect-only to confirm collection without error (test will be skipped or collected depending on env).</action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && pytest tests/integration/test_realtime_openai_live.py --collect-only -q 2>&1 | grep -E "(test_openai_realtime|error|ERROR)" && echo "Collection OK"</automated>
  </verify>
  <done>tests/integration/test_realtime_openai_live.py exists and pytest --collect-only shows test_openai_realtime_session_ready_and_pcm_chunk collected without collection error; the api_key never appears in any assertion failure message (never format "api_key={os.environ['OPENAI_API_KEY']}" in assert); ruff and mypy pass on the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| integration test → OpenAI wss:// | Real API key travels over TLS; must never appear in test output/assertions |
| factory → provider instantiation | Deferred import must not fail when [realtime] not installed; factory.py must stay importable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02W-01 | Information Disclosure | Integration test assertion messages | mitigate | Never include os.environ["OPENAI_API_KEY"] in assert message strings; assertion failure messages must only contain event types and status codes |
| T-02W-02 | Information Disclosure | Integration test skip reason | accept | Skip reason contains "OPENAI_API_KEY not set" — no key value, safe to log |
| T-02W-03 | Spoofing | Factory resolves wrong provider | mitigate | Task 1 verify step confirms isinstance check; deferred import ensures OpenAIRealtimeClient is returned, not MockRealtimeProvider |
| T-02W-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; all deps already declared |
</threat_model>

<verification>
## Phase 2 Plan 03 Verification

After all three tasks complete:

1. `python -c "from eq_chatbot_core.realtime import get_realtime_provider, OpenAIRealtimeClient; client = get_realtime_provider('openai', api_key='x'); print(type(client))"` — prints OpenAIRealtimeClient
2. `python -c "from eq_chatbot_core.realtime.factory import build_default_realtime_provider_registry; r = build_default_realtime_provider_registry(); print(r.registered_names())"` — shows ["mock", "openai"]
3. `pytest tests/integration/test_realtime_openai_live.py --collect-only -q` — collects the test without error
4. `pytest tests/unit/ -q` — full unit suite passes (no regressions)
5. `ruff check src/eq_chatbot_core/realtime/factory.py src/eq_chatbot_core/realtime/__init__.py tests/integration/test_realtime_openai_live.py` — exits 0
6. `mypy src/eq_chatbot_core/realtime/factory.py src/eq_chatbot_core/realtime/__init__.py` — exits 0
</verification>

<success_criteria>
- get_realtime_provider("openai", api_key="x") returns an OpenAIRealtimeClient without error
- from eq_chatbot_core.realtime import OpenAIRealtimeClient, OpenAIRealtimeConfig, OPENAI_REALTIME_CAPABILITIES all resolve when [realtime] is installed
- "openai" appears in build_default_realtime_provider_registry().registered_names()
- tests/integration/test_realtime_openai_live.py is collectable by pytest; test is skipped when OPENAI_API_KEY is absent
- Full unit suite tests/unit/ continues to pass (no regressions from factory/init changes)
- ruff and mypy pass on all modified files
</success_criteria>

<output>
Create .planning/phases/02-openai-realtime-provider/02-03-SUMMARY.md when done
</output>
