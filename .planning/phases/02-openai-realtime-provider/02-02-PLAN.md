---
phase: 02-openai-realtime-provider
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - tests/unit/realtime/test_realtime_openai.py
autonomous: true
requirements:
  - PROV-01
  - PROV-02
  - PROV-03
  - PROV-04
  - QUAL-01
must_haves:
  truths:
    - "All PROV-01..04 behaviors are tested with no real network calls"
    - "PITFALL-28 VAD session payload is tested: turn_detection present when True, absent when False"
    - "TOOL_CALL_COMPLETED payload shape is asserted: item.call_id, item.name, item.arguments accessible"
    - "All NormalizedRealtimeEventTypes mappings are covered by at least one test"
    - "OpenAIRealtimeConfig field defaults are asserted with exact string equality"
  artifacts:
    - path: tests/unit/realtime/test_realtime_openai.py
      provides: "OpenAI unit test suite covering PROV-01..04 and QUAL-01 OpenAI portion"
      min_lines: 150
  key_links:
    - from: test_realtime_openai.py
      to: conftest.py
      via: "mock_websockets_module autouse fixture (session-scoped); mock_ws_instance fixture (function-scoped)"
      pattern: "mock_ws_instance"
    - from: TestIterNormalizedEvents
      to: _to_normalized_runtime_event
      via: "direct method call on client instance (no WS needed)"
      pattern: "client._to_normalized_runtime_event"
---

<objective>
Create tests/unit/realtime/test_realtime_openai.py — the full unit test suite for OpenAIRealtimeClient.

Purpose: Verify every PROV-01..04 behavior and the QUAL-01 OpenAI portion without any real network calls, using the Phase 1 AsyncMock websockets fixture. Tests must be precise enough that a future port bug (wrong event type string, wrong payload shape, VAD logic inverted) is caught immediately.

Output: A single test file with 8+ test classes, 20+ test functions covering config defaults, capabilities, connect lifecycle, VAD payload logic, event normalization for all 11 event types, tool call payload shape, and close lifecycle. No new conftest.py needed — the Phase 1 conftest covers mocking.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/ROADMAP.md
@/Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/02-openai-realtime-provider/02-RESEARCH.md
@/Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/02-openai-realtime-provider/02-PATTERNS.md
@/Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/02-openai-realtime-provider/02-VALIDATION.md

<interfaces>
<!-- Phase 1 conftest fixtures — reuse, do NOT duplicate. -->

From tests/unit/realtime/conftest.py:
  mock_websockets_module   — session-scoped, autouse=True
    installs MagicMock for websockets module before any import in the session.
    Restores sys.modules after session. All tests in tests/unit/realtime/ receive this automatically.

  mock_ws_instance         — function-scoped, explicit (request it by name in test signature)
    Returns a fresh AsyncMock object with:
      .closed = False
      .recv = AsyncMock()     (returns JSON strings from queue if configured)
      .send = AsyncMock()
      .close = AsyncMock()
    Usage: async def test_foo(mock_ws_instance): ...

<!-- From 02-01-PLAN.md produced file -->
From src/eq_chatbot_core/realtime/providers/openai.py:
  class OpenAIRealtimeConfig(frozen dataclass, slots=True):
      api_key: str
      model: str = "gpt-realtime"
      voice: str = "ash"
      instructions: str | None = None
      include_turn_detection: bool = True

  OPENAI_REALTIME_CAPABILITIES: RealtimeProviderCapabilities
      .server_vad = True
      .manual_turn_commit_required = False
      .tool_calling = True
      .tool_result_submission_mode = "conversation_item"
      .voice_selection = True
      .interruption_cancel = True
      .streaming_audio_input = True
      .streaming_audio_output = True
      .startup_validation = True

  class OpenAIRealtimeClient(BaseRealtimeWebsocketClient, RealtimeProvider):
      def __init__(self, config: OpenAIRealtimeConfig, *, trace_events: bool = False)
      async def _on_connected(self) -> None          # calls initialize_session()
      async def _on_message(self, raw: str) -> None  # no-op
      def _connection_error_endpoint(self) -> str    # model-only URL
      def _build_session_update_event(self, *, instructions, voice, tools) -> dict
      @staticmethod def _normalize_tools(tools) -> list | None
      def normalize_event(self, event: dict) -> dict
      def normalize_audio_event_type(self, event_type: str) -> str
      def _to_normalized_runtime_event(self, event: dict) -> NormalizedRealtimeEvent
      async def initialize_session(self, *, instructions=None, voice=None, tools=None)
      async def update_session(self, payload: dict)
      async def append_client_audio(self, pcm16_audio: bytes)
      async def commit_client_turn(self)
      async def create_response(self)
      async def cancel_response(self, *, response_id: str | None = None)
      async def register_tools(self, tools: list)
      async def submit_tool_result(self, *, call_id: str, output: str)
      async def iter_normalized_events(self) -> AsyncIterator[NormalizedRealtimeEvent]

From src/eq_chatbot_core/realtime/contracts.py:
  class NormalizedRealtimeEventTypes (string constants):
      SESSION_READY, RESPONSE_AUDIO_DELTA, RESPONSE_AUDIO_DONE, RESPONSE_DONE,
      INPUT_SPEECH_STARTED, INPUT_SPEECH_STOPPED, RESPONSE_CREATED,
      INPUT_AUDIO_COMMITTED, ERROR, TOOL_CALL_COMPLETED, TOOL_CALL_STARTED, UNHANDLED
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test_realtime_openai.py — config, capabilities, VAD payload, error cases</name>
  <files>tests/unit/realtime/test_realtime_openai.py</files>
  <read_first>
    - tests/unit/realtime/conftest.py (full file — understand autouse fixture, mock_ws_instance shape, sys.modules restoration)
    - src/eq_chatbot_core/realtime/providers/openai.py (complete file from Plan 02-01 — know exact method names, field names, payload shapes)
    - /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/02-openai-realtime-provider/02-PATTERNS.md §Test file header, §Test class structure pattern
    - /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/02-openai-realtime-provider/02-RESEARCH.md §Validation Architecture (requirement-to-test mapping table)
  </read_first>
  <behavior>
    - File header imports: pytest, unittest.mock (AsyncMock, MagicMock, patch); then from eq_chatbot_core.realtime.providers.openai import OpenAIRealtimeClient, OpenAIRealtimeConfig, OPENAI_REALTIME_CAPABILITIES, RealtimeAudioEventNames; from eq_chatbot_core.realtime.contracts import NormalizedRealtimeEventTypes, RealtimeAdapterContract
    - Mark all async tests with @pytest.mark.asyncio
    - TestOpenAIRealtimeConfig: test_frozen (verify dataclasses.FrozenInstanceError on mutation attempt), test_default_model_is_gpt_realtime (exact string "gpt-realtime"), test_default_voice_is_ash (exact string "ash"), test_default_include_turn_detection_true (True), test_custom_fields (verify all fields set via constructor)
    - TestCapabilities: test_server_vad_true, test_manual_turn_commit_required_false, test_tool_result_mode_conversation_item (exact string), test_all_streaming_flags (input/output both True)
    - TestConstructorValidation: test_empty_api_key_raises_value_error (empty string), test_whitespace_api_key_raises_value_error (spaces only), test_empty_model_raises_value_error (verify D-03)
    - TestVADSessionPayload: test_turn_detection_present_when_include_true — call client._build_session_update_event(instructions=None, voice=None, tools=None) and assert turn_detection key IS in session["audio"]["input"]; test_turn_detection_absent_when_include_false — same call with include_turn_detection=False config, assert turn_detection key is NOT in session["audio"]["input"]
    - TestConnectionErrorEndpoint: test_does_not_contain_api_key — assert "secret" not in client._connection_error_endpoint() where api_key="secret"; test_contains_model — assert "gpt-realtime" in endpoint
    - TestNormalizeTools: test_tool_definition_conversion (ToolDefinition instance → correct dict shape with type/name/description/parameters/strict keys), test_dict_passthrough (raw dict unchanged), test_none_returns_none, test_empty_returns_none
    - ruff + mypy clean on this first part of the file
  </behavior>
  <action>Create tests/unit/realtime/test_realtime_openai.py with the file header (imports) and the first five test classes: TestOpenAIRealtimeConfig, TestCapabilities, TestConstructorValidation, TestVADSessionPayload, TestConnectionErrorEndpoint, TestNormalizeTools. These classes test static/sync behavior — no WS mock needed. Run pytest tests/unit/realtime/test_realtime_openai.py -q after writing to confirm all pass.</action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && pytest tests/unit/realtime/test_realtime_openai.py -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>pytest reports all tests in the file passing; ruff and mypy pass on the file; at minimum TestOpenAIRealtimeConfig, TestCapabilities, TestConstructorValidation, TestVADSessionPayload, TestConnectionErrorEndpoint, TestNormalizeTools are present with at least 2 tests each.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add event normalization tests + connect/close lifecycle tests</name>
  <files>tests/unit/realtime/test_realtime_openai.py</files>
  <read_first>
    - tests/unit/realtime/test_realtime_openai.py (current state from Task 1)
    - tests/unit/realtime/conftest.py (mock_ws_instance fixture — how to use it in async tests)
    - src/eq_chatbot_core/realtime/providers/openai.py (_to_normalized_runtime_event routing table, TOOL_CALL_COMPLETED payload shape)
    - /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core/.planning/phases/02-openai-realtime-provider/02-PATTERNS.md §Event normalization pipeline (exact payload shape for TOOL_CALL_COMPLETED case — item sub-dict with id, call_id, name, arguments)
  </read_first>
  <behavior>
    - TestIterNormalizedEvents: tests call client._to_normalized_runtime_event(event_dict) directly (no WS needed):
      - test_session_created_maps_to_session_ready: input {"type": "session.created"} → output type == NormalizedRealtimeEventTypes.SESSION_READY
      - test_session_updated_maps_to_session_ready: input {"type": "session.updated"} → SESSION_READY
      - test_response_audio_delta_normalized: input {"type": "response.audio.delta"} → RESPONSE_AUDIO_DELTA (verifies Stage 1 alias + Stage 2 routing)
      - test_response_audio_done_normalized: input {"type": "response.audio.done"} → RESPONSE_AUDIO_DONE
      - test_response_done_normalized: input {"type": "response.done"} → RESPONSE_DONE
      - test_input_speech_started_normalized: input {"type": "input_audio_buffer.speech_started"} → INPUT_SPEECH_STARTED
      - test_input_speech_stopped_normalized: same for stopped → INPUT_SPEECH_STOPPED
      - test_response_created_normalized: {"type": "response.created"} → RESPONSE_CREATED
      - test_input_audio_committed_normalized: {"type": "input_audio_buffer.committed"} → INPUT_AUDIO_COMMITTED
      - test_error_normalized: {"type": "error"} → ERROR
      - test_unknown_event_normalized: {"type": "some.unknown.event"} → UNHANDLED
    - TestToolCallNormalization (Pitfall 5 — custom payload shape is load-bearing):
      - test_function_call_arguments_done_has_item_sub_dict: feed {"type": "response.function_call_arguments.done", "item_id": "item-1", "call_id": "call-1", "name": "my_fn", "arguments": '{}', "response_id": "resp-1"} to _to_normalized_runtime_event; assert result["type"] == NormalizedRealtimeEventTypes.TOOL_CALL_COMPLETED; assert result["payload"]["item"]["call_id"] == "call-1"; assert result["payload"]["item"]["name"] == "my_fn"; assert result["payload"]["item"]["arguments"] == '{}'
      - test_function_call_top_level_fields: assert result["payload"]["call_id"] == "call-1" and result["payload"]["name"] == "my_fn" (top-level fields also present)
    - TestConnectLifecycle (uses mock_ws_instance — confirms _on_connected fires initialize_session):
      - test_on_connected_calls_initialize_session: patch initialize_session on instance; call await client._on_connected(); assert initialize_session was awaited once
      - test_implements_contract: isinstance(client, RealtimeAdapterContract) is True
    - TestCloseLifecycle:
      - test_close_does_not_raise_when_not_connected: client.close() should not raise (already disconnected state)
    - ruff + mypy pass on complete file
  </behavior>
  <action>Append TestIterNormalizedEvents, TestToolCallNormalization, TestConnectLifecycle, and TestCloseLifecycle to tests/unit/realtime/test_realtime_openai.py. For TestIterNormalizedEvents use _to_normalized_runtime_event directly — no WS mock needed. For TestToolCallNormalization assert both item sub-dict AND top-level fields are present in payload (Pitfall 5 from RESEARCH.md). For TestConnectLifecycle use patch.object or a manual patch to confirm initialize_session is called from _on_connected. Run pytest tests/unit/realtime/test_realtime_openai.py -q to confirm all tests pass.</action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && pytest tests/unit/realtime/test_realtime_openai.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>pytest shows all tests passing (no failures, no errors); TestIterNormalizedEvents covers at least 11 event type mappings; TestToolCallNormalization asserts both item.call_id and top-level call_id; ruff and mypy pass on the complete file; full suite pytest tests/unit/ -q also passes (no regression in Phase 1 tests).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test assertions → api_key strings | Integration test must not hardcode or print real API keys; unit tests use fake "test-key" only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02T-01 | Information Disclosure | Unit test assertions | mitigate | All unit tests use fake api_key="test-key" or "secret" — never a real key; no key appears in output |
| T-02T-02 | Information Disclosure | pytest -v output | accept | Fake keys in test fixtures are low-value; real key only appears in integration test (Plan 02-03) where it is gated by env var |
| T-02T-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; websockets already audited in Phase 1 |
</threat_model>

<verification>
## Phase 2 Plan 02 Verification

After both tasks complete:

1. `pytest tests/unit/realtime/test_realtime_openai.py -v` — all tests pass, no errors
2. `pytest tests/unit/realtime/ -q` — full Phase 1+2 unit suite passes (no regressions)
3. `ruff check tests/unit/realtime/test_realtime_openai.py` — exits 0
4. `mypy tests/unit/realtime/test_realtime_openai.py` — exits 0
5. Count: at minimum 20 test functions present (`pytest --collect-only tests/unit/realtime/test_realtime_openai.py | grep "test_" | wc -l`)
</verification>

<success_criteria>
- tests/unit/realtime/test_realtime_openai.py exists with at least 20 test functions
- TestVADSessionPayload: include_turn_detection=True → turn_detection in payload; False → absent
- TestToolCallNormalization: response.function_call_arguments.done → TOOL_CALL_COMPLETED with payload["item"]["call_id"] accessible
- TestIterNormalizedEvents: all 11+ wire event types have a test mapping to their NormalizedRealtimeEventTypes constant
- TestConstructorValidation: empty api_key raises ValueError
- All tests pass with pytest tests/unit/realtime/test_realtime_openai.py -q
- Full unit suite tests/unit/ passes without regression
</success_criteria>

<output>
Create .planning/phases/02-openai-realtime-provider/02-02-SUMMARY.md when done
</output>
