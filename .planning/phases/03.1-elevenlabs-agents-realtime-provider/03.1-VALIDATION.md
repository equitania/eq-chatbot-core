---
phase: 03.1
slug: elevenlabs-agents-realtime-provider
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-25
---

# Phase 03.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 9.x (asyncio auto mode) |
| **Config file** | pyproject.toml |
| **Quick run command** | `.venv/bin/python -m pytest tests/unit/realtime/ -q` |
| **Full suite command** | `.venv/bin/python -m pytest tests/unit/ -q` |
| **Estimated runtime** | ~5 seconds (unit), integration skipped without EU creds |

---

## Sampling Rate

- **After every task commit:** Run `.venv/bin/python -m pytest tests/unit/realtime/ -q`
- **After every plan wave:** Run `.venv/bin/python -m pytest tests/unit/ -q`
- **Before `/gsd:verify-work`:** Full unit suite must be green; `ruff check src/` clean
- **Max feedback latency:** ~10 seconds

---

## Per-Task Verification Map

> Planner fills this from PLAN.md tasks. Anchor every behavior-adding task to an automated pytest command.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 03.1-01-01 | 01 | 1 | PROV-FUT-03 | T-03.1-01 | xi-api-key never in logs / URL | unit | `.venv/bin/python -m pytest tests/unit/test_realtime_elevenlabs.py -q` | ❌ W0 | ⬜ pending |
| 03.1-0X-0X | 0X | 2 | QUAL-01 | — | event mapping + no-op turn methods | unit | `.venv/bin/python -m pytest tests/unit/test_realtime_elevenlabs.py -q` | ❌ W0 | ⬜ pending |
| 03.1-0X-0X | 0X | 2 | QUAL-03 | — | connect → SESSION_READY → clean disconnect (EU) | integration | `.venv/bin/python -m pytest tests/integration/test_realtime_elevenlabs.py -q` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/unit/test_realtime_elevenlabs.py` — mocked-WebSocket unit suite for PROV-FUT-03 / QUAL-01
- [ ] `tests/integration/test_realtime_elevenlabs.py` — skip-safe EU integration test for QUAL-03 (mirror tests/integration/test_realtime_gemini_live.py)
- [ ] No framework install needed — pytest + websockets already present (`[realtime]` extra)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live EU convai roundtrip (real signed-URL + EU agent) | QUAL-03 | Requires ElevenLabs Enterprise EU credentials + pre-created agent | `uv pip install -e ".[realtime]"`; set `ELEVENLABS_EU_API_KEY` + `ELEVENLABS_AGENT_ID`; run integration test |
| `conversation_config_override` JSON nesting (prompt/voice) | PROV-FUT-03 | LOW-confidence nesting per research A1 — verify on first live call | Inspect outgoing `conversation_initiation_client_data` against live agent |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
