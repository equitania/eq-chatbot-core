---
phase: "03.1"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/eq_chatbot_core/realtime/providers/elevenlabs.py
autonomous: true
requirements:
  - PROV-FUT-03
must_haves:
  truths:
    - "isinstance(ElevenLabsRealtimeClient(config), RealtimeAdapterContract) is True"
    - "ElevenLabsRealtimeConfig.session_sample_rate defaults to 16_000"
    - "ValueError raised on empty api_key before any network I/O"
    - "ValueError raised on empty agent_id before any network I/O"
    - "commit_client_turn() and create_response() are no-ops — no send_json call"
    - "append_client_audio sends {user_audio_chunk: base64} with NO 'type' key"
    - "submit_tool_result sends type=client_tool_result with tool_call_id (NOT call_id)"
    - "_connection_error_endpoint() never contains the xi-api-key"
    - "ELEVENLABS_REALTIME_CAPABILITIES.tool_result_submission_mode == 'elevenlabs_native'"
    - "ELEVENLABS_REALTIME_CAPABILITIES.manual_turn_commit_required == False"
    - "ELEVENLABS_REALTIME_CAPABILITIES.session_sample_rate == 16_000"
  artifacts:
    - path: src/eq_chatbot_core/realtime/providers/elevenlabs.py
      provides: "ElevenLabsRealtimeClient, ElevenLabsRealtimeConfig, ELEVENLABS_REALTIME_CAPABILITIES"
      exports:
        - ElevenLabsRealtimeClient
        - ElevenLabsRealtimeConfig
        - ELEVENLABS_REALTIME_CAPABILITIES
  key_links:
    - from: ElevenLabsRealtimeClient
      to: BaseRealtimeWebsocketClient
      via: "class ElevenLabsRealtimeClient(BaseRealtimeWebsocketClient, RealtimeProvider)"
      pattern: "class ElevenLabsRealtimeClient"
    - from: append_client_audio
      to: ElevenLabs WebSocket wire
      via: "send_json with top-level user_audio_chunk key"
      pattern: "user_audio_chunk"
    - from: submit_tool_result
      to: ElevenLabs WebSocket wire
      via: "client_tool_result message with tool_call_id field"
      pattern: "client_tool_result"
---

<objective>
Implement ElevenLabsRealtimeClient — a lean WebSocket transport adapter for ElevenLabs Agents
(speech-to-speech) that satisfies RealtimeAdapterContract, mirroring the OpenAI provider structure.

Purpose: Delivers the core of PROV-FUT-03 (ElevenLabs promoted from future to first-class GDPR
provider). Factory wiring and tests land in Plans 02 and 03.
Output: src/eq_chatbot_core/realtime/providers/elevenlabs.py — complete, importable, all 11
contract methods implemented, structurally conforming to RealtimeAdapterContract without network calls.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-CONTEXT.md
@.planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-RESEARCH.md
@.planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase 2026-05-25. No exploration needed. -->

From src/eq_chatbot_core/realtime/contracts.py:
  INPUT_AUDIO_SAMPLE_RATE: int = 24_000   # Do NOT import — ElevenLabs uses 16 kHz fixed locally
  NormalizedRealtimeEventTypes.SESSION_READY          = "session.ready"
  NormalizedRealtimeEventTypes.RESPONSE_AUDIO_DELTA   = "response.audio.delta"
  NormalizedRealtimeEventTypes.RESPONSE_DONE          = "response.done"
  NormalizedRealtimeEventTypes.INPUT_AUDIO_COMMITTED  = "input.audio.committed"
  NormalizedRealtimeEventTypes.INPUT_SPEECH_STARTED   = "input.speech.started"
  NormalizedRealtimeEventTypes.TOOL_CALL_COMPLETED    = "tool.call.completed"
  NormalizedRealtimeEventTypes.UNHANDLED              = "provider.event.unhandled"

  RealtimeProviderCapabilities fields (frozen dataclass, slots=True):
    streaming_audio_input, streaming_audio_output, server_vad,
    manual_turn_commit_required, tool_calling, tool_result_submission_mode: str,
    voice_selection, interruption_cancel, startup_validation=True,
    session_sample_rate: int = 24_000

  RealtimeAdapterContract (Protocol, runtime_checkable) — 11 methods:
    connect, close, initialize_session(*, instructions, voice, tools),
    update_session(payload), append_client_audio(pcm16_audio),
    commit_client_turn, create_response, cancel_response(*, response_id),
    register_tools(tools), submit_tool_result(*, call_id, output),
    iter_normalized_events() -> AsyncIterator

From src/eq_chatbot_core/realtime/websocket_client.py (BaseRealtimeWebsocketClient):
  __init__(url: str, headers: dict[str, str])
  connect()  — inherited, do NOT override
  close()    — inherited, do NOT override
  send_json(payload: dict) -> None   — call directly in contract methods
  iter_events() -> AsyncIterator[dict]  — wrap in iter_normalized_events
  Abstract (MUST override): _on_connected(), _on_message(raw: str), _connection_error_endpoint() -> str

From src/eq_chatbot_core/realtime/abc.py (RealtimeProvider ABC):
  @property capabilities(self) -> RealtimeProviderCapabilities   — abstract
  @property provider_name(self) -> str                          — abstract

From src/eq_chatbot_core/providers/base.py:
  ToolDefinition: name: str, description: str, parameters: dict, strict: bool = False
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ElevenLabsRealtimeConfig + ELEVENLABS_REALTIME_CAPABILITIES</name>
  <files>src/eq_chatbot_core/realtime/providers/elevenlabs.py</files>
  <read_first>
    - src/eq_chatbot_core/realtime/providers/openai.py (lines 1-80: docstring, imports, config dataclass, capabilities constant — structural template)
    - src/eq_chatbot_core/realtime/contracts.py (RealtimeProviderCapabilities field names, session_sample_rate)
    - .planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-PATTERNS.md (sections "Imports pattern", "Config dataclass pattern", "Capabilities constant pattern")
  </read_first>
  <behavior>
    - ElevenLabsRealtimeConfig(api_key="k", agent_id="a").session_sample_rate == 16_000
    - ElevenLabsRealtimeConfig(api_key="k", agent_id="a").base_url == "wss://api.elevenlabs.io"
    - ElevenLabsRealtimeConfig(api_key="k", agent_id="a").voice is None
    - ElevenLabsRealtimeConfig(api_key="k", agent_id="a").instructions is None
    - dataclasses.asdict raises on mutate attempt (frozen=True)
    - ELEVENLABS_REALTIME_CAPABILITIES.manual_turn_commit_required == False
    - ELEVENLABS_REALTIME_CAPABILITIES.tool_result_submission_mode == "elevenlabs_native"
    - ELEVENLABS_REALTIME_CAPABILITIES.session_sample_rate == 16_000
    - ELEVENLABS_REALTIME_CAPABILITIES.server_vad == True
  </behavior>
  <action>
    Create src/eq_chatbot_core/realtime/providers/elevenlabs.py.

    Module docstring: mirror openai.py lines 1-11, substitute "ElevenLabs Agents Conversational AI" for "OpenAI Realtime API". List three key differences from OpenAI: (1) server-side VAD — commit_client_turn() and create_response() are no-ops; (2) audio sample rate is 16 kHz, not 24 kHz; (3) user_audio_chunk is a bare top-level key, not a type-discriminated message.

    Imports: from __future__ import annotations; import base64; import logging; from collections.abc import AsyncIterator; from dataclasses import dataclass; from typing import Any. Then: from eq_chatbot_core.providers.base import ToolDefinition; from eq_chatbot_core.realtime.abc import RealtimeProvider; from eq_chatbot_core.realtime.contracts import NormalizedRealtimeEventFull, NormalizedRealtimeEventTypes, RealtimeProviderCapabilities; from eq_chatbot_core.realtime.websocket_client import BaseRealtimeWebsocketClient. Do NOT import INPUT_AUDIO_SAMPLE_RATE. Define _logger = logging.getLogger(__name__).

    ElevenLabsRealtimeConfig: @dataclass(frozen=True, slots=True). Fields in order: api_key: str (docstring: "xi-api-key for REST signed-URL call; NEVER placed in the WebSocket URL"), agent_id: str (docstring: "pre-existing agent in ElevenLabs dashboard"), base_url: str = "wss://api.elevenlabs.io" (comment: EU Enterprise override: wss://api.eu.residency.elevenlabs.io), voice: str | None = None, instructions: str | None = None (comment: maps to conversation_config_override.agent.prompt.prompt), session_sample_rate: int = 16_000 (comment: PROV-FUT-03 — 16 kHz; differs from 24 kHz default).

    ELEVENLABS_REALTIME_CAPABILITIES: RealtimeProviderCapabilities with EXACT VALUES: streaming_audio_input=True, streaming_audio_output=True, server_vad=True, manual_turn_commit_required=False, tool_calling=True, tool_result_submission_mode="elevenlabs_native", voice_selection=True, interruption_cancel=True, startup_validation=True, session_sample_rate=16_000.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && .venv/bin/python -c "
import dataclasses
from eq_chatbot_core.realtime.providers.elevenlabs import ElevenLabsRealtimeConfig, ELEVENLABS_REALTIME_CAPABILITIES
c = ElevenLabsRealtimeConfig(api_key='k', agent_id='a')
assert c.session_sample_rate == 16_000
assert c.base_url == 'wss://api.elevenlabs.io'
assert c.voice is None and c.instructions is None
assert ELEVENLABS_REALTIME_CAPABILITIES.manual_turn_commit_required == False
assert ELEVENLABS_REALTIME_CAPABILITIES.tool_result_submission_mode == 'elevenlabs_native'
assert ELEVENLABS_REALTIME_CAPABILITIES.session_sample_rate == 16_000
assert ELEVENLABS_REALTIME_CAPABILITIES.server_vad == True
print('Task 1 OK')
"</automated>
  </verify>
  <done>ElevenLabsRealtimeConfig is frozen+slots dataclass with correct defaults. ELEVENLABS_REALTIME_CAPABILITIES has tool_result_submission_mode="elevenlabs_native" and session_sample_rate=16_000. Python import succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ElevenLabsRealtimeClient — all 11 contract methods</name>
  <files>src/eq_chatbot_core/realtime/providers/elevenlabs.py</files>
  <read_first>
    - src/eq_chatbot_core/realtime/providers/openai.py (lines 83-417: full client class for structural template)
    - src/eq_chatbot_core/realtime/providers/gemini_live.py (lines 139-153: _on_connected no-auto-init pattern; lines 276-277: create_response no-op; lines 159-183: _connection_error_endpoint)
    - .planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-PATTERNS.md (sections "_on_connected pattern", "iter_normalized_events pattern", "_to_normalized_runtime_event dispatch table", "append_client_audio pattern", "submit_tool_result pattern", "cancel_response pattern", "no-op turn control pattern")
    - .planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-RESEARCH.md (sections "Open Point 3: Precise Event Names", "Pattern 1: Audio Input", "Pattern 2: Session Initialization", "Pattern 3: Tool Result", "Pattern 4: Keepalive", "Anti-Patterns to Avoid")
  </read_first>
  <behavior>
    - isinstance(ElevenLabsRealtimeClient(ElevenLabsRealtimeConfig(api_key="k", agent_id="a")), RealtimeAdapterContract) is True
    - ElevenLabsRealtimeClient(ElevenLabsRealtimeConfig(api_key="", agent_id="a")) raises ValueError matching "api_key"
    - ElevenLabsRealtimeClient(ElevenLabsRealtimeConfig(api_key="  ", agent_id="a")) raises ValueError matching "api_key"
    - ElevenLabsRealtimeClient(ElevenLabsRealtimeConfig(api_key="k", agent_id="")) raises ValueError matching "agent_id"
    - _connection_error_endpoint() returns string starting with "wss://" containing agent_id and NOT containing the api_key value
    - _to_normalized_runtime_event({"type": "conversation_initiation_metadata"})["type"] == NormalizedRealtimeEventTypes.SESSION_READY
    - _to_normalized_runtime_event({"type": "audio", "audio_event": {"audio_base_64": "AAEC"}})["type"] == NormalizedRealtimeEventTypes.RESPONSE_AUDIO_DELTA
    - _to_normalized_runtime_event({"type": "agent_response"})["type"] == NormalizedRealtimeEventTypes.RESPONSE_DONE
    - _to_normalized_runtime_event({"type": "agent_response_complete"})["type"] == NormalizedRealtimeEventTypes.RESPONSE_DONE
    - _to_normalized_runtime_event({"type": "user_transcript"})["type"] == NormalizedRealtimeEventTypes.INPUT_AUDIO_COMMITTED
    - _to_normalized_runtime_event({"type": "interruption"})["type"] == NormalizedRealtimeEventTypes.INPUT_SPEECH_STARTED
    - _to_normalized_runtime_event({"type": "ping", "ping_event": {"event_id": 1}}) returns None
    - _to_normalized_runtime_event({"type": "client_tool_call", "client_tool_call": {"tool_call_id": "c1", "tool_name": "fn", "parameters": "{}"}})["type"] == NormalizedRealtimeEventTypes.TOOL_CALL_COMPLETED
    - _to_normalized_runtime_event({"type": "unknown_event"})["type"] == NormalizedRealtimeEventTypes.UNHANDLED
    - append_client_audio(b"\x00\x01") sends payload with key "user_audio_chunk" and without key "type"
    - append_client_audio(b"") does NOT call send_json
    - commit_client_turn() does NOT call send_json (server-side VAD — no-op)
    - create_response() does NOT call send_json (server-side — no-op)
    - cancel_response() does NOT call send_json (interruption is server-side — no-op)
    - submit_tool_result(call_id="c1", output="done") sends payload with type="client_tool_result", key "tool_call_id"="c1", key "result"="done", key "is_error"=False, and does NOT contain key "call_id"
    - provider_name property returns "elevenlabs"
    - capabilities property returns ELEVENLABS_REALTIME_CAPABILITIES
  </behavior>
  <action>
    Add ElevenLabsRealtimeClient class to the file created in Task 1.

    Class declaration: class ElevenLabsRealtimeClient(BaseRealtimeWebsocketClient, RealtimeProvider).

    __init__(self, config: ElevenLabsRealtimeConfig, *, trace_events: bool = False) -> None:
      Fail-fast (per D-03): if not config.api_key.strip() raise ValueError("ElevenLabsRealtimeConfig.api_key must be non-empty."). if not config.agent_id.strip() raise ValueError("ElevenLabsRealtimeConfig.agent_id must be non-empty.").
      Build url: url = f"{config.base_url}/v1/convai/conversation?agent_id={config.agent_id}". Comment: xi-api-key is NOT a WebSocket header — it is only used in the REST signed-URL flow for private agents. Public agents require no auth header. Pass headers={} to base class.
      Call super().__init__(url=url, headers={}). Store self._config = config, self._trace_events = trace_events.

    @property provider_name(self) -> str: return "elevenlabs".
    @property capabilities(self) -> RealtimeProviderCapabilities: return ELEVENLABS_REALTIME_CAPABILITIES.

    Abstract method overrides (all three required):
      _on_connected(self) -> None: Log if trace_events. Do NOT call initialize_session() — ElevenLabs sends conversation_initiation_metadata server-side (Gemini pattern, NOT OpenAI pattern).
      _on_message(self, raw: str) -> None: pass (ABC conformance only — iter_normalized_events flow used in production).
      _connection_error_endpoint(self) -> str: return f"{self._config.base_url}/v1/convai/conversation?agent_id={self._config.agent_id}". Comment: api_key never appears in WebSocket URL — no redaction needed on URL. agent_id is not a secret.

    initialize_session(self, *, instructions=None, voice=None, tools=None) -> None:
      Build payload dict with type="conversation_initiation_client_data" and conversation_config_override={}. If instructions or self._config.instructions, set payload["conversation_config_override"]["agent"] = {"prompt": {"prompt": instructions or self._config.instructions}}. If voice or self._config.voice, set payload["conversation_config_override"]["tts"] = {"voice_id": voice or self._config.voice}. Await send_json(payload).

    update_session(self, payload: dict[str, Any]) -> None: await self.send_json(payload). (Copy verbatim from openai.py.)

    append_client_audio(self, pcm16_audio: bytes) -> None:
      If not pcm16_audio: return.
      CRITICAL: send {"user_audio_chunk": base64.b64encode(pcm16_audio).decode("ascii")} with NO "type" key. Add comment: "CRITICAL: ElevenLabs uses a bare top-level key — NOT a type-discriminated message. Sending {'type': 'user_audio_chunk', ...} is WRONG and will be silently ignored by the server."

    commit_client_turn(self) -> None: pass. Docstring: "No-op — ElevenLabs performs server-side VAD turn-taking; no client commit needed."
    create_response(self) -> None: pass. Docstring: "No-op — ElevenLabs generates responses server-side after VAD turn detection."
    cancel_response(self, *, response_id: str | None = None) -> None: pass. Docstring: "No-op — ElevenLabs handles interruption server-side via VAD."

    register_tools(self, tools: list[Any]) -> None: await self.initialize_session(tools=tools).

    submit_tool_result(self, *, call_id: str, output: str) -> None:
      Send {"type": "client_tool_result", "tool_call_id": call_id, "result": output, "is_error": False}. Comment: "ElevenLabs uses 'client_tool_result' — NOT 'conversation.item.create'. CRITICAL: field name is 'tool_call_id' (NOT 'call_id'); contract uses call_id, wire format uses tool_call_id."

    _to_normalized_runtime_event(self, event: dict[str, Any]) -> NormalizedRealtimeEventFull | None:
      event_type = event.get("type"). If not isinstance(event_type, str): return UNHANDLED with source="missing_type".
      Dispatch table (exact wire event names per RESEARCH.md Open Point 3):
        "conversation_initiation_metadata" → SESSION_READY
        "audio" → RESPONSE_AUDIO_DELTA; extract audio_b64 from event.get("audio_event", {}).get("audio_base_64", "")
        "agent_response" → RESPONSE_DONE. Add TODO comment: agent_response_complete may become canonical RESPONSE_DONE signal once SDK wire dispatch includes it (April 2026 schema addition, not yet in v2.49.1 wire dispatch).
        "agent_response_complete" → RESPONSE_DONE. Comment: "Forward-compatible — not in current SDK wire dispatch (v2.49.1) but safe to handle gracefully."
        "user_transcript" → INPUT_AUDIO_COMMITTED
        "interruption" → INPUT_SPEECH_STARTED (user interrupted agent via server VAD)
        "ping" → return None (sentinel: caller sends pong and skips yield). Do NOT yield this event.
        "client_tool_call" → TOOL_CALL_COMPLETED; extract tool_info = event.get("client_tool_call", {}); payload = {"call_id": tool_info.get("tool_call_id"), "name": tool_info.get("tool_name"), "arguments": tool_info.get("parameters", "{}")}
        "agent_response_correction" → UNHANDLED
        default → UNHANDLED with source=event_type or "unknown"

    iter_normalized_events(self) -> AsyncIterator[NormalizedRealtimeEventFull]:
      async for event in self.iter_events():
        if event.get("type") == "ping": extract event_id from event.get("ping_event", {}).get("event_id"); await self.send_json({"type": "pong", "event_id": event_id}); continue.
        normalized = self._to_normalized_runtime_event(event); if normalized is not None: yield normalized.

    __all__ at module bottom: ["ElevenLabsRealtimeClient", "ElevenLabsRealtimeConfig", "ELEVENLABS_REALTIME_CAPABILITIES"]

    Run ruff check and ruff format (line-length 120) before declaring done.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && .venv/bin/python -c "
from eq_chatbot_core.realtime.contracts import RealtimeAdapterContract, NormalizedRealtimeEventTypes
from eq_chatbot_core.realtime.providers.elevenlabs import ElevenLabsRealtimeClient, ElevenLabsRealtimeConfig, ELEVENLABS_REALTIME_CAPABILITIES

config = ElevenLabsRealtimeConfig(api_key='xi-test', agent_id='agent-abc')
client = ElevenLabsRealtimeClient(config)
assert isinstance(client, RealtimeAdapterContract), 'not RealtimeAdapterContract'
assert client.provider_name == 'elevenlabs'
assert client.capabilities is ELEVENLABS_REALTIME_CAPABILITIES
ep = client._connection_error_endpoint()
assert 'xi-test' not in ep, f'api_key leaked: {ep}'
assert 'agent-abc' in ep, f'agent_id missing: {ep}'

# Event dispatch
r = client._to_normalized_runtime_event({'type': 'conversation_initiation_metadata'})
assert r['type'] == NormalizedRealtimeEventTypes.SESSION_READY
r = client._to_normalized_runtime_event({'type': 'audio', 'audio_event': {'audio_base_64': 'AAEC'}})
assert r['type'] == NormalizedRealtimeEventTypes.RESPONSE_AUDIO_DELTA
r = client._to_normalized_runtime_event({'type': 'agent_response'})
assert r['type'] == NormalizedRealtimeEventTypes.RESPONSE_DONE
r = client._to_normalized_runtime_event({'type': 'ping', 'ping_event': {'event_id': 1}})
assert r is None, f'ping should return None sentinel, got {r}'
r = client._to_normalized_runtime_event({'type': 'unknown_xyz'})
assert r['type'] == NormalizedRealtimeEventTypes.UNHANDLED
print('Task 2 isinstance+dispatch OK')
"</automated>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && .venv/bin/python -m ruff check src/eq_chatbot_core/realtime/providers/elevenlabs.py --quiet</automated>
  </verify>
  <done>
    ElevenLabsRealtimeClient satisfies isinstance check against RealtimeAdapterContract.
    All 11 contract methods implemented. Event dispatch table covers all RESEARCH.md-verified wire event names.
    _connection_error_endpoint does not contain api_key value.
    append_client_audio sends top-level user_audio_chunk key with no type key.
    submit_tool_result uses type=client_tool_result with tool_call_id (not call_id).
    commit_client_turn, create_response, cancel_response are no-ops.
    ruff clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application → ElevenLabs REST | xi-api-key sent as HTTP header to signed-URL endpoint; must not appear in logs |
| application → ElevenLabs WebSocket | No key in URL or headers; agent_id is not a secret |
| caller → ElevenLabsRealtimeClient | api_key and agent_id provided by caller; must fail-fast on empty values before I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | Information Disclosure | _connection_error_endpoint() | mitigate | Return only base_url + agent_id; xi-api-key is NOT in the WebSocket URL (used only in REST call); assert "xi-api-key" not in _connection_error_endpoint() result in unit test |
| T-03.1-02 | Information Disclosure | _logger / trace_events logging | mitigate | Never log self._config.api_key; _on_connected logs only connection metadata (base_url shape, not key value) |
| T-03.1-03 | Spoofing | signed-URL expiry (15-min window) | accept | Adapter does not manage signed-URL lifecycle; consumer is responsible for pre-fetching before connect(); document in README — low risk for library adapter |
| T-03.1-04 | Tampering | user_audio_chunk wire format | mitigate | Unit test asserts sent payload has top-level key "user_audio_chunk" and NO "type" key — prevents silent protocol divergence from OpenAI format |
| T-03.1-05 | Elevation of Privilege | EU key used with global endpoint | accept | EU API keys reject on global endpoint with 403; documented in README GDPR guide; not enforceable at adapter level |
| T-03.1-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase — websockets already in [realtime] extra, audited in Phase 1 |
</threat_model>

<verification>
After both tasks complete:

1. File exists: src/eq_chatbot_core/realtime/providers/elevenlabs.py
2. isinstance check: python -c "from eq_chatbot_core.realtime.contracts import RealtimeAdapterContract; from eq_chatbot_core.realtime.providers.elevenlabs import ElevenLabsRealtimeClient, ElevenLabsRealtimeConfig; assert isinstance(ElevenLabsRealtimeClient(ElevenLabsRealtimeConfig(api_key='k', agent_id='a')), RealtimeAdapterContract)"
3. Key redaction: _connection_error_endpoint() output does not contain the api_key string
4. Wire format: append_client_audio sends payload with "user_audio_chunk" key and without "type" key
5. Tool result: submit_tool_result sends "tool_call_id" field, not "call_id"
6. Capabilities: tool_result_submission_mode == "elevenlabs_native", session_sample_rate == 16_000
7. Ruff clean: .venv/bin/python -m ruff check src/eq_chatbot_core/realtime/providers/elevenlabs.py
</verification>

<success_criteria>
- ElevenLabsRealtimeClient(ElevenLabsRealtimeConfig(api_key="k", agent_id="a")) instantiates without error
- isinstance(client, RealtimeAdapterContract) is True
- ValueError on empty api_key (before network I/O); ValueError on empty agent_id (before network I/O)
- ELEVENLABS_REALTIME_CAPABILITIES.tool_result_submission_mode == "elevenlabs_native" (RESEARCH.md correction honored)
- ELEVENLABS_REALTIME_CAPABILITIES.session_sample_rate == 16_000 (PROV-FUT-03)
- _connection_error_endpoint() contains agent_id and does NOT contain api_key
- Event dispatch: conversation_initiation_metadata → SESSION_READY; audio → RESPONSE_AUDIO_DELTA; agent_response → RESPONSE_DONE; ping → None sentinel; unknown → UNHANDLED
- ruff check passes (line-length 120)
</success_criteria>

<output>
Create .planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-01-SUMMARY.md when done.
</output>
