---
phase: "03.1"
plan: 3
type: execute
wave: 2
depends_on:
  - "03.1-01"
files_modified:
  - src/eq_chatbot_core/realtime/factory.py
  - tests/integration/test_realtime_elevenlabs.py
  - README.md
autonomous: true
requirements:
  - PROV-FUT-03
  - QUAL-03
must_haves:
  truths:
    - "get_realtime_provider('elevenlabs', api_key=..., agent_id=...) returns ElevenLabsRealtimeClient"
    - "'elevenlabs' appears in get_realtime_provider registry alongside openai/gemini_live/nova_sonic"
    - "factory raises ValueError with usage hint when api_key missing"
    - "factory raises ValueError with usage hint when agent_id missing"
    - "factory.py is importable without [realtime] extra installed (deferred import)"
    - "Integration test is skip-safe when ELEVENLABS_API_KEY or ELEVENLABS_AGENT_ID absent"
    - "Integration test connects, receives SESSION_READY, sends 3200-byte silence, disconnects cleanly"
    - "README documents all four EU compliance conditions + voice-cloning caveat (LOCKED content)"
  artifacts:
    - path: src/eq_chatbot_core/realtime/factory.py
      provides: "elevenlabs factory registration + _build_elevenlabs_provider builder"
      contains: "_build_elevenlabs_provider"
    - path: tests/integration/test_realtime_elevenlabs.py
      provides: "Skip-safe QUAL-03 integration test"
      exports: []
    - path: README.md
      provides: "ElevenLabs GDPR setup guide with four conditions + voice-cloning caveat"
      contains: "EU-hosted Custom LLM"
  key_links:
    - from: get_realtime_provider("elevenlabs", ...)
      to: _build_elevenlabs_provider
      via: "factory registry lambda"
      pattern: "_build_elevenlabs_provider"
    - from: _build_elevenlabs_provider
      to: ElevenLabsRealtimeClient
      via: "deferred import inside builder function"
      pattern: "from eq_chatbot_core.realtime.providers.elevenlabs import"
    - from: tests/integration/test_realtime_elevenlabs.py
      to: ElevenLabs API
      via: "real WebSocket connect (skipped when credentials absent)"
      pattern: "ELEVENLABS_API_KEY"
---

<objective>
Wire ElevenLabsRealtimeClient into the factory registry, deliver the skip-safe QUAL-03 integration
test, and write the README GDPR setup guide.

Purpose: Completes PROV-FUT-03 (factory equal-rank registration) and QUAL-03 (integration test with
real API skipped when credentials absent). README content is LOCKED per CONTEXT.md Decision 12.
Output: factory.py updated with elevenlabs registration; tests/integration/test_realtime_elevenlabs.py;
README.md ElevenLabs GDPR section.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-CONTEXT.md
@.planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-RESEARCH.md
@.planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-PATTERNS.md
@.planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-01-SUMMARY.md

<interfaces>
<!-- Extracted from codebase 2026-05-25. No exploration needed. -->

From src/eq_chatbot_core/realtime/factory.py (current structure):
  build_default_realtime_provider_registry() -> RealtimeProviderRegistry
    Registers: mock, openai, gemini_live, nova_sonic
    All builders use lambda **kwargs: _build_xxx_provider(**kwargs) pattern
  _build_openai_provider(**kwargs): validates api_key presence, deferred import, config+client
  _build_gemini_live_provider(**kwargs): validates api_key or access_token per mode, deferred import
  _build_nova_sonic_provider(**kwargs): stdlib-only deferred import, no validation needed
  RealtimeProviderDefinition(name, factory_fn, description="")
  _get_realtime_provider_impl(name, **kwargs) -> Any  (public entry point via __init__)

From src/eq_chatbot_core/realtime/providers/elevenlabs.py (Plan 01 output):
  ElevenLabsRealtimeConfig(api_key: str, agent_id: str, base_url="wss://api.elevenlabs.io", ...)
  ElevenLabsRealtimeClient(config: ElevenLabsRealtimeConfig, *, trace_events=False)

From tests/integration/test_realtime_gemini_live.py (analog for integration test):
  pytest.importorskip("websockets")  — first statement after imports
  pytestmark = pytest.mark.integration
  @pytest.mark.skipif(not os.getenv("KEY1") or not os.getenv("KEY2"), reason="...")
  @pytest.mark.asyncio
  async def test_..._session_ready():
      config = ProviderConfig(key=os.environ["KEY1"], ...)
      async with ProviderClient(config) as client:
          await client.initialize_session()
          async for event in client.iter_normalized_events():
              assert event["type"] == NormalizedRealtimeEventTypes.SESSION_READY
              break
          silence = b"\x00\x00" * N_SAMPLES
          await client.append_client_audio(silence)

ElevenLabs 16 kHz integration test silence: 100ms = 1600 samples × 2 bytes = 3200 bytes
  i.e. silence = b"\x00\x00" * 1600
  (NOT b"\x00\x00" * 2400 — that is Gemini's 24kHz formula)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register elevenlabs in factory.py</name>
  <files>src/eq_chatbot_core/realtime/factory.py</files>
  <read_first>
    - src/eq_chatbot_core/realtime/factory.py (full file — understand all existing registrations + builder functions; lines 37-75 for registry builder, lines 78-135 for existing builders)
    - .planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-PATTERNS.md (section "src/eq_chatbot_core/realtime/factory.py: Step 1 Registration" and "Step 2 Builder function")
  </read_first>
  <action>
    Modify src/eq_chatbot_core/realtime/factory.py.

    Step 1 — Update build_default_realtime_provider_registry() docstring (line ~41):
    Change 'Phase 3 adds "gemini_live" and "nova_sonic".' to
    'Phase 3 adds "gemini_live" and "nova_sonic". Phase 3.1 adds "elevenlabs".'

    Step 2 — Add "elevenlabs" registration AFTER the nova_sonic block (inside build_default_realtime_provider_registry):
    registry.register(
        RealtimeProviderDefinition(
            name="elevenlabs",
            factory_fn=lambda **kwargs: _build_elevenlabs_provider(**kwargs),
            description="ElevenLabs Conversational AI — speech-to-speech, server VAD, GDPR-preferred provider.",
        )
    )

    Step 3 — Add _build_elevenlabs_provider function AFTER _build_nova_sonic_provider:
    def _build_elevenlabs_provider(**kwargs: Any) -> Any:
      Docstring: "Build an ElevenLabsRealtimeClient from keyword arguments. Deferred import keeps factory.py importable without the [realtime] extra installed. D-03 fail-fast: credential validation runs before the deferred import so the caller gets a clear library-native ValueError regardless of [realtime] install state."
      Validate: if not kwargs.get("api_key", "").strip(): raise ValueError('ElevenLabs realtime provider requires an "api_key" keyword argument. Pass it via get_realtime_provider("elevenlabs", api_key="xi-...", agent_id="...").')
      Validate: if not kwargs.get("agent_id", "").strip(): raise ValueError('ElevenLabs realtime provider requires an "agent_id" keyword argument. Pass it via get_realtime_provider("elevenlabs", api_key="xi-...", agent_id="...").')
      Deferred import: from eq_chatbot_core.realtime.providers.elevenlabs import ElevenLabsRealtimeClient, ElevenLabsRealtimeConfig  # noqa: PLC0415
      config = ElevenLabsRealtimeConfig(**kwargs)
      return ElevenLabsRealtimeClient(config)

    Run ruff check and ruff format after editing.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && .venv/bin/python -c "
from eq_chatbot_core.realtime.factory import build_default_realtime_provider_registry
registry = build_default_realtime_provider_registry()
names = registry.registered_names()
assert 'elevenlabs' in names, f'elevenlabs not registered: {names}'
assert 'openai' in names and 'gemini_live' in names and 'nova_sonic' in names
print('Registered providers:', names)

# Fail-fast: missing api_key
import pytest
try:
    registry.get('elevenlabs').factory_fn(agent_id='agent-abc')
    assert False, 'should have raised'
except ValueError as e:
    assert 'api_key' in str(e), str(e)

# Fail-fast: missing agent_id
try:
    registry.get('elevenlabs').factory_fn(api_key='xi-test')
    assert False, 'should have raised'
except ValueError as e:
    assert 'agent_id' in str(e), str(e)

print('Task 1 factory OK')
"</automated>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && .venv/bin/python -m ruff check src/eq_chatbot_core/realtime/factory.py --quiet</automated>
  </verify>
  <done>
    "elevenlabs" appears in build_default_realtime_provider_registry() alongside openai/gemini_live/nova_sonic.
    _build_elevenlabs_provider raises ValueError with "api_key" hint when api_key missing.
    _build_elevenlabs_provider raises ValueError with "agent_id" hint when agent_id missing.
    factory.py ruff clean.
    factory.py is importable without [realtime] extra (deferred import inside builder).
  </done>
</task>

<task type="auto">
  <name>Task 2: Skip-safe integration test + README GDPR section</name>
  <files>
    tests/integration/test_realtime_elevenlabs.py
    README.md
  </files>
  <read_first>
    - tests/integration/test_realtime_gemini_live.py (full file — exact structural template for skip-safe integration test)
    - README.md (find existing realtime provider section to locate insertion point; look for "## Realtime" or "### ElevenLabs" headings)
    - .planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-PATTERNS.md (section "tests/integration/test_realtime_elevenlabs.py" — full template including file header, skipif guard, test body with 3200-byte silence)
    - .planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-PATTERNS.md (section "README / docs GDPR section" — LOCKED content for all four conditions + voice-cloning caveat)
  </read_first>
  <action>
    --- INTEGRATION TEST ---
    Create tests/integration/test_realtime_elevenlabs.py.

    File docstring: describe QUAL-03 scope, required env vars (ELEVENLABS_API_KEY, ELEVENLABS_AGENT_ID), skip behavior, run command.

    Imports: import os; import pytest. Then: pytest.importorskip("websockets"). Then: from eq_chatbot_core.realtime.contracts import NormalizedRealtimeEventTypes; from eq_chatbot_core.realtime.providers.elevenlabs import ElevenLabsRealtimeClient, ElevenLabsRealtimeConfig.

    pytestmark = pytest.mark.integration.

    @pytest.mark.skipif(
        not os.getenv("ELEVENLABS_API_KEY") or not os.getenv("ELEVENLABS_AGENT_ID"),
        reason="ELEVENLABS_API_KEY / ELEVENLABS_AGENT_ID not set — skipping ElevenLabs integration test",
    )
    @pytest.mark.asyncio
    async def test_elevenlabs_session_ready_and_disconnect() -> None:
      Docstring: "QUAL-03: Connect to real ElevenLabs agent, receive SESSION_READY, send 100ms PCM16 silence, disconnect cleanly. No commit_client_turn() — ElevenLabs uses server-side VAD."
      config = ElevenLabsRealtimeConfig(
          api_key=os.environ["ELEVENLABS_API_KEY"],
          agent_id=os.environ["ELEVENLABS_AGENT_ID"],
          base_url=os.getenv("ELEVENLABS_BASE_URL", "wss://api.elevenlabs.io"),
      )
      async with ElevenLabsRealtimeClient(config) as client:
          async for event in client.iter_normalized_events():
              assert event["type"] == NormalizedRealtimeEventTypes.SESSION_READY, (
                  f"Expected SESSION_READY as first event, got: {event['type']}"
              )
              break
          # 100ms silence at 16 kHz mono PCM16 = 1600 samples × 2 bytes = 3200 bytes
          # (16 kHz differs from Gemini/OpenAI 24 kHz — PROV-FUT-03 session_sample_rate=16_000)
          silence = b"\x00\x00" * 1600
          await client.append_client_audio(silence)
          # No commit_client_turn() — ElevenLabs server VAD handles turn detection

    --- README GDPR SECTION ---
    Edit README.md to add the ElevenLabs GDPR section. Find the appropriate location (after existing realtime provider documentation, or create an "### ElevenLabs (Recommended GDPR Provider)" heading under a Realtime Providers section).

    LOCKED content to add (from CONTEXT.md Decision 12 + PATTERNS.md README section):

    ### ElevenLabs (Recommended GDPR Provider)

    ElevenLabs Conversational AI ("elevenlabs") is the recommended provider for EU/GDPR deployments.

    #### Full EU Compliance Checklist

    Four conditions must ALL be met for complete data residency compliance:

    1. **Enterprise plan** — EU data residency is available on the Enterprise plan only. Standard and Creator plans route data through US infrastructure.

    2. **Zero Retention Mode** — Enable Zero Retention Mode in the ElevenLabs Enterprise dashboard and confirm it via the Zero Retention API. Covers TTS, STT, and Conversational AI sessions. Voice cloning models are excluded (see caveat below).

    3. **EU-hosted Custom LLM backend** — ElevenLabs Agents orchestrate an LLM under the hood. For full EU residency, configure a Custom LLM endpoint hosted in the EU (e.g. Azure OpenAI EU region, or a self-hosted model in an EU data centre). Configure this in the ElevenLabs dashboard, not in the adapter.

    4. **EU data-residency endpoint** — Pass the EU base URL as base_url:
       (code block showing get_realtime_provider("elevenlabs", api_key="YOUR_EU_API_KEY", agent_id="YOUR_AGENT_ID", base_url="wss://api.eu.residency.elevenlabs.io"))

       Note: The EU API key is a **separate key** provisioned by ElevenLabs Enterprise support. Your global xi-api-key will return 403 Forbidden on the EU endpoint.

    #### Voice Cloning Caveat

    Voice cloning models are **not eligible for Zero Retention Mode** — cloned voice model data persists in ElevenLabs infrastructure. If your use case requires voice cloning, assess whether that data qualifies as personal data under GDPR before deploying in an EU-regulated context.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && .venv/bin/python -m pytest tests/integration/test_realtime_elevenlabs.py -v --collect-only 2>&1 | grep -E "test_elevenlabs|SKIP|DESELECTED|ERROR"</automated>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && grep -c "EU-hosted Custom LLM" README.md</automated>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && grep -c "voice cloning\|voice cloning\|Voice Cloning\|Voice cloning" README.md</automated>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && grep -c "wss://api.eu.residency.elevenlabs.io" README.md</automated>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && .venv/bin/python -m pytest tests/integration/test_realtime_elevenlabs.py -v 2>&1 | tail -5</automated>
  </verify>
  <done>
    tests/integration/test_realtime_elevenlabs.py exists; pytest --collect-only resolves test_elevenlabs_session_ready_and_disconnect without error.
    Test is automatically skipped when ELEVENLABS_API_KEY / ELEVENLABS_AGENT_ID are absent (confirmed by pytest output showing SKIPPED).
    README.md contains "EU-hosted Custom LLM" (condition 3), "wss://api.eu.residency.elevenlabs.io" (condition 4), and voice-cloning caveat.
    Silence chunk in test uses b"\x00\x00" * 1600 (3200 bytes for 16 kHz, NOT 2400 bytes for 24 kHz).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → _build_elevenlabs_provider | api_key and agent_id from caller kwargs; must validate before deferred import |
| integration test → ElevenLabs API | Real credentials from env vars; test emits no secrets to stdout/logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-03-01 | Information Disclosure | _build_elevenlabs_provider ValueError messages | mitigate | Error message text references only the kwarg name ("api_key") — never the api_key value; factory validation runs before any import or logging |
| T-03.1-03-02 | Information Disclosure | integration test stdout | mitigate | Integration test does not print or log credentials; ElevenLabsRealtimeClient._connection_error_endpoint is already verified not to contain api_key (Plan 01 T-03.1-01) |
| T-03.1-03-03 | Spoofing | EU endpoint documentation accuracy | mitigate | README documents that EU API key is SEPARATE from global key (Pitfall 7 from RESEARCH.md); prevents 403 confusion |
| T-03.1-03-04 | Elevation of Privilege | deferred import without [realtime] installed | accept | Factory raises ImportError if [realtime] extra absent — caller gets readable error; already tested via existing factory import guard tests |
| T-03.1-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase |
</threat_model>

<verification>
After both tasks complete:

1. Factory registration: python -c "from eq_chatbot_core.realtime.factory import build_default_realtime_provider_registry; r = build_default_realtime_provider_registry(); assert 'elevenlabs' in r.registered_names()"
2. Factory fail-fast (missing api_key): factory_fn(agent_id="a") raises ValueError containing "api_key"
3. Factory fail-fast (missing agent_id): factory_fn(api_key="k") raises ValueError containing "agent_id"
4. Integration test collectible: pytest tests/integration/test_realtime_elevenlabs.py --collect-only (no ImportError or collection error)
5. Integration test skips when no creds: unset ELEVENLABS_API_KEY; pytest tests/integration/test_realtime_elevenlabs.py -v shows SKIPPED
6. README content grep: grep -c "EU-hosted Custom LLM" README.md == 1; grep -c "wss://api.eu.residency.elevenlabs.io" README.md >= 1
7. Silence chunk size: grep "1600" tests/integration/test_realtime_elevenlabs.py (must be 1600, not 2400)
8. Full unit suite still green: .venv/bin/python -m pytest tests/unit/realtime/ -q
</verification>

<success_criteria>
- "elevenlabs" in build_default_realtime_provider_registry().registered_names()
- _build_elevenlabs_provider raises ValueError with "api_key" in message when api_key missing
- _build_elevenlabs_provider raises ValueError with "agent_id" in message when agent_id missing
- tests/integration/test_realtime_elevenlabs.py pytest-collectable with no errors
- Test skips automatically when ELEVENLABS_API_KEY or ELEVENLABS_AGENT_ID absent
- Silence chunk in integration test is b"\x00\x00" * 1600 (3200 bytes, 16 kHz) not 2400
- README contains all four EU compliance conditions including "EU-hosted Custom LLM"
- README contains voice-cloning caveat
- README contains "wss://api.eu.residency.elevenlabs.io" as the EU endpoint
- Full unit suite (tests/unit/realtime/) remains green after factory.py modification
</success_criteria>

<output>
Create .planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-03-SUMMARY.md when done.
</output>
