---
phase: "03.1"
plan: 2
type: execute
wave: 2
depends_on:
  - "03.1-01"
files_modified:
  - tests/unit/realtime/test_realtime_elevenlabs.py
autonomous: true
requirements:
  - QUAL-01
must_haves:
  truths:
    - "Unit test suite covers all VALIDATION.md per-task behaviors without real network calls"
    - "xi-api-key never appears in _connection_error_endpoint() return value (unit-tested)"
    - "user_audio_chunk payload has no 'type' key (unit-tested)"
    - "submit_tool_result sends 'tool_call_id' not 'call_id' (unit-tested)"
    - "commit_client_turn and create_response call no send_json (no-op, unit-tested)"
    - "all RESEARCH.md-verified wire event names map to correct NormalizedRealtimeEventTypes constants"
    - "empty api_key and empty agent_id raise ValueError (unit-tested)"
  artifacts:
    - path: tests/unit/realtime/test_realtime_elevenlabs.py
      provides: "Full unit test suite for ElevenLabsRealtimeClient and ElevenLabsRealtimeConfig"
      exports: []
  key_links:
    - from: tests/unit/realtime/test_realtime_elevenlabs.py
      to: tests/unit/realtime/conftest.py
      via: "autouse mock_websockets_module fixture (session-scoped)"
      pattern: "mock_websockets_module"
    - from: tests/unit/realtime/test_realtime_elevenlabs.py
      to: src/eq_chatbot_core/realtime/providers/elevenlabs.py
      via: "from eq_chatbot_core.realtime.providers.elevenlabs import ..."
      pattern: "ElevenLabsRealtimeClient"
---

<objective>
Write the unit test suite for ElevenLabsRealtimeClient — all behaviors from VALIDATION.md verified
without real network calls, using the session-scoped mock_websockets_module conftest fixture.

Purpose: Delivers QUAL-01 (ElevenLabs unit-test portion). Mirrors test_realtime_openai.py structure
exactly. Covers constructor validation, capability assertions, event normalization, wire format
correctness (user_audio_chunk bare key, client_tool_result / tool_call_id field), no-op turn methods,
and key redaction.
Output: tests/unit/realtime/test_realtime_elevenlabs.py — importable, all tests passing, no network calls.
</objective>

<execution_context>
@/Users/picard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-CONTEXT.md
@.planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-PATTERNS.md
@.planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-VALIDATION.md

<interfaces>
<!-- Extracted from codebase 2026-05-25. No exploration needed. -->

From tests/unit/realtime/conftest.py (autouse, session-scoped — fires automatically):
  mock_websockets_module: MagicMock  — installs mock websockets in sys.modules for all tests
    in tests/unit/realtime/. Provider MUST be imported AFTER conftest fixture fires
    (i.e., import inside test body or at module level — pytest loads conftest first).
  mock_ws_instance fixture (function-scoped): fresh AsyncMock WS instance per test.
    mock_ws_instance.recv = AsyncMock(return_value='{"type": "test"}')
    mock_ws_instance.send = AsyncMock()
    mock_ws_instance.close = AsyncMock()

From src/eq_chatbot_core/realtime/providers/elevenlabs.py (Plan 01 output):
  ElevenLabsRealtimeConfig(api_key: str, agent_id: str, base_url="wss://api.elevenlabs.io",
                           voice=None, instructions=None, session_sample_rate=16_000)
  ELEVENLABS_REALTIME_CAPABILITIES.manual_turn_commit_required == False
  ELEVENLABS_REALTIME_CAPABILITIES.tool_result_submission_mode == "elevenlabs_native"
  ELEVENLABS_REALTIME_CAPABILITIES.session_sample_rate == 16_000
  ElevenLabsRealtimeClient.__init__(config, *, trace_events=False)
    raises ValueError("api_key") if api_key empty/whitespace
    raises ValueError("agent_id") if agent_id empty/whitespace
  ElevenLabsRealtimeClient._connection_error_endpoint() -> str  (no api_key in result)
  ElevenLabsRealtimeClient._to_normalized_runtime_event(event: dict) -> NormalizedRealtimeEventFull | None
  ElevenLabsRealtimeClient.append_client_audio(pcm16_audio: bytes) -> Coroutine
  ElevenLabsRealtimeClient.commit_client_turn() -> Coroutine (no-op)
  ElevenLabsRealtimeClient.create_response() -> Coroutine (no-op)
  ElevenLabsRealtimeClient.cancel_response(*, response_id=None) -> Coroutine (no-op)
  ElevenLabsRealtimeClient.submit_tool_result(*, call_id: str, output: str) -> Coroutine

From src/eq_chatbot_core/realtime/contracts.py:
  RealtimeAdapterContract (Protocol, @runtime_checkable)
  NormalizedRealtimeEventTypes: SESSION_READY, RESPONSE_AUDIO_DELTA, RESPONSE_DONE,
    INPUT_AUDIO_COMMITTED, INPUT_SPEECH_STARTED, TOOL_CALL_COMPLETED, UNHANDLED

Primary analog: tests/unit/realtime/test_realtime_openai.py
  — class layout, helper factory _make_client(), AsyncMock patch pattern, event dispatch tests
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write test_realtime_elevenlabs.py — config, capabilities, constructor validation, redaction</name>
  <files>tests/unit/realtime/test_realtime_elevenlabs.py</files>
  <read_first>
    - tests/unit/realtime/test_realtime_openai.py (lines 1-165: module header, imports, _make_client helper, TestOpenAIRealtimeConfig, TestCapabilities, TestConstructorValidation, TestConnectionErrorEndpoint — full structural template)
    - tests/unit/realtime/conftest.py (autouse mock_websockets_module fixture — understand scope and import order requirement)
    - .planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-PATTERNS.md (sections "File header / import pattern", "Helper factory pattern", "Constructor validation test pattern", "Test class layout table")
  </read_first>
  <action>
    Create tests/unit/realtime/test_realtime_elevenlabs.py.

    Module docstring: mirror test_realtime_openai.py lines 1-9. Reference QUAL-01 and the autouse conftest fixture.

    Imports: import dataclasses; import pytest; from unittest.mock import AsyncMock, patch; from eq_chatbot_core.realtime.contracts import NormalizedRealtimeEventTypes, RealtimeAdapterContract; from eq_chatbot_core.realtime.providers.elevenlabs import ELEVENLABS_REALTIME_CAPABILITIES, ElevenLabsRealtimeClient, ElevenLabsRealtimeConfig.

    Module constants: _FAKE_KEY = "xi-fake-api-key"; _FAKE_AGENT = "agent-fake-id".

    Helper: def _make_client() -> ElevenLabsRealtimeClient: config = ElevenLabsRealtimeConfig(api_key=_FAKE_KEY, agent_id=_FAKE_AGENT); return ElevenLabsRealtimeClient(config).

    Class TestElevenLabsRealtimeConfig:
      test_frozen_dataclass: attempt setattr raises FrozenInstanceError (use pytest.raises or try/except AttributeError/FrozenInstanceError).
      test_default_sample_rate: ElevenLabsRealtimeConfig(api_key="k", agent_id="a").session_sample_rate == 16_000.
      test_default_base_url: == "wss://api.elevenlabs.io".
      test_default_voice_none: .voice is None.
      test_default_instructions_none: .instructions is None.
      test_custom_base_url: ElevenLabsRealtimeConfig(api_key="k", agent_id="a", base_url="wss://api.eu.residency.elevenlabs.io").base_url == "wss://api.eu.residency.elevenlabs.io".

    Class TestElevenLabsCapabilities:
      test_no_manual_turn_commit: ELEVENLABS_REALTIME_CAPABILITIES.manual_turn_commit_required == False.
      test_tool_result_mode_elevenlabs_native: ELEVENLABS_REALTIME_CAPABILITIES.tool_result_submission_mode == "elevenlabs_native".
      test_session_sample_rate_16k: ELEVENLABS_REALTIME_CAPABILITIES.session_sample_rate == 16_000.
      test_server_vad_true: ELEVENLABS_REALTIME_CAPABILITIES.server_vad == True.
      test_tool_calling_true: ELEVENLABS_REALTIME_CAPABILITIES.tool_calling == True.

    Class TestConstructorValidation:
      test_empty_api_key_raises: ElevenLabsRealtimeConfig(api_key="", agent_id="a") → ElevenLabsRealtimeClient(...) raises ValueError matching "api_key".
      test_whitespace_api_key_raises: api_key="   " raises ValueError matching "api_key".
      test_empty_agent_id_raises: agent_id="" raises ValueError matching "agent_id".
      test_whitespace_agent_id_raises: agent_id="   " raises ValueError matching "agent_id".
      test_valid_config_does_not_raise: ElevenLabsRealtimeClient(ElevenLabsRealtimeConfig(api_key="k", agent_id="a")) does not raise.

    Class TestConnectionErrorEndpoint:
      test_api_key_not_in_endpoint: _connection_error_endpoint() does not contain _FAKE_KEY.
      test_agent_id_in_endpoint: _connection_error_endpoint() contains _FAKE_AGENT.
      test_endpoint_starts_with_wss: _connection_error_endpoint().startswith("wss://").
      test_no_auth_header_in_url: "xi-api-key" not in _connection_error_endpoint() and "Authorization" not in _connection_error_endpoint().

    Class TestConnectLifecycle:
      test_isinstance_realtime_adapter_contract: isinstance(_make_client(), RealtimeAdapterContract) is True.
      test_provider_name: _make_client().provider_name == "elevenlabs".
      test_capabilities_property: _make_client().capabilities is ELEVENLABS_REALTIME_CAPABILITIES.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && .venv/bin/python -m pytest tests/unit/realtime/test_realtime_elevenlabs.py::TestElevenLabsRealtimeConfig tests/unit/realtime/test_realtime_elevenlabs.py::TestElevenLabsCapabilities tests/unit/realtime/test_realtime_elevenlabs.py::TestConstructorValidation tests/unit/realtime/test_realtime_elevenlabs.py::TestConnectionErrorEndpoint tests/unit/realtime/test_realtime_elevenlabs.py::TestConnectLifecycle -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    TestElevenLabsRealtimeConfig (6 tests), TestElevenLabsCapabilities (5 tests),
    TestConstructorValidation (5 tests), TestConnectionErrorEndpoint (4 tests),
    TestConnectLifecycle (3 tests) all pass. No network calls. No real websockets used.
  </done>
</task>

<task type="auto">
  <name>Task 2: Event mapping, audio format, turn control, tool result tests</name>
  <files>tests/unit/realtime/test_realtime_elevenlabs.py</files>
  <read_first>
    - tests/unit/realtime/test_realtime_openai.py (lines 329-520: TestIterNormalizedEvents, async test patterns, AsyncMock patch usage — structural template for event + wire format tests)
    - .planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-PATTERNS.md (sections "Event mapping test pattern", "no-op turn control test pattern", "Audio format test pattern", "Tool result test pattern")
    - .planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-VALIDATION.md (Per-Task Verification Map — all QUAL-01 behaviors)
    - .planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-RESEARCH.md (Open Point 3 table of verified event names; Open Point 4 tool_call_id correction)
  </read_first>
  <action>
    Append the following test classes to tests/unit/realtime/test_realtime_elevenlabs.py (same file as Task 1).

    Class TestElevenLabsEventMapping:
      All methods call _make_client()._to_normalized_runtime_event(frame) and assert on result["type"].
      test_conversation_initiation_metadata_maps_to_session_ready: frame = {"type": "conversation_initiation_metadata", "conversation_initiation_metadata_event": {}} → SESSION_READY.
      test_audio_maps_to_response_audio_delta: frame = {"type": "audio", "audio_event": {"audio_base_64": "AAEC"}} → RESPONSE_AUDIO_DELTA.
      test_agent_response_maps_to_response_done: frame = {"type": "agent_response", "agent_response_event": {"agent_response": "hello"}} → RESPONSE_DONE.
      test_agent_response_complete_maps_to_response_done: frame = {"type": "agent_response_complete"} → RESPONSE_DONE (forward-compatible).
      test_user_transcript_maps_to_input_audio_committed: frame = {"type": "user_transcript"} → INPUT_AUDIO_COMMITTED.
      test_interruption_maps_to_input_speech_started: frame = {"type": "interruption", "interruption_event": {"event_id": 1}} → INPUT_SPEECH_STARTED.
      test_client_tool_call_maps_to_tool_call_completed: frame = {"type": "client_tool_call", "client_tool_call": {"tool_call_id": "c1", "tool_name": "fn", "parameters": "{}"}} → TOOL_CALL_COMPLETED.
      test_tool_call_completed_payload_has_call_id: same frame, result["payload"]["call_id"] == "c1".
      test_tool_call_completed_payload_has_name: result["payload"]["name"] == "fn".
      test_ping_returns_none_sentinel: frame = {"type": "ping", "ping_event": {"event_id": 42, "ping_ms": 10}} → result is None.
      test_agent_response_correction_maps_to_unhandled: frame = {"type": "agent_response_correction"} → UNHANDLED.
      test_unknown_event_maps_to_unhandled: frame = {"type": "xyz_unknown_event"} → UNHANDLED.
      test_missing_type_maps_to_unhandled: frame = {"payload": "no type key"} → UNHANDLED with source == "missing_type".

    Class TestElevenLabsAudioInput:
      All methods use @pytest.mark.asyncio and patch.object(client, "send_json", new_callable=AsyncMock).
      test_audio_chunk_format_has_user_audio_chunk_key: await client.append_client_audio(b"\x00\x01" * 8); sent = mock_send.call_args[0][0]; assert "user_audio_chunk" in sent.
      test_audio_chunk_format_no_type_key: same send — assert "type" not in sent. This is the CRITICAL wire-format assertion (Pitfall 1 from RESEARCH.md).
      test_audio_chunk_is_base64: import base64; assert sent["user_audio_chunk"] == base64.b64encode(b"\x00\x01" * 8).decode("ascii").
      test_empty_audio_is_noop: await client.append_client_audio(b""); mock_send.assert_not_called().

    Class TestElevenLabsTurnControl:
      All methods use @pytest.mark.asyncio and patch.object(client, "send_json", new_callable=AsyncMock).
      test_commit_client_turn_noop: await client.commit_client_turn(); mock_send.assert_not_called(). Docstring: "ElevenLabs server-side VAD — no client commit needed."
      test_create_response_noop: await client.create_response(); mock_send.assert_not_called().
      test_cancel_response_noop: await client.cancel_response(); mock_send.assert_not_called().

    Class TestElevenLabsToolResult:
      All methods use @pytest.mark.asyncio and patch.object(client, "send_json", new_callable=AsyncMock).
      test_tool_result_uses_client_tool_result_type: await client.submit_tool_result(call_id="c1", output="done"); sent = mock_send.call_args[0][0]; assert sent["type"] == "client_tool_result".
      test_tool_result_uses_tool_call_id_not_call_id: assert "tool_call_id" in sent; assert "call_id" not in sent. (CRITICAL: Pitfall 2 from RESEARCH.md — contract field call_id must map to wire field tool_call_id.)
      test_tool_result_tool_call_id_value: assert sent["tool_call_id"] == "c1".
      test_tool_result_result_field: assert sent["result"] == "done".
      test_tool_result_is_error_false: assert sent["is_error"] == False.

    After adding all classes, run full unit suite to confirm no regressions.
  </action>
  <verify>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && .venv/bin/python -m pytest tests/unit/realtime/test_realtime_elevenlabs.py -v --tb=short 2>&1 | tail -30</automated>
    <automated>cd /Users/picard/gitbase/PyPi-Projects/eq_chatbot_core && .venv/bin/python -m pytest tests/unit/realtime/ -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    All tests in test_realtime_elevenlabs.py pass (target: ~35 tests).
    Full tests/unit/realtime/ suite remains green (no regressions from existing providers).
    TestElevenLabsEventMapping covers all 13 wire event cases from RESEARCH.md Open Point 3.
    TestElevenLabsAudioInput: "type" not in sent payload is explicitly asserted (Pitfall 1 guard).
    TestElevenLabsToolResult: "tool_call_id" in sent and "call_id" not in sent is explicitly asserted (Pitfall 2 guard).
    TestElevenLabsTurnControl: mock_send.assert_not_called() for all three no-op methods.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → ElevenLabsRealtimeClient | Only constructor and pure methods called; no real WebSocket; mock_websockets_module autouse fixture prevents real network connections |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-02-01 | Information Disclosure | test_api_key_not_in_endpoint assertion | mitigate | assert _FAKE_KEY not in _connection_error_endpoint() — catches any future regression that leaks api_key into logged URL |
| T-03.1-02-02 | Tampering | user_audio_chunk wire format regression | mitigate | assert "type" not in sent payload — catches any OpenAI-style regression that adds a "type" key |
| T-03.1-02-03 | Tampering | tool_call_id vs call_id regression | mitigate | assert "tool_call_id" in sent AND "call_id" not in sent — catches field name drift |
| T-03.1-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase |
</threat_model>

<verification>
After both tasks complete:

1. Full unit suite green: .venv/bin/python -m pytest tests/unit/realtime/test_realtime_elevenlabs.py -v
2. No regressions: .venv/bin/python -m pytest tests/unit/realtime/ -q
3. Key assertions present (grep check):
   grep -c "tool_call_id.*not in\|not in.*tool_call_id\|assert.*tool_call_id.*not\|assert.*not.*tool_call_id" tests/unit/realtime/test_realtime_elevenlabs.py
   (must be >= 1)
4. Wire format assertion present:
   grep -c '"type".*not in\|not in.*"type"' tests/unit/realtime/test_realtime_elevenlabs.py
   (must be >= 1)
</verification>

<success_criteria>
- tests/unit/realtime/test_realtime_elevenlabs.py exists and all tests pass (target ~35 tests)
- No real network calls in any test (mock_websockets_module autouse fixture active)
- TestElevenLabsAudioInput.test_audio_chunk_format_no_type_key explicitly asserts "type" not in sent payload
- TestElevenLabsToolResult.test_tool_result_uses_tool_call_id_not_call_id explicitly asserts "tool_call_id" in sent and "call_id" not in sent
- TestConnectionErrorEndpoint.test_api_key_not_in_endpoint explicitly asserts _FAKE_KEY not in _connection_error_endpoint()
- TestElevenLabsTurnControl: commit_client_turn, create_response, cancel_response — mock_send.assert_not_called() for all three
- Full tests/unit/realtime/ suite remains green (no regression)
</success_criteria>

<output>
Create .planning/phases/03.1-elevenlabs-agents-realtime-provider/03.1-02-SUMMARY.md when done.
</output>
